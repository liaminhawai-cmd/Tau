/* three-gpu-pathtracer 0.0.23 + three-mesh-bvh 0.7.8 global build for Tau - generated by scripts/build-pathtracer-global.mjs */
var TAU_PT=(()=>{var Or=Object.defineProperty;var rs=Object.getOwnPropertyDescriptor;var os=Object.getOwnPropertyNames;var is=Object.prototype.hasOwnProperty;var jo=(i,e)=>{for(var t in e)Or(i,t,{get:e[t],enumerable:!0})},ns=(i,e,t,r)=>{if(e&&typeof e=="object"||typeof e=="function")for(let n of os(e))!is.call(i,n)&&n!==t&&Or(i,n,{get:()=>e[n],enumerable:!(r=rs(e,n))||r.enumerable});return i};var ss=i=>ns(Or({},"__esModule",{value:!0}),i);var ga={};jo(ga,{BlurredEnvMapGenerator:()=>Ho,CubeToEquirectGenerator:()=>We,DenoiseMaterial:()=>Wo,DynamicPathTracingSceneGenerator:()=>Ao,EquirectCamera:()=>Go,FogVolumeMaterial:()=>Vo,GradientEquirectTexture:()=>Ft,PathTracingRenderer:()=>pt,PathTracingSceneGenerator:()=>Ue,PathTracingSceneWorker:()=>Mo,PhysicalCamera:()=>At,PhysicalPathTracingMaterial:()=>Ct,PhysicalSpotLight:()=>Oo,ProceduralEquirectTexture:()=>It,ShapedAreaLight:()=>zo,WebGLPathTracer:()=>ko});var u=globalThis.THREE,ya=u.ACESFilmicToneMapping,ba=u.AddEquation,Ta=u.AddOperation,wa=u.AdditiveAnimationBlendMode,Xo=u.AdditiveBlending,Sa=u.AgXToneMapping,_a=u.AlphaFormat,Aa=u.AlwaysCompare,Ma=u.AlwaysDepth,Ra=u.AlwaysStencilFunc,Ca=u.AmbientLight,Ia=u.AnimationAction,Fa=u.AnimationClip,Ba=u.AnimationLoader,Pa=u.AnimationMixer,Da=u.AnimationObjectGroup,Ea=u.AnimationUtils,La=u.ArcCurve,Na=u.ArrayCamera,ka=u.ArrowHelper,Ga=u.AttachedBindMode,Oa=u.Audio,za=u.AudioAnalyser,Ua=u.AudioContext,Ha=u.AudioListener,Wa=u.AudioLoader,Va=u.AxesHelper,Bt=u.BackSide,qa=u.BasicDepthPacking,ja=u.BasicShadowMap,Yo=u.BatchedMesh,Xa=u.Bone,Ya=u.BooleanKeyframeTrack,$a=u.Box2,K=u.Box3,Qa=u.Box3Helper,Ka=u.BoxGeometry,Za=u.BoxHelper,X=u.BufferAttribute,J=u.BufferGeometry,Ja=u.BufferGeometryLoader,zr=u.ByteType,ec=u.Cache,$o=u.Camera,tc=u.CameraHelper,rc=u.CanvasTexture,oc=u.CapsuleGeometry,ic=u.CatmullRomCurve3,nc=u.CineonToneMapping,sc=u.CircleGeometry,ae=u.ClampToEdgeWrapping,Qo=u.Clock,pe=u.Color,ac=u.ColorKeyframeTrack,cc=u.ColorManagement,lc=u.CompressedArrayTexture,uc=u.CompressedCubeTexture,fc=u.CompressedTexture,pc=u.CompressedTextureLoader,mc=u.ConeGeometry,dc=u.ConstantAlphaFactor,hc=u.ConstantColorFactor,xc=u.Controls,gc=u.CubeCamera,vc=u.CubeReflectionMapping,yc=u.CubeRefractionMapping,bc=u.CubeTexture,Tc=u.CubeTextureLoader,wc=u.CubeUVReflectionMapping,Sc=u.CubicBezierCurve,_c=u.CubicBezierCurve3,Ac=u.CubicInterpolant,Mc=u.CullFaceBack,Rc=u.CullFaceFront,Cc=u.CullFaceFrontBack,Ic=u.CullFaceNone,Fc=u.Curve,Bc=u.CurvePath,Pc=u.CustomBlending,Dc=u.CustomToneMapping,Ec=u.CylinderGeometry,Lc=u.Cylindrical,Nc=u.Data3DTexture,Ko=u.DataArrayTexture,W=u.DataTexture,kc=u.DataTextureLoader,ie=u.DataUtils,Gc=u.DecrementStencilOp,Oc=u.DecrementWrapStencilOp,zc=u.DefaultLoadingManager,Uc=u.DepthFormat,Hc=u.DepthStencilFormat,Wc=u.DepthTexture,Vc=u.DetachedBindMode,qc=u.DirectionalLight,jc=u.DirectionalLightHelper,Xc=u.DiscreteInterpolant,Yc=u.DisplayP3ColorSpace,$c=u.DodecahedronGeometry,Pt=u.DoubleSide,Qc=u.DstAlphaFactor,Kc=u.DstColorFactor,Zc=u.DynamicCopyUsage,Jc=u.DynamicDrawUsage,el=u.DynamicReadUsage,tl=u.EdgesGeometry,rl=u.EllipseCurve,ol=u.EqualCompare,il=u.EqualDepth,nl=u.EqualStencilFunc,Ve=u.EquirectangularReflectionMapping,sl=u.EquirectangularRefractionMapping,al=u.Euler,cl=u.EventDispatcher,ll=u.ExtrudeGeometry,ul=u.FileLoader,fl=u.Float16BufferAttribute,Ur=u.Float32BufferAttribute,N=u.FloatType,pl=u.Fog,ml=u.FogExp2,dl=u.FramebufferTexture,dt=u.FrontSide,hl=u.Frustum,xl=u.GLBufferAttribute,gl=u.GLSL1,vl=u.GLSL3,yl=u.GreaterCompare,bl=u.GreaterDepth,Tl=u.GreaterEqualCompare,wl=u.GreaterEqualDepth,Sl=u.GreaterEqualStencilFunc,_l=u.GreaterStencilFunc,Al=u.GridHelper,as=u.Group,ee=u.HalfFloatType,Ml=u.HemisphereLight,Rl=u.HemisphereLightHelper,Cl=u.IcosahedronGeometry,Il=u.ImageBitmapLoader,Fl=u.ImageLoader,Bl=u.ImageUtils,Pl=u.IncrementStencilOp,Dl=u.IncrementWrapStencilOp,El=u.InstancedBufferAttribute,Ll=u.InstancedBufferGeometry,Nl=u.InstancedInterleavedBuffer,kl=u.InstancedMesh,Gl=u.Int16BufferAttribute,Ol=u.Int32BufferAttribute,zl=u.Int8BufferAttribute,Dt=u.IntType,Ul=u.InterleavedBuffer,Hl=u.InterleavedBufferAttribute,Wl=u.Interpolant,Vl=u.InterpolateDiscrete,ql=u.InterpolateLinear,jl=u.InterpolateSmooth,Xl=u.InvertStencilOp,Yl=u.KeepStencilOp,$l=u.KeyframeTrack,Ql=u.LOD,Kl=u.LatheGeometry,Zl=u.Layers,Jl=u.LessCompare,eu=u.LessDepth,tu=u.LessEqualCompare,ru=u.LessEqualDepth,ou=u.LessEqualStencilFunc,iu=u.LessStencilFunc,nu=u.Light,su=u.LightProbe,au=u.Line,ce=u.Line3,cs=u.LineBasicMaterial,cu=u.LineCurve,lu=u.LineCurve3,uu=u.LineDashedMaterial,fu=u.LineLoop,pu=u.LineSegments,mu=u.LinearDisplayP3ColorSpace,te=u.LinearFilter,du=u.LinearInterpolant,Zo=u.LinearMipMapLinearFilter,hu=u.LinearMipMapNearestFilter,xu=u.LinearMipmapLinearFilter,gu=u.LinearMipmapNearestFilter,vu=u.LinearSRGBColorSpace,yu=u.LinearToneMapping,bu=u.LinearTransfer,Tu=u.Loader,wu=u.LoaderUtils,Su=u.LoadingManager,_u=u.LoopOnce,Au=u.LoopPingPong,Mu=u.LoopRepeat,Ru=u.LuminanceAlphaFormat,Cu=u.LuminanceFormat,Iu=u.MOUSE,Fu=u.Material,Bu=u.MaterialLoader,Pu=u.MathUtils,Du=u.Matrix2,Hr=u.Matrix3,O=u.Matrix4,Eu=u.MaxEquation,Be=u.Mesh,Wr=u.MeshBasicMaterial,Lu=u.MeshDepthMaterial,Nu=u.MeshDistanceMaterial,ku=u.MeshLambertMaterial,Gu=u.MeshMatcapMaterial,Ou=u.MeshNormalMaterial,zu=u.MeshPhongMaterial,Uu=u.MeshPhysicalMaterial,Jo=u.MeshStandardMaterial,Hu=u.MeshToonMaterial,Wu=u.MinEquation,Vu=u.MirroredRepeatWrapping,qu=u.MixOperation,ju=u.MultiplyBlending,Xu=u.MultiplyOperation,k=u.NearestFilter,Yu=u.NearestMipMapLinearFilter,$u=u.NearestMipMapNearestFilter,Qu=u.NearestMipmapLinearFilter,Ku=u.NearestMipmapNearestFilter,Zu=u.NeutralToneMapping,Ju=u.NeverCompare,ef=u.NeverDepth,tf=u.NeverStencilFunc,me=u.NoBlending,rf=u.NoColorSpace,ei=u.NoToneMapping,of=u.NormalAnimationBlendMode,Et=u.NormalBlending,nf=u.NotEqualCompare,sf=u.NotEqualDepth,af=u.NotEqualStencilFunc,cf=u.NumberKeyframeTrack,ls=u.Object3D,lf=u.ObjectLoader,uf=u.ObjectSpaceNormalMap,ff=u.OctahedronGeometry,pf=u.OneFactor,mf=u.OneMinusConstantAlphaFactor,df=u.OneMinusConstantColorFactor,hf=u.OneMinusDstAlphaFactor,xf=u.OneMinusDstColorFactor,gf=u.OneMinusSrcAlphaFactor,vf=u.OneMinusSrcColorFactor,ti=u.OrthographicCamera,yf=u.P3Primaries,bf=u.PCFShadowMap,Tf=u.PCFSoftShadowMap,ri=u.PMREMGenerator,wf=u.Path,Lt=u.PerspectiveCamera,Nt=u.Plane,Sf=u.PlaneGeometry,_f=u.PlaneHelper,Af=u.PointLight,Mf=u.PointLightHelper,Rf=u.Points,Cf=u.PointsMaterial,If=u.PolarGridHelper,Ff=u.PolyhedronGeometry,Bf=u.PositionalAudio,Pf=u.PropertyBinding,Df=u.PropertyMixer,Ef=u.QuadraticBezierCurve,Lf=u.QuadraticBezierCurve3,oi=u.Quaternion,Nf=u.QuaternionKeyframeTrack,kf=u.QuaternionLinearInterpolant,Gf=u.RED_GREEN_RGTC2_Format,Of=u.RED_RGTC1_Format,ii=u.REVISION,zf=u.RGBADepthPacking,E=u.RGBAFormat,kt=u.RGBAIntegerFormat,Uf=u.RGBA_ASTC_10x10_Format,Hf=u.RGBA_ASTC_10x5_Format,Wf=u.RGBA_ASTC_10x6_Format,Vf=u.RGBA_ASTC_10x8_Format,qf=u.RGBA_ASTC_12x10_Format,jf=u.RGBA_ASTC_12x12_Format,Xf=u.RGBA_ASTC_4x4_Format,Yf=u.RGBA_ASTC_5x4_Format,$f=u.RGBA_ASTC_5x5_Format,Qf=u.RGBA_ASTC_6x5_Format,Kf=u.RGBA_ASTC_6x6_Format,Zf=u.RGBA_ASTC_8x5_Format,Jf=u.RGBA_ASTC_8x6_Format,ep=u.RGBA_ASTC_8x8_Format,tp=u.RGBA_BPTC_Format,rp=u.RGBA_ETC2_EAC_Format,op=u.RGBA_PVRTC_2BPPV1_Format,ip=u.RGBA_PVRTC_4BPPV1_Format,np=u.RGBA_S3TC_DXT1_Format,sp=u.RGBA_S3TC_DXT3_Format,ap=u.RGBA_S3TC_DXT5_Format,cp=u.RGBDepthPacking,lp=u.RGBFormat,up=u.RGBIntegerFormat,fp=u.RGB_BPTC_SIGNED_Format,pp=u.RGB_BPTC_UNSIGNED_Format,mp=u.RGB_ETC1_Format,dp=u.RGB_ETC2_Format,hp=u.RGB_PVRTC_2BPPV1_Format,xp=u.RGB_PVRTC_4BPPV1_Format,gp=u.RGB_S3TC_DXT1_Format,vp=u.RGDepthPacking,Gt=u.RGFormat,Ot=u.RGIntegerFormat,yp=u.RawShaderMaterial,us=u.Ray,bp=u.Raycaster,Tp=u.Rec709Primaries,ni=u.RectAreaLight,Pe=u.RedFormat,si=u.RedIntegerFormat,wp=u.ReinhardToneMapping,Sp=u.RenderTarget,le=u.RepeatWrapping,_p=u.ReplaceStencilOp,Ap=u.ReverseSubtractEquation,Mp=u.RingGeometry,Rp=u.SIGNED_RED_GREEN_RGTC2_Format,Cp=u.SIGNED_RED_RGTC1_Format,Ip=u.SRGBColorSpace,Fp=u.SRGBTransfer,ai=u.Scene,Bp=u.ShaderChunk,Pp=u.ShaderLib,_e=u.ShaderMaterial,Dp=u.ShadowMaterial,Ep=u.Shape,Lp=u.ShapeGeometry,Np=u.ShapePath,kp=u.ShapeUtils,ci=u.ShortType,Gp=u.Skeleton,Op=u.SkeletonHelper,zp=u.SkinnedMesh,li=u.Source,Vr=u.Sphere,Up=u.SphereGeometry,ui=u.Spherical,Hp=u.SphericalHarmonics3,Wp=u.SplineCurve,fi=u.SpotLight,Vp=u.SpotLightHelper,qp=u.Sprite,jp=u.SpriteMaterial,Xp=u.SrcAlphaFactor,Yp=u.SrcAlphaSaturateFactor,$p=u.SrcColorFactor,Qp=u.StaticCopyUsage,Kp=u.StaticDrawUsage,Zp=u.StaticReadUsage,Jp=u.StereoCamera,em=u.StreamCopyUsage,tm=u.StreamDrawUsage,rm=u.StreamReadUsage,om=u.StringKeyframeTrack,im=u.SubtractEquation,nm=u.SubtractiveBlending,sm=u.TOUCH,am=u.TangentSpaceNormalMap,cm=u.TetrahedronGeometry,lm=u.Texture,um=u.TextureLoader,fm=u.TextureUtils,pm=u.TorusGeometry,mm=u.TorusKnotGeometry,Ae=u.Triangle,dm=u.TriangleFanDrawMode,hm=u.TriangleStripDrawMode,xm=u.TrianglesDrawMode,gm=u.TubeGeometry,vm=u.UVMapping,ym=u.Uint16BufferAttribute,bm=u.Uint32BufferAttribute,Tm=u.Uint8BufferAttribute,wm=u.Uint8ClampedBufferAttribute,Sm=u.Uniform,_m=u.UniformsGroup,Am=u.UniformsLib,Mm=u.UniformsUtils,ht=u.UnsignedByteType,Rm=u.UnsignedInt248Type,Cm=u.UnsignedInt5999Type,De=u.UnsignedIntType,Im=u.UnsignedShort4444Type,Fm=u.UnsignedShort5551Type,pi=u.UnsignedShortType,Bm=u.VSMShadowMap,Y=u.Vector2,I=u.Vector3,Te=u.Vector4,Pm=u.VectorKeyframeTrack,Dm=u.VideoTexture,Em=u.WebGL3DRenderTarget,mi=u.WebGLArrayRenderTarget,Lm=u.WebGLCoordinateSystem,Nm=u.WebGLCubeRenderTarget,km=u.WebGLMultipleRenderTargets,ve=u.WebGLRenderTarget,Gm=u.WebGLRenderer,Om=u.WebGLUtils,zm=u.WebGPUCoordinateSystem,Um=u.WireframeGeometry,Hm=u.WrapAroundEnding,Wm=u.ZeroCurvatureEnding,Vm=u.ZeroFactor,qm=u.ZeroSlopeEnding,jm=u.ZeroStencilOp,Xm=u.createCanvasElement;var di=Math.pow(2,-24),zt=Symbol("SKIP_GENERATION");function qr(i){return i.index?i.index.count:i.attributes.position.count}function ye(i){return qr(i)/3}function jr(i,e=ArrayBuffer){return i>65535?new Uint32Array(new e(4*i)):new Uint16Array(new e(2*i))}function hi(i,e){if(!i.index){let t=i.attributes.position.count,r=e.useSharedArrayBuffer?SharedArrayBuffer:ArrayBuffer,n=jr(t,r);i.setIndex(new X(n,1));for(let s=0;s<t;s++)n[s]=s}}function Xr(i,e){let t=ye(i),r=e||i.drawRange,n=r.start/3,s=(r.start+r.count)/3,o=Math.max(0,n),c=Math.min(t,s)-o;return[{offset:Math.floor(o),count:Math.floor(c)}]}function Yr(i,e){if(!i.groups||!i.groups.length)return Xr(i,e);let t=[],r=new Set,n=e||i.drawRange,s=n.start/3,o=(n.start+n.count)/3;for(let l of i.groups){let m=l.start/3,p=(l.start+l.count)/3;r.add(Math.max(s,m)),r.add(Math.min(o,p))}let c=Array.from(r.values()).sort((l,m)=>l-m);for(let l=0;l<c.length-1;l++){let m=c[l],p=c[l+1];t.push({offset:Math.floor(m),count:Math.floor(p-m)})}return t}function xi(i,e){let t=ye(i),r=Yr(i,e).sort((o,c)=>o.offset-c.offset),n=r[r.length-1];n.count=Math.min(t-n.offset,n.count);let s=0;return r.forEach(({count:o})=>s+=o),t!==s}function Ut(i,e,t,r,n){let s=1/0,o=1/0,c=1/0,l=-1/0,m=-1/0,p=-1/0,f=1/0,a=1/0,h=1/0,x=-1/0,b=-1/0,d=-1/0;for(let g=e*6,v=(e+t)*6;g<v;g+=6){let y=i[g+0],w=i[g+1],T=y-w,S=y+w;T<s&&(s=T),S>l&&(l=S),y<f&&(f=y),y>x&&(x=y);let _=i[g+2],A=i[g+3],M=_-A,C=_+A;M<o&&(o=M),C>m&&(m=C),_<a&&(a=_),_>b&&(b=_);let F=i[g+4],R=i[g+5],B=F-R,P=F+R;B<c&&(c=B),P>p&&(p=P),F<h&&(h=F),F>d&&(d=F)}r[0]=s,r[1]=o,r[2]=c,r[3]=l,r[4]=m,r[5]=p,n[0]=f,n[1]=a,n[2]=h,n[3]=x,n[4]=b,n[5]=d}function gi(i,e=null,t=null,r=null){let n=i.attributes.position,s=i.index?i.index.array:null,o=ye(i),c=n.normalized,l;e===null?(l=new Float32Array(o*6*4),t=0,r=o):(l=e,t=t||0,r=r||o);let m=n.array,p=n.offset||0,f=3;n.isInterleavedBufferAttribute&&(f=n.data.stride);let a=["getX","getY","getZ"];for(let h=t;h<t+r;h++){let x=h*3,b=h*6,d=x+0,g=x+1,v=x+2;s&&(d=s[d],g=s[g],v=s[v]),c||(d=d*f+p,g=g*f+p,v=v*f+p);for(let y=0;y<3;y++){let w,T,S;c?(w=n[a[y]](d),T=n[a[y]](g),S=n[a[y]](v)):(w=m[d+y],T=m[g+y],S=m[v+y]);let _=w;T<_&&(_=T),S<_&&(_=S);let A=w;T>A&&(A=T),S>A&&(A=S);let M=(A-_)/2,C=y*2;l[b+C+0]=_+M,l[b+C+1]=M+(Math.abs(_)+M)*di}}return l}function G(i,e,t){return t.min.x=e[i],t.min.y=e[i+1],t.min.z=e[i+2],t.max.x=e[i+3],t.max.y=e[i+4],t.max.z=e[i+5],t}function $r(i){let e=-1,t=-1/0;for(let r=0;r<3;r++){let n=i[r+3]-i[r];n>t&&(t=n,e=r)}return e}function Qr(i,e){e.set(i)}function Kr(i,e,t){let r,n;for(let s=0;s<3;s++){let o=s+3;r=i[s],n=e[s],t[s]=r<n?r:n,r=i[o],n=e[o],t[o]=r>n?r:n}}function xt(i,e,t){for(let r=0;r<3;r++){let n=e[i+2*r],s=e[i+2*r+1],o=n-s,c=n+s;o<t[r]&&(t[r]=o),c>t[r+3]&&(t[r+3]=c)}}function qe(i){let e=i[3]-i[0],t=i[4]-i[1],r=i[5]-i[2];return 2*(e*t+t*r+r*e)}var we=32,ps=(i,e)=>i.candidate-e.candidate,Me=new Array(we).fill().map(()=>({count:0,bounds:new Float32Array(6),rightCacheBounds:new Float32Array(6),leftCacheBounds:new Float32Array(6),candidate:0})),Ht=new Float32Array(6);function yi(i,e,t,r,n,s){let o=-1,c=0;if(s===0)o=$r(e),o!==-1&&(c=(e[o]+e[o+3])/2);else if(s===1)o=$r(i),o!==-1&&(c=ms(t,r,n,o));else if(s===2){let l=qe(i),m=1.25*n,p=r*6,f=(r+n)*6;for(let a=0;a<3;a++){let h=e[a],d=(e[a+3]-h)/we;if(n<we/4){let g=[...Me];g.length=n;let v=0;for(let w=p;w<f;w+=6,v++){let T=g[v];T.candidate=t[w+2*a],T.count=0;let{bounds:S,leftCacheBounds:_,rightCacheBounds:A}=T;for(let M=0;M<3;M++)A[M]=1/0,A[M+3]=-1/0,_[M]=1/0,_[M+3]=-1/0,S[M]=1/0,S[M+3]=-1/0;xt(w,t,S)}g.sort(ps);let y=n;for(let w=0;w<y;w++){let T=g[w];for(;w+1<y&&g[w+1].candidate===T.candidate;)g.splice(w+1,1),y--}for(let w=p;w<f;w+=6){let T=t[w+2*a];for(let S=0;S<y;S++){let _=g[S];T>=_.candidate?xt(w,t,_.rightCacheBounds):(xt(w,t,_.leftCacheBounds),_.count++)}}for(let w=0;w<y;w++){let T=g[w],S=T.count,_=n-T.count,A=T.leftCacheBounds,M=T.rightCacheBounds,C=0;S!==0&&(C=qe(A)/l);let F=0;_!==0&&(F=qe(M)/l);let R=1+1.25*(C*S+F*_);R<m&&(o=a,m=R,c=T.candidate)}}else{for(let y=0;y<we;y++){let w=Me[y];w.count=0,w.candidate=h+d+y*d;let T=w.bounds;for(let S=0;S<3;S++)T[S]=1/0,T[S+3]=-1/0}for(let y=p;y<f;y+=6){let S=~~((t[y+2*a]-h)/d);S>=we&&(S=we-1);let _=Me[S];_.count++,xt(y,t,_.bounds)}let g=Me[we-1];Qr(g.bounds,g.rightCacheBounds);for(let y=we-2;y>=0;y--){let w=Me[y],T=Me[y+1];Kr(w.bounds,T.rightCacheBounds,w.rightCacheBounds)}let v=0;for(let y=0;y<we-1;y++){let w=Me[y],T=w.count,S=w.bounds,A=Me[y+1].rightCacheBounds;T!==0&&(v===0?Qr(S,Ht):Kr(S,Ht,Ht)),v+=T;let M=0,C=0;v!==0&&(M=qe(Ht)/l);let F=n-v;F!==0&&(C=qe(A)/l);let R=1+1.25*(M*v+C*F);R<m&&(o=a,m=R,c=w.candidate)}}}}else console.warn(`MeshBVH: Invalid build strategy value ${s} used.`);return{axis:o,pos:c}}function ms(i,e,t,r){let n=0;for(let s=e,o=e+t;s<o;s++)n+=i[s*6+r*2];return n/t}var je=class{constructor(){this.boundingData=new Float32Array(6)}};function bi(i,e,t,r,n,s){let o=r,c=r+n-1,l=s.pos,m=s.axis*2;for(;;){for(;o<=c&&t[o*6+m]<l;)o++;for(;o<=c&&t[c*6+m]>=l;)c--;if(o<c){for(let p=0;p<3;p++){let f=e[o*3+p];e[o*3+p]=e[c*3+p],e[c*3+p]=f}for(let p=0;p<6;p++){let f=t[o*6+p];t[o*6+p]=t[c*6+p],t[c*6+p]=f}o++,c--}else return o}}function Ti(i,e,t,r,n,s){let o=r,c=r+n-1,l=s.pos,m=s.axis*2;for(;;){for(;o<=c&&t[o*6+m]<l;)o++;for(;o<=c&&t[c*6+m]>=l;)c--;if(o<c){let p=i[o];i[o]=i[c],i[c]=p;for(let f=0;f<6;f++){let a=t[o*6+f];t[o*6+f]=t[c*6+f],t[c*6+f]=a}o++,c--}else return o}}function U(i,e){return e[i+15]===65535}function V(i,e){return e[i+6]}function q(i,e){return e[i+14]}function Q(i){return i+8}function $(i,e){return e[i+6]}function Xe(i,e){return e[i+7]}var wi,gt,jt,Si,ds=Math.pow(2,32);function Xt(i){return"count"in i?1:1+Xt(i.left)+Xt(i.right)}function _i(i,e,t){return wi=new Float32Array(t),gt=new Uint32Array(t),jt=new Uint16Array(t),Si=new Uint8Array(t),Jr(i,e)}function Jr(i,e){let t=i/4,r=i/2,n="count"in e,s=e.boundingData;for(let o=0;o<6;o++)wi[t+o]=s[o];if(n)if(e.buffer){let o=e.buffer;Si.set(new Uint8Array(o),i);for(let c=i,l=i+o.byteLength;c<l;c+=32){let m=c/2;U(m,jt)||(gt[c/4+6]+=t)}return i+o.byteLength}else{let o=e.offset,c=e.count;return gt[t+6]=o,jt[r+14]=c,jt[r+15]=65535,i+32}else{let o=e.left,c=e.right,l=e.splitAxis,m;if(m=Jr(i+32,o),m/4>ds)throw new Error("MeshBVH: Cannot store child pointer greater than 32 bits.");return gt[t+6]=m/4,m=Jr(m,c),gt[t+7]=l,m}}function hs(i,e){let t=(i.index?i.index.count:i.attributes.position.count)/3,r=t>2**16,n=r?4:2,s=e?new SharedArrayBuffer(t*n):new ArrayBuffer(t*n),o=r?new Uint32Array(s):new Uint16Array(s);for(let c=0,l=o.length;c<l;c++)o[c]=c;return o}function xs(i,e,t,r,n){let{maxDepth:s,verbose:o,maxLeafTris:c,strategy:l,onProgress:m,indirect:p}=n,f=i._indirectBuffer,a=i.geometry,h=a.index?a.index.array:null,x=p?Ti:bi,b=ye(a),d=new Float32Array(6),g=!1,v=new je;return Ut(e,t,r,v.boundingData,d),w(v,t,r,d),v;function y(T){m&&m(T/b)}function w(T,S,_,A=null,M=0){if(!g&&M>=s&&(g=!0,o&&(console.warn(`MeshBVH: Max depth of ${s} reached when generating BVH. Consider increasing maxDepth.`),console.warn(a))),_<=c||M>=s)return y(S+_),T.offset=S,T.count=_,T;let C=yi(T.boundingData,A,e,S,_,l);if(C.axis===-1)return y(S+_),T.offset=S,T.count=_,T;let F=x(f,h,e,S,_,C);if(F===S||F===S+_)y(S+_),T.offset=S,T.count=_;else{T.splitAxis=C.axis;let R=new je,B=S,P=F-S;T.left=R,Ut(e,B,P,R.boundingData,d),w(R,B,P,d,M+1);let D=new je,H=F,ge=_-P;T.right=D,Ut(e,H,ge,D.boundingData,d),w(D,H,ge,d,M+1)}return T}}function Ai(i,e){let t=i.geometry;e.indirect&&(i._indirectBuffer=hs(t,e.useSharedArrayBuffer),xi(t,e.range)&&!e.verbose&&console.warn('MeshBVH: Provided geometry contains groups or a range that do not fully span the vertex contents while using the "indirect" option. BVH may incorrectly report intersections on unrendered portions of the geometry.')),i._indirectBuffer||hi(t,e);let r=e.useSharedArrayBuffer?SharedArrayBuffer:ArrayBuffer,n=gi(t),s=e.indirect?Xr(t,e.range):Yr(t,e.range);i._roots=s.map(o=>{let c=xs(i,n,o.offset,o.count,e),l=Xt(c),m=new r(32*l);return _i(0,c,m),m})}var ne=class{constructor(){this.min=1/0,this.max=-1/0}setFromPointsField(e,t){let r=1/0,n=-1/0;for(let s=0,o=e.length;s<o;s++){let l=e[s][t];r=l<r?l:r,n=l>n?l:n}this.min=r,this.max=n}setFromPoints(e,t){let r=1/0,n=-1/0;for(let s=0,o=t.length;s<o;s++){let c=t[s],l=e.dot(c);r=l<r?l:r,n=l>n?l:n}this.min=r,this.max=n}isSeparated(e){return this.min>e.max||e.min>this.max}};ne.prototype.setFromBox=function(){let i=new I;return function(t,r){let n=r.min,s=r.max,o=1/0,c=-1/0;for(let l=0;l<=1;l++)for(let m=0;m<=1;m++)for(let p=0;p<=1;p++){i.x=n.x*l+s.x*(1-l),i.y=n.y*m+s.y*(1-m),i.z=n.z*p+s.z*(1-p);let f=t.dot(i);o=Math.min(f,o),c=Math.max(f,c)}this.min=o,this.max=c}}();var Td=function(){let i=new ne;return function(t,r){let n=t.points,s=t.satAxes,o=t.satBounds,c=r.points,l=r.satAxes,m=r.satBounds;for(let p=0;p<3;p++){let f=o[p],a=s[p];if(i.setFromPoints(a,c),f.isSeparated(i))return!1}for(let p=0;p<3;p++){let f=m[p],a=l[p];if(i.setFromPoints(a,n),f.isSeparated(i))return!1}}}();var gs=function(){let i=new I,e=new I,t=new I;return function(n,s,o){let c=n.start,l=i,m=s.start,p=e;t.subVectors(c,m),i.subVectors(n.end,n.start),e.subVectors(s.end,s.start);let f=t.dot(p),a=p.dot(l),h=p.dot(p),x=t.dot(l),d=l.dot(l)*h-a*a,g,v;d!==0?g=(f*a-x*h)/d:g=0,v=(f+g*a)/h,o.x=g,o.y=v}}(),vt=function(){let i=new Y,e=new I,t=new I;return function(n,s,o,c){gs(n,s,i);let l=i.x,m=i.y;if(l>=0&&l<=1&&m>=0&&m<=1){n.at(l,o),s.at(m,c);return}else if(l>=0&&l<=1){m<0?s.at(0,c):s.at(1,c),n.closestPointToPoint(c,!0,o);return}else if(m>=0&&m<=1){l<0?n.at(0,o):n.at(1,o),s.closestPointToPoint(o,!0,c);return}else{let p;l<0?p=n.start:p=n.end;let f;m<0?f=s.start:f=s.end;let a=e,h=t;if(n.closestPointToPoint(f,!0,e),s.closestPointToPoint(p,!0,t),a.distanceToSquared(f)<=h.distanceToSquared(p)){o.copy(a),c.copy(f);return}else{o.copy(p),c.copy(h);return}}}}(),Mi=function(){let i=new I,e=new I,t=new Nt,r=new ce;return function(s,o){let{radius:c,center:l}=s,{a:m,b:p,c:f}=o;if(r.start=m,r.end=p,r.closestPointToPoint(l,!0,i).distanceTo(l)<=c||(r.start=m,r.end=f,r.closestPointToPoint(l,!0,i).distanceTo(l)<=c)||(r.start=p,r.end=f,r.closestPointToPoint(l,!0,i).distanceTo(l)<=c))return!0;let b=o.getPlane(t);if(Math.abs(b.distanceToPoint(l))<=c){let g=b.projectPoint(l,e);if(o.containsPoint(g))return!0}return!1}}();var vs=1e-15;function eo(i){return Math.abs(i)<vs}var Z=class extends Ae{constructor(...e){super(...e),this.isExtendedTriangle=!0,this.satAxes=new Array(4).fill().map(()=>new I),this.satBounds=new Array(4).fill().map(()=>new ne),this.points=[this.a,this.b,this.c],this.sphere=new Vr,this.plane=new Nt,this.needsUpdate=!0}intersectsSphere(e){return Mi(e,this)}update(){let e=this.a,t=this.b,r=this.c,n=this.points,s=this.satAxes,o=this.satBounds,c=s[0],l=o[0];this.getNormal(c),l.setFromPoints(c,n);let m=s[1],p=o[1];m.subVectors(e,t),p.setFromPoints(m,n);let f=s[2],a=o[2];f.subVectors(t,r),a.setFromPoints(f,n);let h=s[3],x=o[3];h.subVectors(r,e),x.setFromPoints(h,n),this.sphere.setFromPoints(this.points),this.plane.setFromNormalAndCoplanarPoint(c,e),this.needsUpdate=!1}};Z.prototype.closestPointToSegment=function(){let i=new I,e=new I,t=new ce;return function(n,s=null,o=null){let{start:c,end:l}=n,m=this.points,p,f=1/0;for(let a=0;a<3;a++){let h=(a+1)%3;t.start.copy(m[a]),t.end.copy(m[h]),vt(t,n,i,e),p=i.distanceToSquared(e),p<f&&(f=p,s&&s.copy(i),o&&o.copy(e))}return this.closestPointToPoint(c,i),p=c.distanceToSquared(i),p<f&&(f=p,s&&s.copy(i),o&&o.copy(c)),this.closestPointToPoint(l,i),p=l.distanceToSquared(i),p<f&&(f=p,s&&s.copy(i),o&&o.copy(l)),Math.sqrt(f)}}();Z.prototype.intersectsTriangle=function(){let i=new Z,e=new Array(3),t=new Array(3),r=new ne,n=new ne,s=new I,o=new I,c=new I,l=new I,m=new I,p=new ce,f=new ce,a=new ce,h=new I;function x(b,d,g){let v=b.points,y=0,w=-1;for(let T=0;T<3;T++){let{start:S,end:_}=p;S.copy(v[T]),_.copy(v[(T+1)%3]),p.delta(o);let A=eo(d.distanceToPoint(S));if(eo(d.normal.dot(o))&&A){g.copy(p),y=2;break}let M=d.intersectLine(p,h);if(!M&&A&&h.copy(S),(M||A)&&!eo(h.distanceTo(_))){if(y<=1)(y===1?g.start:g.end).copy(h),A&&(w=y);else if(y>=2){(w===1?g.start:g.end).copy(h),y=2;break}if(y++,y===2&&w===-1)break}}return y}return function(d,g=null,v=!1){this.needsUpdate&&this.update(),d.isExtendedTriangle?d.needsUpdate&&d.update():(i.copy(d),i.update(),d=i);let y=this.plane,w=d.plane;if(Math.abs(y.normal.dot(w.normal))>1-1e-10){let T=this.satBounds,S=this.satAxes;t[0]=d.a,t[1]=d.b,t[2]=d.c;for(let M=0;M<4;M++){let C=T[M],F=S[M];if(r.setFromPoints(F,t),C.isSeparated(r))return!1}let _=d.satBounds,A=d.satAxes;e[0]=this.a,e[1]=this.b,e[2]=this.c;for(let M=0;M<4;M++){let C=_[M],F=A[M];if(r.setFromPoints(F,e),C.isSeparated(r))return!1}for(let M=0;M<4;M++){let C=S[M];for(let F=0;F<4;F++){let R=A[F];if(s.crossVectors(C,R),r.setFromPoints(s,e),n.setFromPoints(s,t),r.isSeparated(n))return!1}}return g&&(v||console.warn("ExtendedTriangle.intersectsTriangle: Triangles are coplanar which does not support an output edge. Setting edge to 0, 0, 0."),g.start.set(0,0,0),g.end.set(0,0,0)),!0}else{let T=x(this,w,f);if(T===1&&d.containsPoint(f.end))return g&&(g.start.copy(f.end),g.end.copy(f.end)),!0;if(T!==2)return!1;let S=x(d,y,a);if(S===1&&this.containsPoint(a.end))return g&&(g.start.copy(a.end),g.end.copy(a.end)),!0;if(S!==2)return!1;if(f.delta(c),a.delta(l),c.dot(l)<0){let B=a.start;a.start=a.end,a.end=B}let _=f.start.dot(c),A=f.end.dot(c),M=a.start.dot(c),C=a.end.dot(c),F=A<M,R=_<C;return _!==C&&M!==A&&F===R?!1:(g&&(m.subVectors(f.start,a.start),m.dot(c)>0?g.start.copy(f.start):g.start.copy(a.start),m.subVectors(f.end,a.end),m.dot(c)<0?g.end.copy(f.end):g.end.copy(a.end)),!0)}}}();Z.prototype.distanceToPoint=function(){let i=new I;return function(t){return this.closestPointToPoint(t,i),t.distanceTo(i)}}();Z.prototype.distanceToTriangle=function(){let i=new I,e=new I,t=["a","b","c"],r=new ce,n=new ce;return function(o,c=null,l=null){let m=c||l?r:null;if(this.intersectsTriangle(o,m))return(c||l)&&(c&&m.getCenter(c),l&&m.getCenter(l)),0;let p=1/0;for(let f=0;f<3;f++){let a,h=t[f],x=o[h];this.closestPointToPoint(x,i),a=x.distanceToSquared(i),a<p&&(p=a,c&&c.copy(i),l&&l.copy(x));let b=this[h];o.closestPointToPoint(b,i),a=b.distanceToSquared(i),a<p&&(p=a,c&&c.copy(b),l&&l.copy(i))}for(let f=0;f<3;f++){let a=t[f],h=t[(f+1)%3];r.set(this[a],this[h]);for(let x=0;x<3;x++){let b=t[x],d=t[(x+1)%3];n.set(o[b],o[d]),vt(r,n,i,e);let g=i.distanceToSquared(e);g<p&&(p=g,c&&c.copy(i),l&&l.copy(e))}}return Math.sqrt(p)}}();var j=class{constructor(e,t,r){this.isOrientedBox=!0,this.min=new I,this.max=new I,this.matrix=new O,this.invMatrix=new O,this.points=new Array(8).fill().map(()=>new I),this.satAxes=new Array(3).fill().map(()=>new I),this.satBounds=new Array(3).fill().map(()=>new ne),this.alignedSatBounds=new Array(3).fill().map(()=>new ne),this.needsUpdate=!1,e&&this.min.copy(e),t&&this.max.copy(t),r&&this.matrix.copy(r)}set(e,t,r){this.min.copy(e),this.max.copy(t),this.matrix.copy(r),this.needsUpdate=!0}copy(e){this.min.copy(e.min),this.max.copy(e.max),this.matrix.copy(e.matrix),this.needsUpdate=!0}};j.prototype.update=function(){return function(){let e=this.matrix,t=this.min,r=this.max,n=this.points;for(let m=0;m<=1;m++)for(let p=0;p<=1;p++)for(let f=0;f<=1;f++){let a=1*m|2*p|4*f,h=n[a];h.x=m?r.x:t.x,h.y=p?r.y:t.y,h.z=f?r.z:t.z,h.applyMatrix4(e)}let s=this.satBounds,o=this.satAxes,c=n[0];for(let m=0;m<3;m++){let p=o[m],f=s[m],a=1<<m,h=n[a];p.subVectors(c,h),f.setFromPoints(p,n)}let l=this.alignedSatBounds;l[0].setFromPointsField(n,"x"),l[1].setFromPointsField(n,"y"),l[2].setFromPointsField(n,"z"),this.invMatrix.copy(this.matrix).invert(),this.needsUpdate=!1}}();j.prototype.intersectsBox=function(){let i=new ne;return function(t){this.needsUpdate&&this.update();let r=t.min,n=t.max,s=this.satBounds,o=this.satAxes,c=this.alignedSatBounds;if(i.min=r.x,i.max=n.x,c[0].isSeparated(i)||(i.min=r.y,i.max=n.y,c[1].isSeparated(i))||(i.min=r.z,i.max=n.z,c[2].isSeparated(i)))return!1;for(let l=0;l<3;l++){let m=o[l],p=s[l];if(i.setFromBox(m,t),p.isSeparated(i))return!1}return!0}}();j.prototype.intersectsTriangle=function(){let i=new Z,e=new Array(3),t=new ne,r=new ne,n=new I;return function(o){this.needsUpdate&&this.update(),o.isExtendedTriangle?o.needsUpdate&&o.update():(i.copy(o),i.update(),o=i);let c=this.satBounds,l=this.satAxes;e[0]=o.a,e[1]=o.b,e[2]=o.c;for(let a=0;a<3;a++){let h=c[a],x=l[a];if(t.setFromPoints(x,e),h.isSeparated(t))return!1}let m=o.satBounds,p=o.satAxes,f=this.points;for(let a=0;a<3;a++){let h=m[a],x=p[a];if(t.setFromPoints(x,f),h.isSeparated(t))return!1}for(let a=0;a<3;a++){let h=l[a];for(let x=0;x<4;x++){let b=p[x];if(n.crossVectors(h,b),t.setFromPoints(n,e),r.setFromPoints(n,f),t.isSeparated(r))return!1}}return!0}}();j.prototype.closestPointToPoint=function(){return function(e,t){return this.needsUpdate&&this.update(),t.copy(e).applyMatrix4(this.invMatrix).clamp(this.min,this.max).applyMatrix4(this.matrix),t}}();j.prototype.distanceToPoint=function(){let i=new I;return function(t){return this.closestPointToPoint(t,i),t.distanceTo(i)}}();j.prototype.distanceToBox=function(){let i=["x","y","z"],e=new Array(12).fill().map(()=>new ce),t=new Array(12).fill().map(()=>new ce),r=new I,n=new I;return function(o,c=0,l=null,m=null){if(this.needsUpdate&&this.update(),this.intersectsBox(o))return(l||m)&&(o.getCenter(n),this.closestPointToPoint(n,r),o.closestPointToPoint(r,n),l&&l.copy(r),m&&m.copy(n)),0;let p=c*c,f=o.min,a=o.max,h=this.points,x=1/0;for(let d=0;d<8;d++){let g=h[d];n.copy(g).clamp(f,a);let v=g.distanceToSquared(n);if(v<x&&(x=v,l&&l.copy(g),m&&m.copy(n),v<p))return Math.sqrt(v)}let b=0;for(let d=0;d<3;d++)for(let g=0;g<=1;g++)for(let v=0;v<=1;v++){let y=(d+1)%3,w=(d+2)%3,T=g<<y|v<<w,S=1<<d|g<<y|v<<w,_=h[T],A=h[S];e[b].set(_,A);let C=i[d],F=i[y],R=i[w],B=t[b],P=B.start,D=B.end;P[C]=f[C],P[F]=g?f[F]:a[F],P[R]=v?f[R]:a[F],D[C]=a[C],D[F]=g?f[F]:a[F],D[R]=v?f[R]:a[F],b++}for(let d=0;d<=1;d++)for(let g=0;g<=1;g++)for(let v=0;v<=1;v++){n.x=d?a.x:f.x,n.y=g?a.y:f.y,n.z=v?a.z:f.z,this.closestPointToPoint(n,r);let y=n.distanceToSquared(r);if(y<x&&(x=y,l&&l.copy(r),m&&m.copy(n),y<p))return Math.sqrt(y)}for(let d=0;d<12;d++){let g=e[d];for(let v=0;v<12;v++){let y=t[v];vt(g,y,r,n);let w=r.distanceToSquared(n);if(w<x&&(x=w,l&&l.copy(r),m&&m.copy(n),w<p))return Math.sqrt(w)}}return Math.sqrt(x)}}();var Re=class{constructor(e){this._getNewPrimitive=e,this._primitives=[]}getPrimitive(){let e=this._primitives;return e.length===0?this._getNewPrimitive():e.pop()}releasePrimitive(e){this._primitives.push(e)}};var to=class extends Re{constructor(){super(()=>new Z)}},re=new to;var ro=class{constructor(){this.float32Array=null,this.uint16Array=null,this.uint32Array=null;let e=[],t=null;this.setBuffer=r=>{t&&e.push(t),t=r,this.float32Array=new Float32Array(r),this.uint16Array=new Uint16Array(r),this.uint32Array=new Uint32Array(r)},this.clearBuffer=()=>{t=null,this.float32Array=null,this.uint16Array=null,this.uint32Array=null,e.length!==0&&this.setBuffer(e.pop())}}},L=new ro;var Ce,$e,Ye=[],Yt=new Re(()=>new K);function Ri(i,e,t,r,n,s){Ce=Yt.getPrimitive(),$e=Yt.getPrimitive(),Ye.push(Ce,$e),L.setBuffer(i._roots[e]);let o=io(0,i.geometry,t,r,n,s);L.clearBuffer(),Yt.releasePrimitive(Ce),Yt.releasePrimitive($e),Ye.pop(),Ye.pop();let c=Ye.length;return c>0&&($e=Ye[c-1],Ce=Ye[c-2]),o}function io(i,e,t,r,n=null,s=0,o=0){let{float32Array:c,uint16Array:l,uint32Array:m}=L,p=i*2;if(U(p,l)){let a=V(i,m),h=q(p,l);return G(i,c,Ce),r(a,h,!1,o,s+i,Ce)}else{let C=function(R){let{uint16Array:B,uint32Array:P}=L,D=R*2;for(;!U(D,B);)R=Q(R),D=R*2;return V(R,P)},F=function(R){let{uint16Array:B,uint32Array:P}=L,D=R*2;for(;!U(D,B);)R=$(R,P),D=R*2;return V(R,P)+q(D,B)},a=Q(i),h=$(i,m),x=a,b=h,d,g,v,y;if(n&&(v=Ce,y=$e,G(x,c,v),G(b,c,y),d=n(v),g=n(y),g<d)){x=h,b=a;let R=d;d=g,g=R,v=y}v||(v=Ce,G(x,c,v));let w=U(x*2,l),T=t(v,w,d,o+1,s+x),S;if(T===2){let R=C(x),P=F(x)-R;S=r(R,P,!0,o+1,s+x,v)}else S=T&&io(x,e,t,r,n,s,o+1);if(S)return!0;y=$e,G(b,c,y);let _=U(b*2,l),A=t(y,_,g,o+1,s+b),M;if(A===2){let R=C(b),P=F(b)-R;M=r(R,P,!0,o+1,s+b,y)}else M=A&&io(b,e,t,r,n,s,o+1);return!!M}}var yt=new I,no=new I;function Ci(i,e,t={},r=0,n=1/0){let s=r*r,o=n*n,c=1/0,l=null;if(i.shapecast({boundsTraverseOrder:p=>(yt.copy(e).clamp(p.min,p.max),yt.distanceToSquared(e)),intersectsBounds:(p,f,a)=>a<c&&a<o,intersectsTriangle:(p,f)=>{p.closestPointToPoint(e,yt);let a=e.distanceToSquared(yt);return a<c&&(no.copy(yt),c=a,l=f),a<s}}),c===1/0)return null;let m=Math.sqrt(c);return t.point?t.point.copy(no):t.point=no.clone(),t.distance=m,t.faceIndex=l,t}var Qe=new I,Ke=new I,Ze=new I,Qt=new Y,Kt=new Y,Zt=new Y,Ii=new I,Fi=new I,Bi=new I,Jt=new I;function ys(i,e,t,r,n,s,o,c){let l;if(s===Bt?l=i.intersectTriangle(r,t,e,!0,n):l=i.intersectTriangle(e,t,r,s!==Pt,n),l===null)return null;let m=i.origin.distanceTo(n);return m<o||m>c?null:{distance:m,point:n.clone()}}function bs(i,e,t,r,n,s,o,c,l,m,p){Qe.fromBufferAttribute(e,s),Ke.fromBufferAttribute(e,o),Ze.fromBufferAttribute(e,c);let f=ys(i,Qe,Ke,Ze,Jt,l,m,p);if(f){r&&(Qt.fromBufferAttribute(r,s),Kt.fromBufferAttribute(r,o),Zt.fromBufferAttribute(r,c),f.uv=Ae.getInterpolation(Jt,Qe,Ke,Ze,Qt,Kt,Zt,new Y)),n&&(Qt.fromBufferAttribute(n,s),Kt.fromBufferAttribute(n,o),Zt.fromBufferAttribute(n,c),f.uv1=Ae.getInterpolation(Jt,Qe,Ke,Ze,Qt,Kt,Zt,new Y)),t&&(Ii.fromBufferAttribute(t,s),Fi.fromBufferAttribute(t,o),Bi.fromBufferAttribute(t,c),f.normal=Ae.getInterpolation(Jt,Qe,Ke,Ze,Ii,Fi,Bi,new I),f.normal.dot(i.direction)>0&&f.normal.multiplyScalar(-1));let a={a:s,b:o,c,normal:new I,materialIndex:0};Ae.getNormal(Qe,Ke,Ze,a.normal),f.face=a,f.faceIndex=s}return f}function Je(i,e,t,r,n,s,o){let c=r*3,l=c+0,m=c+1,p=c+2,f=i.index;i.index&&(l=f.getX(l),m=f.getX(m),p=f.getX(p));let{position:a,normal:h,uv:x,uv1:b}=i.attributes,d=bs(t,a,h,x,b,l,m,p,e,s,o);return d?(d.faceIndex=r,n&&n.push(d),d):null}function z(i,e,t,r){let n=i.a,s=i.b,o=i.c,c=e,l=e+1,m=e+2;t&&(c=t.getX(c),l=t.getX(l),m=t.getX(m)),n.x=r.getX(c),n.y=r.getY(c),n.z=r.getZ(c),s.x=r.getX(l),s.y=r.getY(l),s.z=r.getZ(l),o.x=r.getX(m),o.y=r.getY(m),o.z=r.getZ(m)}function Pi(i,e,t,r,n,s,o,c){let{geometry:l,_indirectBuffer:m}=i;for(let p=r,f=r+n;p<f;p++)Je(l,e,t,p,s,o,c)}function Di(i,e,t,r,n,s,o){let{geometry:c,_indirectBuffer:l}=i,m=1/0,p=null;for(let f=r,a=r+n;f<a;f++){let h;h=Je(c,e,t,f,null,s,o),h&&h.distance<m&&(p=h,m=h.distance)}return p}function Ei(i,e,t,r,n,s,o){let{geometry:c}=t,{index:l}=c,m=c.attributes.position;for(let p=i,f=e+i;p<f;p++){let a;if(a=p,z(o,a*3,l,m),o.needsUpdate=!0,r(o,a,n,s))return!0}return!1}function Li(i,e=null){e&&Array.isArray(e)&&(e=new Set(e));let t=i.geometry,r=t.index?t.index.array:null,n=t.attributes.position,s,o,c,l,m=0,p=i._roots;for(let a=0,h=p.length;a<h;a++)s=p[a],o=new Uint32Array(s),c=new Uint16Array(s),l=new Float32Array(s),f(0,m),m+=s.byteLength;function f(a,h,x=!1){let b=a*2;if(c[b+15]===65535){let g=o[a+6],v=c[b+14],y=1/0,w=1/0,T=1/0,S=-1/0,_=-1/0,A=-1/0;for(let M=3*g,C=3*(g+v);M<C;M++){let F=r[M],R=n.getX(F),B=n.getY(F),P=n.getZ(F);R<y&&(y=R),R>S&&(S=R),B<w&&(w=B),B>_&&(_=B),P<T&&(T=P),P>A&&(A=P)}return l[a+0]!==y||l[a+1]!==w||l[a+2]!==T||l[a+3]!==S||l[a+4]!==_||l[a+5]!==A?(l[a+0]=y,l[a+1]=w,l[a+2]=T,l[a+3]=S,l[a+4]=_,l[a+5]=A,!0):!1}else{let g=a+8,v=o[a+6],y=g+h,w=v+h,T=x,S=!1,_=!1;e?T||(S=e.has(y),_=e.has(w),T=!S&&!_):(S=!0,_=!0);let A=T||S,M=T||_,C=!1;A&&(C=f(g,h,T));let F=!1;M&&(F=f(v,h,T));let R=C||F;if(R)for(let B=0;B<3;B++){let P=g+B,D=v+B,H=l[P],ge=l[P+3],be=l[D],Se=l[D+3];l[a+B]=H<be?H:be,l[a+B+3]=ge>Se?ge:Se}return R}}}function ue(i,e,t,r,n){let s,o,c,l,m,p,f=1/t.direction.x,a=1/t.direction.y,h=1/t.direction.z,x=t.origin.x,b=t.origin.y,d=t.origin.z,g=e[i],v=e[i+3],y=e[i+1],w=e[i+3+1],T=e[i+2],S=e[i+3+2];return f>=0?(s=(g-x)*f,o=(v-x)*f):(s=(v-x)*f,o=(g-x)*f),a>=0?(c=(y-b)*a,l=(w-b)*a):(c=(w-b)*a,l=(y-b)*a),s>l||c>o||((c>s||isNaN(s))&&(s=c),(l<o||isNaN(o))&&(o=l),h>=0?(m=(T-d)*h,p=(S-d)*h):(m=(S-d)*h,p=(T-d)*h),s>p||m>o)?!1:((m>s||s!==s)&&(s=m),(p<o||o!==o)&&(o=p),s<=n&&o>=r)}function Ni(i,e,t,r,n,s,o,c){let{geometry:l,_indirectBuffer:m}=i;for(let p=r,f=r+n;p<f;p++){let a=m?m[p]:p;Je(l,e,t,a,s,o,c)}}function ki(i,e,t,r,n,s,o){let{geometry:c,_indirectBuffer:l}=i,m=1/0,p=null;for(let f=r,a=r+n;f<a;f++){let h;h=Je(c,e,t,l?l[f]:f,null,s,o),h&&h.distance<m&&(p=h,m=h.distance)}return p}function Gi(i,e,t,r,n,s,o){let{geometry:c}=t,{index:l}=c,m=c.attributes.position;for(let p=i,f=e+i;p<f;p++){let a;if(a=t.resolveTriangleIndex(p),z(o,a*3,l,m),o.needsUpdate=!0,r(o,a,n,s))return!0}return!1}function Oi(i,e,t,r,n,s,o){L.setBuffer(i._roots[e]),so(0,i,t,r,n,s,o),L.clearBuffer()}function so(i,e,t,r,n,s,o){let{float32Array:c,uint16Array:l,uint32Array:m}=L,p=i*2;if(U(p,l)){let a=V(i,m),h=q(p,l);Pi(e,t,r,a,h,n,s,o)}else{let a=Q(i);ue(a,c,r,s,o)&&so(a,e,t,r,n,s,o);let h=$(i,m);ue(h,c,r,s,o)&&so(h,e,t,r,n,s,o)}}var Ts=["x","y","z"];function zi(i,e,t,r,n,s){L.setBuffer(i._roots[e]);let o=ao(0,i,t,r,n,s);return L.clearBuffer(),o}function ao(i,e,t,r,n,s){let{float32Array:o,uint16Array:c,uint32Array:l}=L,m=i*2;if(U(m,c)){let f=V(i,l),a=q(m,c);return Di(e,t,r,f,a,n,s)}else{let f=Xe(i,l),a=Ts[f],x=r.direction[a]>=0,b,d;x?(b=Q(i),d=$(i,l)):(b=$(i,l),d=Q(i));let v=ue(b,o,r,n,s)?ao(b,e,t,r,n,s):null;if(v){let T=v.point[a];if(x?T<=o[d+f]:T>=o[d+f+3])return v}let w=ue(d,o,r,n,s)?ao(d,e,t,r,n,s):null;return v&&w?v.distance<=w.distance?v:w:v||w||null}}var er=new K,et=new Z,tt=new Z,bt=new O,Ui=new j,tr=new j;function Hi(i,e,t,r){L.setBuffer(i._roots[e]);let n=co(0,i,t,r);return L.clearBuffer(),n}function co(i,e,t,r,n=null){let{float32Array:s,uint16Array:o,uint32Array:c}=L,l=i*2;if(n===null&&(t.boundingBox||t.computeBoundingBox(),Ui.set(t.boundingBox.min,t.boundingBox.max,r),n=Ui),U(l,o)){let p=e.geometry,f=p.index,a=p.attributes.position,h=t.index,x=t.attributes.position,b=V(i,c),d=q(l,o);if(bt.copy(r).invert(),t.boundsTree)return G(i,s,tr),tr.matrix.copy(bt),tr.needsUpdate=!0,t.boundsTree.shapecast({intersectsBounds:v=>tr.intersectsBox(v),intersectsTriangle:v=>{v.a.applyMatrix4(r),v.b.applyMatrix4(r),v.c.applyMatrix4(r),v.needsUpdate=!0;for(let y=b*3,w=(d+b)*3;y<w;y+=3)if(z(tt,y,f,a),tt.needsUpdate=!0,v.intersectsTriangle(tt))return!0;return!1}});for(let g=b*3,v=(d+b)*3;g<v;g+=3){z(et,g,f,a),et.a.applyMatrix4(bt),et.b.applyMatrix4(bt),et.c.applyMatrix4(bt),et.needsUpdate=!0;for(let y=0,w=h.count;y<w;y+=3)if(z(tt,y,h,x),tt.needsUpdate=!0,et.intersectsTriangle(tt))return!0}}else{let p=i+8,f=c[i+6];return G(p,s,er),!!(n.intersectsBox(er)&&co(p,e,t,r,n)||(G(f,s,er),n.intersectsBox(er)&&co(f,e,t,r,n)))}}var rr=new O,lo=new j,Tt=new j,ws=new I,Ss=new I,_s=new I,As=new I;function Wi(i,e,t,r={},n={},s=0,o=1/0){e.boundingBox||e.computeBoundingBox(),lo.set(e.boundingBox.min,e.boundingBox.max,t),lo.needsUpdate=!0;let c=i.geometry,l=c.attributes.position,m=c.index,p=e.attributes.position,f=e.index,a=re.getPrimitive(),h=re.getPrimitive(),x=ws,b=Ss,d=null,g=null;n&&(d=_s,g=As);let v=1/0,y=null,w=null;return rr.copy(t).invert(),Tt.matrix.copy(rr),i.shapecast({boundsTraverseOrder:T=>lo.distanceToBox(T),intersectsBounds:(T,S,_)=>_<v&&_<o?(S&&(Tt.min.copy(T.min),Tt.max.copy(T.max),Tt.needsUpdate=!0),!0):!1,intersectsRange:(T,S)=>{if(e.boundsTree)return e.boundsTree.shapecast({boundsTraverseOrder:A=>Tt.distanceToBox(A),intersectsBounds:(A,M,C)=>C<v&&C<o,intersectsRange:(A,M)=>{for(let C=A,F=A+M;C<F;C++){z(h,3*C,f,p),h.a.applyMatrix4(t),h.b.applyMatrix4(t),h.c.applyMatrix4(t),h.needsUpdate=!0;for(let R=T,B=T+S;R<B;R++){z(a,3*R,m,l),a.needsUpdate=!0;let P=a.distanceToTriangle(h,x,d);if(P<v&&(b.copy(x),g&&g.copy(d),v=P,y=R,w=C),P<s)return!0}}}});{let _=ye(e);for(let A=0,M=_;A<M;A++){z(h,3*A,f,p),h.a.applyMatrix4(t),h.b.applyMatrix4(t),h.c.applyMatrix4(t),h.needsUpdate=!0;for(let C=T,F=T+S;C<F;C++){z(a,3*C,m,l),a.needsUpdate=!0;let R=a.distanceToTriangle(h,x,d);if(R<v&&(b.copy(x),g&&g.copy(d),v=R,y=C,w=A),R<s)return!0}}}}}),re.releasePrimitive(a),re.releasePrimitive(h),v===1/0?null:(r.point?r.point.copy(b):r.point=b.clone(),r.distance=v,r.faceIndex=y,n&&(n.point?n.point.copy(g):n.point=g.clone(),n.point.applyMatrix4(rr),b.applyMatrix4(rr),n.distance=b.sub(n.point).length(),n.faceIndex=w),r)}function Vi(i,e=null){e&&Array.isArray(e)&&(e=new Set(e));let t=i.geometry,r=t.index?t.index.array:null,n=t.attributes.position,s,o,c,l,m=0,p=i._roots;for(let a=0,h=p.length;a<h;a++)s=p[a],o=new Uint32Array(s),c=new Uint16Array(s),l=new Float32Array(s),f(0,m),m+=s.byteLength;function f(a,h,x=!1){let b=a*2;if(c[b+15]===65535){let g=o[a+6],v=c[b+14],y=1/0,w=1/0,T=1/0,S=-1/0,_=-1/0,A=-1/0;for(let M=g,C=g+v;M<C;M++){let F=3*i.resolveTriangleIndex(M);for(let R=0;R<3;R++){let B=F+R;B=r?r[B]:B;let P=n.getX(B),D=n.getY(B),H=n.getZ(B);P<y&&(y=P),P>S&&(S=P),D<w&&(w=D),D>_&&(_=D),H<T&&(T=H),H>A&&(A=H)}}return l[a+0]!==y||l[a+1]!==w||l[a+2]!==T||l[a+3]!==S||l[a+4]!==_||l[a+5]!==A?(l[a+0]=y,l[a+1]=w,l[a+2]=T,l[a+3]=S,l[a+4]=_,l[a+5]=A,!0):!1}else{let g=a+8,v=o[a+6],y=g+h,w=v+h,T=x,S=!1,_=!1;e?T||(S=e.has(y),_=e.has(w),T=!S&&!_):(S=!0,_=!0);let A=T||S,M=T||_,C=!1;A&&(C=f(g,h,T));let F=!1;M&&(F=f(v,h,T));let R=C||F;if(R)for(let B=0;B<3;B++){let P=g+B,D=v+B,H=l[P],ge=l[P+3],be=l[D],Se=l[D+3];l[a+B]=H<be?H:be,l[a+B+3]=ge>Se?ge:Se}return R}}}function qi(i,e,t,r,n,s,o){L.setBuffer(i._roots[e]),uo(0,i,t,r,n,s,o),L.clearBuffer()}function uo(i,e,t,r,n,s,o){let{float32Array:c,uint16Array:l,uint32Array:m}=L,p=i*2;if(U(p,l)){let a=V(i,m),h=q(p,l);Ni(e,t,r,a,h,n,s,o)}else{let a=Q(i);ue(a,c,r,s,o)&&uo(a,e,t,r,n,s,o);let h=$(i,m);ue(h,c,r,s,o)&&uo(h,e,t,r,n,s,o)}}var Ms=["x","y","z"];function ji(i,e,t,r,n,s){L.setBuffer(i._roots[e]);let o=fo(0,i,t,r,n,s);return L.clearBuffer(),o}function fo(i,e,t,r,n,s){let{float32Array:o,uint16Array:c,uint32Array:l}=L,m=i*2;if(U(m,c)){let f=V(i,l),a=q(m,c);return ki(e,t,r,f,a,n,s)}else{let f=Xe(i,l),a=Ms[f],x=r.direction[a]>=0,b,d;x?(b=Q(i),d=$(i,l)):(b=$(i,l),d=Q(i));let v=ue(b,o,r,n,s)?fo(b,e,t,r,n,s):null;if(v){let T=v.point[a];if(x?T<=o[d+f]:T>=o[d+f+3])return v}let w=ue(d,o,r,n,s)?fo(d,e,t,r,n,s):null;return v&&w?v.distance<=w.distance?v:w:v||w||null}}var or=new K,rt=new Z,ot=new Z,wt=new O,Xi=new j,ir=new j;function Yi(i,e,t,r){L.setBuffer(i._roots[e]);let n=po(0,i,t,r);return L.clearBuffer(),n}function po(i,e,t,r,n=null){let{float32Array:s,uint16Array:o,uint32Array:c}=L,l=i*2;if(n===null&&(t.boundingBox||t.computeBoundingBox(),Xi.set(t.boundingBox.min,t.boundingBox.max,r),n=Xi),U(l,o)){let p=e.geometry,f=p.index,a=p.attributes.position,h=t.index,x=t.attributes.position,b=V(i,c),d=q(l,o);if(wt.copy(r).invert(),t.boundsTree)return G(i,s,ir),ir.matrix.copy(wt),ir.needsUpdate=!0,t.boundsTree.shapecast({intersectsBounds:v=>ir.intersectsBox(v),intersectsTriangle:v=>{v.a.applyMatrix4(r),v.b.applyMatrix4(r),v.c.applyMatrix4(r),v.needsUpdate=!0;for(let y=b,w=d+b;y<w;y++)if(z(ot,3*e.resolveTriangleIndex(y),f,a),ot.needsUpdate=!0,v.intersectsTriangle(ot))return!0;return!1}});for(let g=b,v=d+b;g<v;g++){let y=e.resolveTriangleIndex(g);z(rt,3*y,f,a),rt.a.applyMatrix4(wt),rt.b.applyMatrix4(wt),rt.c.applyMatrix4(wt),rt.needsUpdate=!0;for(let w=0,T=h.count;w<T;w+=3)if(z(ot,w,h,x),ot.needsUpdate=!0,rt.intersectsTriangle(ot))return!0}}else{let p=i+8,f=c[i+6];return G(p,s,or),!!(n.intersectsBox(or)&&po(p,e,t,r,n)||(G(f,s,or),n.intersectsBox(or)&&po(f,e,t,r,n)))}}var nr=new O,mo=new j,St=new j,Rs=new I,Cs=new I,Is=new I,Fs=new I;function $i(i,e,t,r={},n={},s=0,o=1/0){e.boundingBox||e.computeBoundingBox(),mo.set(e.boundingBox.min,e.boundingBox.max,t),mo.needsUpdate=!0;let c=i.geometry,l=c.attributes.position,m=c.index,p=e.attributes.position,f=e.index,a=re.getPrimitive(),h=re.getPrimitive(),x=Rs,b=Cs,d=null,g=null;n&&(d=Is,g=Fs);let v=1/0,y=null,w=null;return nr.copy(t).invert(),St.matrix.copy(nr),i.shapecast({boundsTraverseOrder:T=>mo.distanceToBox(T),intersectsBounds:(T,S,_)=>_<v&&_<o?(S&&(St.min.copy(T.min),St.max.copy(T.max),St.needsUpdate=!0),!0):!1,intersectsRange:(T,S)=>{if(e.boundsTree){let _=e.boundsTree;return _.shapecast({boundsTraverseOrder:A=>St.distanceToBox(A),intersectsBounds:(A,M,C)=>C<v&&C<o,intersectsRange:(A,M)=>{for(let C=A,F=A+M;C<F;C++){let R=_.resolveTriangleIndex(C);z(h,3*R,f,p),h.a.applyMatrix4(t),h.b.applyMatrix4(t),h.c.applyMatrix4(t),h.needsUpdate=!0;for(let B=T,P=T+S;B<P;B++){let D=i.resolveTriangleIndex(B);z(a,3*D,m,l),a.needsUpdate=!0;let H=a.distanceToTriangle(h,x,d);if(H<v&&(b.copy(x),g&&g.copy(d),v=H,y=B,w=C),H<s)return!0}}}})}else{let _=ye(e);for(let A=0,M=_;A<M;A++){z(h,3*A,f,p),h.a.applyMatrix4(t),h.b.applyMatrix4(t),h.c.applyMatrix4(t),h.needsUpdate=!0;for(let C=T,F=T+S;C<F;C++){let R=i.resolveTriangleIndex(C);z(a,3*R,m,l),a.needsUpdate=!0;let B=a.distanceToTriangle(h,x,d);if(B<v&&(b.copy(x),g&&g.copy(d),v=B,y=C,w=A),B<s)return!0}}}}}),re.releasePrimitive(a),re.releasePrimitive(h),v===1/0?null:(r.point?r.point.copy(b):r.point=b.clone(),r.distance=v,r.faceIndex=y,n&&(n.point?n.point.copy(g):n.point=g.clone(),n.point.applyMatrix4(nr),b.applyMatrix4(nr),n.distance=b.sub(n.point).length(),n.faceIndex=w),r)}function ho(){return typeof SharedArrayBuffer<"u"}var _t=new L.constructor,sr=new L.constructor,Ie=new Re(()=>new K),it=new K,nt=new K,xo=new K,go=new K,vo=!1;function Qi(i,e,t,r){if(vo)throw new Error("MeshBVH: Recursive calls to bvhcast not supported.");vo=!0;let n=i._roots,s=e._roots,o,c=0,l=0,m=new O().copy(t).invert();for(let p=0,f=n.length;p<f;p++){_t.setBuffer(n[p]),l=0;let a=Ie.getPrimitive();G(0,_t.float32Array,a),a.applyMatrix4(m);for(let h=0,x=s.length;h<x&&(sr.setBuffer(s[h]),o=he(0,0,t,m,r,c,l,0,0,a),sr.clearBuffer(),l+=s[h].length,!o);h++);if(Ie.releasePrimitive(a),_t.clearBuffer(),c+=n[p].length,o)break}return vo=!1,o}function he(i,e,t,r,n,s=0,o=0,c=0,l=0,m=null,p=!1){let f,a;p?(f=sr,a=_t):(f=_t,a=sr);let h=f.float32Array,x=f.uint32Array,b=f.uint16Array,d=a.float32Array,g=a.uint32Array,v=a.uint16Array,y=i*2,w=e*2,T=U(y,b),S=U(w,v),_=!1;if(S&&T)p?_=n(V(e,g),q(e*2,v),V(i,x),q(i*2,b),l,o+e,c,s+i):_=n(V(i,x),q(i*2,b),V(e,g),q(e*2,v),c,s+i,l,o+e);else if(S){let A=Ie.getPrimitive();G(e,d,A),A.applyMatrix4(t);let M=Q(i),C=$(i,x);G(M,h,it),G(C,h,nt);let F=A.intersectsBox(it),R=A.intersectsBox(nt);_=F&&he(e,M,r,t,n,o,s,l,c+1,A,!p)||R&&he(e,C,r,t,n,o,s,l,c+1,A,!p),Ie.releasePrimitive(A)}else{let A=Q(e),M=$(e,g);G(A,d,xo),G(M,d,go);let C=m.intersectsBox(xo),F=m.intersectsBox(go);if(C&&F)_=he(i,A,t,r,n,s,o,c,l+1,m,p)||he(i,M,t,r,n,s,o,c,l+1,m,p);else if(C)if(T)_=he(i,A,t,r,n,s,o,c,l+1,m,p);else{let R=Ie.getPrimitive();R.copy(xo).applyMatrix4(t);let B=Q(i),P=$(i,x);G(B,h,it),G(P,h,nt);let D=R.intersectsBox(it),H=R.intersectsBox(nt);_=D&&he(A,B,r,t,n,o,s,l,c+1,R,!p)||H&&he(A,P,r,t,n,o,s,l,c+1,R,!p),Ie.releasePrimitive(R)}else if(F)if(T)_=he(i,M,t,r,n,s,o,c,l+1,m,p);else{let R=Ie.getPrimitive();R.copy(go).applyMatrix4(t);let B=Q(i),P=$(i,x);G(B,h,it),G(P,h,nt);let D=R.intersectsBox(it),H=R.intersectsBox(nt);_=D&&he(M,B,r,t,n,o,s,l,c+1,R,!p)||H&&he(M,P,r,t,n,o,s,l,c+1,R,!p),Ie.releasePrimitive(R)}}return _}var ar=new j,Ki=new K,Bs={strategy:0,maxDepth:40,maxLeafTris:10,useSharedArrayBuffer:!1,setBoundingBox:!0,onProgress:null,indirect:!1,verbose:!0,range:null},Le=class i{static serialize(e,t={}){t={cloneBuffers:!0,...t};let r=e.geometry,n=e._roots,s=e._indirectBuffer,o=r.getIndex(),c;return t.cloneBuffers?c={roots:n.map(l=>l.slice()),index:o?o.array.slice():null,indirectBuffer:s?s.slice():null}:c={roots:n,index:o?o.array:null,indirectBuffer:s},c}static deserialize(e,t,r={}){r={setIndex:!0,indirect:!!e.indirectBuffer,...r};let{index:n,roots:s,indirectBuffer:o}=e,c=new i(t,{...r,[zt]:!0});if(c._roots=s,c._indirectBuffer=o||null,r.setIndex){let l=t.getIndex();if(l===null){let m=new X(e.index,1,!1);t.setIndex(m)}else l.array!==n&&(l.array.set(n),l.needsUpdate=!0)}return c}get indirect(){return!!this._indirectBuffer}constructor(e,t={}){if(e.isBufferGeometry){if(e.index&&e.index.isInterleavedBufferAttribute)throw new Error("MeshBVH: InterleavedBufferAttribute is not supported for the index attribute.")}else throw new Error("MeshBVH: Only BufferGeometries are supported.");if(t=Object.assign({...Bs,[zt]:!1},t),t.useSharedArrayBuffer&&!ho())throw new Error("MeshBVH: SharedArrayBuffer is not available.");this.geometry=e,this._roots=null,this._indirectBuffer=null,t[zt]||(Ai(this,t),!e.boundingBox&&t.setBoundingBox&&(e.boundingBox=this.getBoundingBox(new K))),this.resolveTriangleIndex=t.indirect?r=>this._indirectBuffer[r]:r=>r}refit(e=null){return(this.indirect?Vi:Li)(this,e)}traverse(e,t=0){let r=this._roots[t],n=new Uint32Array(r),s=new Uint16Array(r);o(0);function o(c,l=0){let m=c*2,p=s[m+15]===65535;if(p){let f=n[c+6],a=s[m+14];e(l,p,new Float32Array(r,c*4,6),f,a)}else{let f=c+32/4,a=n[c+6],h=n[c+7];e(l,p,new Float32Array(r,c*4,6),h)||(o(f,l+1),o(a,l+1))}}}raycast(e,t=dt,r=0,n=1/0){let s=this._roots,o=this.geometry,c=[],l=t.isMaterial,m=Array.isArray(t),p=o.groups,f=l?t.side:t,a=this.indirect?qi:Oi;for(let h=0,x=s.length;h<x;h++){let b=m?t[p[h].materialIndex].side:f,d=c.length;if(a(this,h,b,e,c,r,n),m){let g=p[h].materialIndex;for(let v=d,y=c.length;v<y;v++)c[v].face.materialIndex=g}}return c}raycastFirst(e,t=dt,r=0,n=1/0){let s=this._roots,o=this.geometry,c=t.isMaterial,l=Array.isArray(t),m=null,p=o.groups,f=c?t.side:t,a=this.indirect?ji:zi;for(let h=0,x=s.length;h<x;h++){let b=l?t[p[h].materialIndex].side:f,d=a(this,h,b,e,r,n);d!=null&&(m==null||d.distance<m.distance)&&(m=d,l&&(d.face.materialIndex=p[h].materialIndex))}return m}intersectsGeometry(e,t){let r=!1,n=this._roots,s=this.indirect?Yi:Hi;for(let o=0,c=n.length;o<c&&(r=s(this,o,e,t),!r);o++);return r}shapecast(e){let t=re.getPrimitive(),r=this.indirect?Gi:Ei,{boundsTraverseOrder:n,intersectsBounds:s,intersectsRange:o,intersectsTriangle:c}=e;if(o&&c){let f=o;o=(a,h,x,b,d)=>f(a,h,x,b,d)?!0:r(a,h,this,c,x,b,t)}else o||(c?o=(f,a,h,x)=>r(f,a,this,c,h,x,t):o=(f,a,h)=>h);let l=!1,m=0,p=this._roots;for(let f=0,a=p.length;f<a;f++){let h=p[f];if(l=Ri(this,f,s,o,n,m),l)break;m+=h.byteLength}return re.releasePrimitive(t),l}bvhcast(e,t,r){let{intersectsRanges:n,intersectsTriangles:s}=r,o=re.getPrimitive(),c=this.geometry.index,l=this.geometry.attributes.position,m=this.indirect?x=>{let b=this.resolveTriangleIndex(x);z(o,b*3,c,l)}:x=>{z(o,x*3,c,l)},p=re.getPrimitive(),f=e.geometry.index,a=e.geometry.attributes.position,h=e.indirect?x=>{let b=e.resolveTriangleIndex(x);z(p,b*3,f,a)}:x=>{z(p,x*3,f,a)};if(s){let x=(b,d,g,v,y,w,T,S)=>{for(let _=g,A=g+v;_<A;_++){h(_),p.a.applyMatrix4(t),p.b.applyMatrix4(t),p.c.applyMatrix4(t),p.needsUpdate=!0;for(let M=b,C=b+d;M<C;M++)if(m(M),o.needsUpdate=!0,s(o,p,M,_,y,w,T,S))return!0}return!1};if(n){let b=n;n=function(d,g,v,y,w,T,S,_){return b(d,g,v,y,w,T,S,_)?!0:x(d,g,v,y,w,T,S,_)}}else n=x}return Qi(this,e,t,n)}intersectsBox(e,t){return ar.set(e.min,e.max,t),ar.needsUpdate=!0,this.shapecast({intersectsBounds:r=>ar.intersectsBox(r),intersectsTriangle:r=>ar.intersectsTriangle(r)})}intersectsSphere(e){return this.shapecast({intersectsBounds:t=>e.intersectsBox(t),intersectsTriangle:t=>t.intersectsSphere(e)})}closestPointToGeometry(e,t,r={},n={},s=0,o=1/0){return(this.indirect?$i:Wi)(this,e,t,r,n,s,o)}closestPointToPoint(e,t={},r=0,n=1/0){return Ci(this,e,t,r,n)}getBoundingBox(e){return e.makeEmpty(),this._roots.forEach(r=>{G(0,new Float32Array(r),Ki),e.union(Ki)}),e}};var Zi=Yo||null,Zx=parseInt(ii)>=166;var Jx=Be.prototype.raycast,eg=Zi!==null?Zi.prototype.raycast:null;function Ps(i){switch(i){case 1:return"R";case 2:return"RG";case 3:return"RGBA";case 4:return"RGBA"}throw new Error}function Ds(i){switch(i){case 1:return Pe;case 2:return Gt;case 3:return E;case 4:return E}}function Ji(i){switch(i){case 1:return si;case 2:return Ot;case 3:return kt;case 4:return kt}}var cr=class extends W{constructor(){super(),this.minFilter=k,this.magFilter=k,this.generateMipmaps=!1,this.overrideItemSize=null,this._forcedType=null}updateFrom(e){let t=this.overrideItemSize,r=e.itemSize,n=e.count;if(t!==null){if(r*n%t!==0)throw new Error("VertexAttributeTexture: overrideItemSize must divide evenly into buffer length.");e.itemSize=t,e.count=n*r/t}let s=e.itemSize,o=e.count,c=e.normalized,l=e.array.constructor,m=l.BYTES_PER_ELEMENT,p=this._forcedType,f=s;if(p===null)switch(l){case Float32Array:p=N;break;case Uint8Array:case Uint16Array:case Uint32Array:p=De;break;case Int8Array:case Int16Array:case Int32Array:p=Dt;break}let a,h,x,b,d=Ps(s);switch(p){case N:x=1,h=Ds(s),c&&m===1?(b=l,d+="8",l===Uint8Array?a=ht:(a=zr,d+="_SNORM")):(b=Float32Array,d+="32F",a=N);break;case Dt:d+=m*8+"I",x=c?Math.pow(2,l.BYTES_PER_ELEMENT*8-1):1,h=Ji(s),m===1?(b=Int8Array,a=zr):m===2?(b=Int16Array,a=ci):(b=Int32Array,a=Dt);break;case De:d+=m*8+"UI",x=c?Math.pow(2,l.BYTES_PER_ELEMENT*8-1):1,h=Ji(s),m===1?(b=Uint8Array,a=ht):m===2?(b=Uint16Array,a=pi):(b=Uint32Array,a=De);break}f===3&&(h===E||h===kt)&&(f=4);let g=Math.ceil(Math.sqrt(o))||1,v=f*g*g,y=new b(v),w=e.normalized;e.normalized=!1;for(let T=0;T<o;T++){let S=f*T;y[S]=e.getX(T)/x,s>=2&&(y[S+1]=e.getY(T)/x),s>=3&&(y[S+2]=e.getZ(T)/x,f===4&&(y[S+3]=1)),s>=4&&(y[S+3]=e.getW(T)/x)}e.normalized=w,this.internalFormat=d,this.format=h,this.type=a,this.image.width=g,this.image.height=g,this.image.data=y,this.needsUpdate=!0,this.dispose(),e.itemSize=r,e.count=n}},st=class extends cr{constructor(){super(),this._forcedType=De}};var at=class extends cr{constructor(){super(),this._forcedType=N}};var lr=class{constructor(){this.index=new st,this.position=new at,this.bvhBounds=new W,this.bvhContents=new W,this._cachedIndexAttr=null,this.index.overrideItemSize=3}updateFrom(e){let{geometry:t}=e;if(Ls(e,this.bvhBounds,this.bvhContents),this.position.updateFrom(t.attributes.position),e.indirect){let r=e._indirectBuffer;if(this._cachedIndexAttr===null||this._cachedIndexAttr.count!==r.length)if(t.index)this._cachedIndexAttr=t.index.clone();else{let n=jr(qr(t));this._cachedIndexAttr=new X(n,1,!1)}Es(t,r,this._cachedIndexAttr),this.index.updateFrom(this._cachedIndexAttr)}else this.index.updateFrom(t.index)}dispose(){let{index:e,position:t,bvhBounds:r,bvhContents:n}=this;e&&e.dispose(),t&&t.dispose(),r&&r.dispose(),n&&n.dispose()}};function Es(i,e,t){let r=t.array,n=i.index?i.index.array:null;for(let s=0,o=e.length;s<o;s++){let c=3*s,l=3*e[s];for(let m=0;m<3;m++)r[c+m]=n?n[l+m]:l+m}}function Ls(i,e,t){let r=i._roots;if(r.length!==1)throw new Error("MeshBVHUniformStruct: Multi-root BVHs not supported.");let n=r[0],s=new Uint16Array(n),o=new Uint32Array(n),c=new Float32Array(n),l=n.byteLength/32,m=2*Math.ceil(Math.sqrt(l/2)),p=new Float32Array(4*m*m),f=Math.ceil(Math.sqrt(l)),a=new Uint32Array(2*f*f);for(let h=0;h<l;h++){let x=h*32/4,b=x*2,d=x;for(let g=0;g<3;g++)p[8*h+0+g]=c[d+0+g],p[8*h+4+g]=c[d+3+g];if(U(b,s)){let g=q(b,s),v=V(x,o),y=4294901760|g;a[h*2+0]=y,a[h*2+1]=v}else{let g=4*$(x,o)/32,v=Xe(x,o);a[h*2+0]=v,a[h*2+1]=g}}e.image.data=p,e.image.width=m,e.image.height=m,e.format=E,e.type=N,e.internalFormat="RGBA32F",e.minFilter=k,e.magFilter=k,e.generateMipmaps=!1,e.needsUpdate=!0,e.dispose(),t.image.data=a,t.image.width=f,t.image.height=f,t.format=Ot,t.type=De,t.internalFormat="RG32UI",t.minFilter=k,t.magFilter=k,t.generateMipmaps=!1,t.needsUpdate=!0,t.dispose()}var Ne={};jo(Ne,{bvh_distance_functions:()=>en,bvh_ray_functions:()=>bo,bvh_struct_definitions:()=>tn,common_functions:()=>yo});var yo=`

// A stack of uint32 indices can can store the indices for
// a perfectly balanced tree with a depth up to 31. Lower stack
// depth gets higher performance.
//
// However not all trees are balanced. Best value to set this to
// is the trees max depth.
#ifndef BVH_STACK_DEPTH
#define BVH_STACK_DEPTH 60
#endif

#ifndef INFINITY
#define INFINITY 1e20
#endif

// Utilities
uvec4 uTexelFetch1D( usampler2D tex, uint index ) {

	uint width = uint( textureSize( tex, 0 ).x );
	uvec2 uv;
	uv.x = index % width;
	uv.y = index / width;

	return texelFetch( tex, ivec2( uv ), 0 );

}

ivec4 iTexelFetch1D( isampler2D tex, uint index ) {

	uint width = uint( textureSize( tex, 0 ).x );
	uvec2 uv;
	uv.x = index % width;
	uv.y = index / width;

	return texelFetch( tex, ivec2( uv ), 0 );

}

vec4 texelFetch1D( sampler2D tex, uint index ) {

	uint width = uint( textureSize( tex, 0 ).x );
	uvec2 uv;
	uv.x = index % width;
	uv.y = index / width;

	return texelFetch( tex, ivec2( uv ), 0 );

}

vec4 textureSampleBarycoord( sampler2D tex, vec3 barycoord, uvec3 faceIndices ) {

	return
		barycoord.x * texelFetch1D( tex, faceIndices.x ) +
		barycoord.y * texelFetch1D( tex, faceIndices.y ) +
		barycoord.z * texelFetch1D( tex, faceIndices.z );

}

void ndcToCameraRay(
	vec2 coord, mat4 cameraWorld, mat4 invProjectionMatrix,
	out vec3 rayOrigin, out vec3 rayDirection
) {

	// get camera look direction and near plane for camera clipping
	vec4 lookDirection = cameraWorld * vec4( 0.0, 0.0, - 1.0, 0.0 );
	vec4 nearVector = invProjectionMatrix * vec4( 0.0, 0.0, - 1.0, 1.0 );
	float near = abs( nearVector.z / nearVector.w );

	// get the camera direction and position from camera matrices
	vec4 origin = cameraWorld * vec4( 0.0, 0.0, 0.0, 1.0 );
	vec4 direction = invProjectionMatrix * vec4( coord, 0.5, 1.0 );
	direction /= direction.w;
	direction = cameraWorld * direction - origin;

	// slide the origin along the ray until it sits at the near clip plane position
	origin.xyz += direction.xyz * near / dot( direction, lookDirection );

	rayOrigin = origin.xyz;
	rayDirection = direction.xyz;

}
`;var en=`

float dot2( vec3 v ) {

	return dot( v, v );

}

// https://www.shadertoy.com/view/ttfGWl
vec3 closestPointToTriangle( vec3 p, vec3 v0, vec3 v1, vec3 v2, out vec3 barycoord ) {

    vec3 v10 = v1 - v0;
    vec3 v21 = v2 - v1;
    vec3 v02 = v0 - v2;

	vec3 p0 = p - v0;
	vec3 p1 = p - v1;
	vec3 p2 = p - v2;

    vec3 nor = cross( v10, v02 );

    // method 2, in barycentric space
    vec3  q = cross( nor, p0 );
    float d = 1.0 / dot2( nor );
    float u = d * dot( q, v02 );
    float v = d * dot( q, v10 );
    float w = 1.0 - u - v;

	if( u < 0.0 ) {

		w = clamp( dot( p2, v02 ) / dot2( v02 ), 0.0, 1.0 );
		u = 0.0;
		v = 1.0 - w;

	} else if( v < 0.0 ) {

		u = clamp( dot( p0, v10 ) / dot2( v10 ), 0.0, 1.0 );
		v = 0.0;
		w = 1.0 - u;

	} else if( w < 0.0 ) {

		v = clamp( dot( p1, v21 ) / dot2( v21 ), 0.0, 1.0 );
		w = 0.0;
		u = 1.0-v;

	}

	barycoord = vec3( u, v, w );
    return u * v1 + v * v2 + w * v0;

}

float distanceToTriangles(
	// geometry info and triangle range
	sampler2D positionAttr, usampler2D indexAttr, uint offset, uint count,

	// point and cut off range
	vec3 point, float closestDistanceSquared,

	// outputs
	inout uvec4 faceIndices, inout vec3 faceNormal, inout vec3 barycoord, inout float side, inout vec3 outPoint
) {

	bool found = false;
	vec3 localBarycoord;
	for ( uint i = offset, l = offset + count; i < l; i ++ ) {

		uvec3 indices = uTexelFetch1D( indexAttr, i ).xyz;
		vec3 a = texelFetch1D( positionAttr, indices.x ).rgb;
		vec3 b = texelFetch1D( positionAttr, indices.y ).rgb;
		vec3 c = texelFetch1D( positionAttr, indices.z ).rgb;

		// get the closest point and barycoord
		vec3 closestPoint = closestPointToTriangle( point, a, b, c, localBarycoord );
		vec3 delta = point - closestPoint;
		float sqDist = dot2( delta );
		if ( sqDist < closestDistanceSquared ) {

			// set the output results
			closestDistanceSquared = sqDist;
			faceIndices = uvec4( indices.xyz, i );
			faceNormal = normalize( cross( a - b, b - c ) );
			barycoord = localBarycoord;
			outPoint = closestPoint;
			side = sign( dot( faceNormal, delta ) );

		}

	}

	return closestDistanceSquared;

}

float distanceSqToBounds( vec3 point, vec3 boundsMin, vec3 boundsMax ) {

	vec3 clampedPoint = clamp( point, boundsMin, boundsMax );
	vec3 delta = point - clampedPoint;
	return dot( delta, delta );

}

float distanceSqToBVHNodeBoundsPoint( vec3 point, sampler2D bvhBounds, uint currNodeIndex ) {

	uint cni2 = currNodeIndex * 2u;
	vec3 boundsMin = texelFetch1D( bvhBounds, cni2 ).xyz;
	vec3 boundsMax = texelFetch1D( bvhBounds, cni2 + 1u ).xyz;
	return distanceSqToBounds( point, boundsMin, boundsMax );

}

// use a macro to hide the fact that we need to expand the struct into separate fields
#define	bvhClosestPointToPoint(		bvh,		point, faceIndices, faceNormal, barycoord, side, outPoint	)	_bvhClosestPointToPoint(		bvh.position, bvh.index, bvh.bvhBounds, bvh.bvhContents,		point, faceIndices, faceNormal, barycoord, side, outPoint	)

float _bvhClosestPointToPoint(
	// bvh info
	sampler2D bvh_position, usampler2D bvh_index, sampler2D bvh_bvhBounds, usampler2D bvh_bvhContents,

	// point to check
	vec3 point,

	// output variables
	inout uvec4 faceIndices, inout vec3 faceNormal, inout vec3 barycoord,
	inout float side, inout vec3 outPoint
 ) {

	// stack needs to be twice as long as the deepest tree we expect because
	// we push both the left and right child onto the stack every traversal
	int ptr = 0;
	uint stack[ BVH_STACK_DEPTH ];
	stack[ 0 ] = 0u;

	float closestDistanceSquared = pow( 100000.0, 2.0 );
	bool found = false;
	while ( ptr > - 1 && ptr < BVH_STACK_DEPTH ) {

		uint currNodeIndex = stack[ ptr ];
		ptr --;

		// check if we intersect the current bounds
		float boundsHitDistance = distanceSqToBVHNodeBoundsPoint( point, bvh_bvhBounds, currNodeIndex );
		if ( boundsHitDistance > closestDistanceSquared ) {

			continue;

		}

		uvec2 boundsInfo = uTexelFetch1D( bvh_bvhContents, currNodeIndex ).xy;
		bool isLeaf = bool( boundsInfo.x & 0xffff0000u );
		if ( isLeaf ) {

			uint count = boundsInfo.x & 0x0000ffffu;
			uint offset = boundsInfo.y;
			closestDistanceSquared = distanceToTriangles(
				bvh_position, bvh_index, offset, count, point, closestDistanceSquared,

				// outputs
				faceIndices, faceNormal, barycoord, side, outPoint
			);

		} else {

			uint leftIndex = currNodeIndex + 1u;
			uint splitAxis = boundsInfo.x & 0x0000ffffu;
			uint rightIndex = boundsInfo.y;
			bool leftToRight = distanceSqToBVHNodeBoundsPoint( point, bvh_bvhBounds, leftIndex ) < distanceSqToBVHNodeBoundsPoint( point, bvh_bvhBounds, rightIndex );//rayDirection[ splitAxis ] >= 0.0;
			uint c1 = leftToRight ? leftIndex : rightIndex;
			uint c2 = leftToRight ? rightIndex : leftIndex;

			// set c2 in the stack so we traverse it later. We need to keep track of a pointer in
			// the stack while we traverse. The second pointer added is the one that will be
			// traversed first
			ptr ++;
			stack[ ptr ] = c2;
			ptr ++;
			stack[ ptr ] = c1;

		}

	}

	return sqrt( closestDistanceSquared );

}
`;var bo=`

#ifndef TRI_INTERSECT_EPSILON
#define TRI_INTERSECT_EPSILON 1e-5
#endif

// Raycasting
bool intersectsBounds( vec3 rayOrigin, vec3 rayDirection, vec3 boundsMin, vec3 boundsMax, out float dist ) {

	// https://www.reddit.com/r/opengl/comments/8ntzz5/fast_glsl_ray_box_intersection/
	// https://tavianator.com/2011/ray_box.html
	vec3 invDir = 1.0 / rayDirection;

	// find intersection distances for each plane
	vec3 tMinPlane = invDir * ( boundsMin - rayOrigin );
	vec3 tMaxPlane = invDir * ( boundsMax - rayOrigin );

	// get the min and max distances from each intersection
	vec3 tMinHit = min( tMaxPlane, tMinPlane );
	vec3 tMaxHit = max( tMaxPlane, tMinPlane );

	// get the furthest hit distance
	vec2 t = max( tMinHit.xx, tMinHit.yz );
	float t0 = max( t.x, t.y );

	// get the minimum hit distance
	t = min( tMaxHit.xx, tMaxHit.yz );
	float t1 = min( t.x, t.y );

	// set distance to 0.0 if the ray starts inside the box
	dist = max( t0, 0.0 );

	return t1 >= dist;

}

bool intersectsTriangle(
	vec3 rayOrigin, vec3 rayDirection, vec3 a, vec3 b, vec3 c,
	out vec3 barycoord, out vec3 norm, out float dist, out float side
) {

	// https://stackoverflow.com/questions/42740765/intersection-between-line-and-triangle-in-3d
	vec3 edge1 = b - a;
	vec3 edge2 = c - a;
	norm = cross( edge1, edge2 );

	float det = - dot( rayDirection, norm );
	float invdet = 1.0 / det;

	vec3 AO = rayOrigin - a;
	vec3 DAO = cross( AO, rayDirection );

	vec4 uvt;
	uvt.x = dot( edge2, DAO ) * invdet;
	uvt.y = - dot( edge1, DAO ) * invdet;
	uvt.z = dot( AO, norm ) * invdet;
	uvt.w = 1.0 - uvt.x - uvt.y;

	// set the hit information
	barycoord = uvt.wxy; // arranged in A, B, C order
	dist = uvt.z;
	side = sign( det );
	norm = side * normalize( norm );

	// add an epsilon to avoid misses between triangles
	uvt += vec4( TRI_INTERSECT_EPSILON );

	return all( greaterThanEqual( uvt, vec4( 0.0 ) ) );

}

bool intersectTriangles(
	// geometry info and triangle range
	sampler2D positionAttr, usampler2D indexAttr, uint offset, uint count,

	// ray
	vec3 rayOrigin, vec3 rayDirection,

	// outputs
	inout float minDistance, inout uvec4 faceIndices, inout vec3 faceNormal, inout vec3 barycoord,
	inout float side, inout float dist
) {

	bool found = false;
	vec3 localBarycoord, localNormal;
	float localDist, localSide;
	for ( uint i = offset, l = offset + count; i < l; i ++ ) {

		uvec3 indices = uTexelFetch1D( indexAttr, i ).xyz;
		vec3 a = texelFetch1D( positionAttr, indices.x ).rgb;
		vec3 b = texelFetch1D( positionAttr, indices.y ).rgb;
		vec3 c = texelFetch1D( positionAttr, indices.z ).rgb;

		if (
			intersectsTriangle( rayOrigin, rayDirection, a, b, c, localBarycoord, localNormal, localDist, localSide )
			&& localDist < minDistance
		) {

			found = true;
			minDistance = localDist;

			faceIndices = uvec4( indices.xyz, i );
			faceNormal = localNormal;

			side = localSide;
			barycoord = localBarycoord;
			dist = localDist;

		}

	}

	return found;

}

bool intersectsBVHNodeBounds( vec3 rayOrigin, vec3 rayDirection, sampler2D bvhBounds, uint currNodeIndex, out float dist ) {

	uint cni2 = currNodeIndex * 2u;
	vec3 boundsMin = texelFetch1D( bvhBounds, cni2 ).xyz;
	vec3 boundsMax = texelFetch1D( bvhBounds, cni2 + 1u ).xyz;
	return intersectsBounds( rayOrigin, rayDirection, boundsMin, boundsMax, dist );

}

// use a macro to hide the fact that we need to expand the struct into separate fields
#define	bvhIntersectFirstHit(		bvh,		rayOrigin, rayDirection, faceIndices, faceNormal, barycoord, side, dist	)	_bvhIntersectFirstHit(		bvh.position, bvh.index, bvh.bvhBounds, bvh.bvhContents,		rayOrigin, rayDirection, faceIndices, faceNormal, barycoord, side, dist	)

bool _bvhIntersectFirstHit(
	// bvh info
	sampler2D bvh_position, usampler2D bvh_index, sampler2D bvh_bvhBounds, usampler2D bvh_bvhContents,

	// ray
	vec3 rayOrigin, vec3 rayDirection,

	// output variables split into separate variables due to output precision
	inout uvec4 faceIndices, inout vec3 faceNormal, inout vec3 barycoord,
	inout float side, inout float dist
) {

	// stack needs to be twice as long as the deepest tree we expect because
	// we push both the left and right child onto the stack every traversal
	int ptr = 0;
	uint stack[ BVH_STACK_DEPTH ];
	stack[ 0 ] = 0u;

	float triangleDistance = INFINITY;
	bool found = false;
	while ( ptr > - 1 && ptr < BVH_STACK_DEPTH ) {

		uint currNodeIndex = stack[ ptr ];
		ptr --;

		// check if we intersect the current bounds
		float boundsHitDistance;
		if (
			! intersectsBVHNodeBounds( rayOrigin, rayDirection, bvh_bvhBounds, currNodeIndex, boundsHitDistance )
			|| boundsHitDistance > triangleDistance
		) {

			continue;

		}

		uvec2 boundsInfo = uTexelFetch1D( bvh_bvhContents, currNodeIndex ).xy;
		bool isLeaf = bool( boundsInfo.x & 0xffff0000u );

		if ( isLeaf ) {

			uint count = boundsInfo.x & 0x0000ffffu;
			uint offset = boundsInfo.y;

			found = intersectTriangles(
				bvh_position, bvh_index, offset, count,
				rayOrigin, rayDirection, triangleDistance,
				faceIndices, faceNormal, barycoord, side, dist
			) || found;

		} else {

			uint leftIndex = currNodeIndex + 1u;
			uint splitAxis = boundsInfo.x & 0x0000ffffu;
			uint rightIndex = boundsInfo.y;

			bool leftToRight = rayDirection[ splitAxis ] >= 0.0;
			uint c1 = leftToRight ? leftIndex : rightIndex;
			uint c2 = leftToRight ? rightIndex : leftIndex;

			// set c2 in the stack so we traverse it later. We need to keep track of a pointer in
			// the stack while we traverse. The second pointer added is the one that will be
			// traversed first
			ptr ++;
			stack[ ptr ] = c2;

			ptr ++;
			stack[ ptr ] = c1;

		}

	}

	return found;

}
`;var tn=`
struct BVH {

	usampler2D index;
	sampler2D position;

	sampler2D bvhBounds;
	usampler2D bvhContents;

};
`;var Cg=`
	${yo}
	${bo}
`;function ur(i,e,t=0){if(i.isInterleavedBufferAttribute){let r=i.itemSize;for(let n=0,s=i.count;n<s;n++){let o=n+t;e.setX(o,i.getX(n)),r>=2&&e.setY(o,i.getY(n)),r>=3&&e.setZ(o,i.getZ(n)),r>=4&&e.setW(o,i.getW(n))}}else{let r=e.array,n=r.constructor,s=r.BYTES_PER_ELEMENT*i.itemSize*t;new n(r.buffer,s,i.array.length).set(i.array)}}function ke(i,e=null){let t=i.array.constructor,r=i.normalized,n=i.itemSize,s=e===null?i.count:e;return new X(new t(n*s),n,r)}function Fe(i,e){if(!i&&!e)return!0;if(!!i!=!!e)return!1;let t=i.count===e.count,r=i.normalized===e.normalized,n=i.array.constructor===e.array.constructor,s=i.itemSize===e.itemSize;return!(!t||!r||!n||!s)}function Ns(i){let e=i[0].index!==null,t=new Set(Object.keys(i[0].attributes));if(!i[0].getAttribute("position"))throw new Error("StaticGeometryGenerator: position attribute is required.");for(let r=0;r<i.length;++r){let n=i[r],s=0;if(e!==(n.index!==null))throw new Error("StaticGeometryGenerator: All geometries must have compatible attributes; make sure index attribute exists among all geometries, or in none of them.");for(let o in n.attributes){if(!t.has(o))throw new Error('StaticGeometryGenerator: All geometries must have compatible attributes; make sure "'+o+'" attribute exists among all geometries, or in none of them.');s++}if(s!==t.size)throw new Error("StaticGeometryGenerator: All geometries must have the same number of attributes.")}}function ks(i){let e=0;for(let t=0,r=i.length;t<r;t++)e+=i[t].getIndex().count;return e}function Gs(i){let e=0;for(let t=0,r=i.length;t<r;t++)e+=i[t].getAttribute("position").count;return e}function Os(i,e,t){i.index&&i.index.count!==e&&i.setIndex(null);let r=i.attributes;for(let n in r)r[n].count!==t&&i.deleteAttribute(n)}function rn(i,e={},t=new J){let{useGroups:r=!1,forceUpdate:n=!1,skipAssigningAttributes:s=[],overwriteIndex:o=!0}=e;Ns(i);let c=i[0].index!==null,l=c?ks(i):-1,m=Gs(i);if(Os(t,l,m),r){let f=0;for(let a=0,h=i.length;a<h;a++){let x=i[a],b;c?b=x.getIndex().count:b=x.getAttribute("position").count,t.addGroup(f,b,a),f+=b}}if(c){let f=!1;if(t.index||(t.setIndex(new X(new Uint32Array(l),1,!1)),f=!0),f||o){let a=0,h=0,x=t.getIndex();for(let b=0,d=i.length;b<d;b++){let g=i[b],v=g.getIndex();if(!(!n&&!f&&s[b]))for(let w=0;w<v.count;++w)x.setX(a+w,v.getX(w)+h);a+=v.count,h+=g.getAttribute("position").count}}}let p=Object.keys(i[0].attributes);for(let f=0,a=p.length;f<a;f++){let h=!1,x=p[f];if(!t.getAttribute(x)){let g=i[0].getAttribute(x);t.setAttribute(x,ke(g,m)),h=!0}let b=0,d=t.getAttribute(x);for(let g=0,v=i.length;g<v;g++){let y=i[g],w=!n&&!h&&s[g],T=y.getAttribute(x);w||ur(T,d,b),b+=T.count}}}function on(i,e,t){let r=i.index,s=i.attributes.position.count,o=r?r.count:s,c=i.groups;c.length===0&&(c=[{count:o,start:0,materialIndex:0}]);let l=i.getAttribute("materialIndex");if(!l||l.count!==s){let p;t.length<=255?p=new Uint8Array(s):p=new Uint16Array(s),l=new X(p,1,!1),i.deleteAttribute("materialIndex"),i.setAttribute("materialIndex",l)}let m=l.array;for(let p=0;p<c.length;p++){let f=c[p],a=f.start,h=f.count,x=Math.min(h,o-a),b=Array.isArray(e)?e[f.materialIndex]:e,d=t.indexOf(b);for(let g=0;g<x;g++){let v=a+g;r&&(v=r.getX(v)),m[v]=d}}}function nn(i,e){if(!i.index){let t=i.attributes.position.count,r=new Array(t);for(let n=0;n<t;n++)r[n]=n;i.setIndex(r)}if(!i.attributes.normal&&e&&e.includes("normal")&&i.computeVertexNormals(),!i.attributes.uv&&e&&e.includes("uv")){let t=i.attributes.position.count;i.setAttribute("uv",new X(new Float32Array(t*2),2,!1))}if(!i.attributes.uv2&&e&&e.includes("uv2")){let t=i.attributes.position.count;i.setAttribute("uv2",new X(new Float32Array(t*2),2,!1))}if(!i.attributes.tangent&&e&&e.includes("tangent"))if(i.attributes.uv&&i.attributes.normal)i.computeTangents();else{let t=i.attributes.position.count;i.setAttribute("tangent",new X(new Float32Array(t*4),4,!1))}if(!i.attributes.color&&e&&e.includes("color")){let t=i.attributes.position.count,r=new Float32Array(t*4);r.fill(1),i.setAttribute("color",new X(r,4))}}function ct(i){let e=0;if(i.byteLength!==0){let t=new Uint8Array(i);for(let r=0;r<i.byteLength;r++){let n=t[r];e=(e<<5)-e+n,e|=0}}return e}function sn(i){let e=i.uuid,t=Object.values(i.attributes);i.index&&(t.push(i.index),e+=`index|${i.index.version}`);let r=Object.keys(t).sort();for(let n of r){let s=t[n];e+=`${n}_${s.version}|`}return e}function an(i){let e=i.skeleton;return e?(e.boneTexture||e.computeBoneTexture(),`${ct(e.boneTexture.image.data.buffer)}_${e.boneTexture.uuid}`):null}var fr=class{constructor(e=null){this.matrixWorld=new O,this.geometryHash=null,this.skeletonHash=null,this.primitiveCount=-1,e!==null&&this.updateFrom(e)}updateFrom(e){let t=e.geometry,r=(t.index?t.index.count:t.attributes.position.count)/3;this.matrixWorld.copy(e.matrixWorld),this.geometryHash=sn(t),this.primitiveCount=r,this.skeletonHash=an(e)}didChange(e){let t=e.geometry,r=(t.index?t.index.count:t.attributes.position.count)/3;return!(this.matrixWorld.equals(e.matrixWorld)&&this.geometryHash===sn(t)&&this.skeletonHash===an(e)&&this.primitiveCount===r)}};var Ge=new I,Oe=new I,ze=new I,cn=new Te,pr=new I,To=new I,ln=new Te,un=new Te,mr=new O,fn=new O;function pn(i,e,t){let r=i.skeleton,n=i.geometry,s=r.bones,o=r.boneInverses;ln.fromBufferAttribute(n.attributes.skinIndex,e),un.fromBufferAttribute(n.attributes.skinWeight,e),mr.elements.fill(0);for(let c=0;c<4;c++){let l=un.getComponent(c);if(l!==0){let m=ln.getComponent(c);fn.multiplyMatrices(s[m].matrixWorld,o[m]),zs(mr,fn,l)}}return mr.multiply(i.bindMatrix).premultiply(i.bindMatrixInverse),t.transformDirection(mr),t}function wo(i,e,t,r,n){pr.set(0,0,0);for(let s=0,o=i.length;s<o;s++){let c=e[s],l=i[s];c!==0&&(To.fromBufferAttribute(l,r),t?pr.addScaledVector(To,c):pr.addScaledVector(To.sub(n),c))}n.add(pr)}function zs(i,e,t){let r=i.elements,n=e.elements;for(let s=0,o=n.length;s<o;s++)r[s]+=n[s]*t}function Us(i){let{index:e,attributes:t}=i;if(e)for(let r=0,n=e.count;r<n;r+=3){let s=e.getX(r),o=e.getX(r+2);e.setX(r,o),e.setX(r+2,s)}else for(let r in t){let n=t[r],s=n.itemSize;for(let o=0,c=n.count;o<c;o+=3)for(let l=0;l<s;l++){let m=n.getComponent(o,l),p=n.getComponent(o+2,l);n.setComponent(o,l,p),n.setComponent(o+2,l,m)}}return i}function mn(i,e={},t=new J){e={applyWorldTransforms:!0,attributes:[],...e};let r=i.geometry,n=e.applyWorldTransforms,s=e.attributes.includes("normal"),o=e.attributes.includes("tangent"),c=r.attributes,l=t.attributes;for(let v in t.attributes)(!e.attributes.includes(v)||!(v in r.attributes))&&t.deleteAttribute(v);!t.index&&r.index&&(t.index=r.index.clone()),l.position||t.setAttribute("position",ke(c.position)),s&&!l.normal&&c.normal&&t.setAttribute("normal",ke(c.normal)),o&&!l.tangent&&c.tangent&&t.setAttribute("tangent",ke(c.tangent)),Fe(r.index,t.index),Fe(c.position,l.position),s&&Fe(c.normal,l.normal),o&&Fe(c.tangent,l.tangent);let m=c.position,p=s?c.normal:null,f=o?c.tangent:null,a=r.morphAttributes.position,h=r.morphAttributes.normal,x=r.morphAttributes.tangent,b=r.morphTargetsRelative,d=i.morphTargetInfluences,g=new Hr;g.getNormalMatrix(i.matrixWorld),r.index&&t.index.array.set(r.index.array);for(let v=0,y=c.position.count;v<y;v++)Ge.fromBufferAttribute(m,v),p&&Oe.fromBufferAttribute(p,v),f&&(cn.fromBufferAttribute(f,v),ze.fromBufferAttribute(f,v)),d&&(a&&wo(a,d,b,v,Ge),h&&wo(h,d,b,v,Oe),x&&wo(x,d,b,v,ze)),i.isSkinnedMesh&&(i.applyBoneTransform(v,Ge),p&&pn(i,v,Oe),f&&pn(i,v,ze)),n&&Ge.applyMatrix4(i.matrixWorld),l.position.setXYZ(v,Ge.x,Ge.y,Ge.z),p&&(n&&Oe.applyNormalMatrix(g),l.normal.setXYZ(v,Oe.x,Oe.y,Oe.z)),f&&(n&&ze.transformDirection(i.matrixWorld),l.tangent.setXYZW(v,ze.x,ze.y,ze.z,cn.w));for(let v in e.attributes){let y=e.attributes[v];y==="position"||y==="tangent"||y==="normal"||!(y in c)||(l[y]||t.setAttribute(y,ke(c[y])),Fe(c[y],l[y]),ur(c[y],l[y]))}return i.matrixWorld.determinant()<0&&Us(t),t}var dr=class extends J{constructor(){super(),this.version=0,this.hash=null,this._diff=new fr}isCompatible(e,t){let r=e.geometry;for(let n=0;n<t.length;n++){let s=t[n],o=r.attributes[s],c=this.attributes[s];if(o&&!Fe(o,c))return!1}return!0}updateFrom(e,t){let r=this._diff;return r.didChange(e)?(mn(e,t,this),r.updateFrom(e),this.version++,this.hash=`${this.uuid}_${this.version}`,!0):!1}};var xr=0,So=1,_o=2;function Hs(i,e){for(let t=0,r=i.length;t<r;t++)i[t].traverseVisible(s=>{s.isMesh&&e(s)})}function Ws(i){let e=[];for(let t=0,r=i.length;t<r;t++){let n=i[t];Array.isArray(n.material)?e.push(...n.material):e.push(n.material)}return e}function Vs(i,e,t){if(i.length===0){e.setIndex(null);let r=e.attributes;for(let n in r)e.deleteAttribute(n);for(let n in t.attributes)e.setAttribute(t.attributes[n],new X(new Float32Array(0),4,!1))}else rn(i,t,e);for(let r in e.attributes)e.attributes[r].needsUpdate=!0}var hr=class{constructor(e){this.objects=null,this.useGroups=!0,this.applyWorldTransforms=!0,this.generateMissingAttributes=!0,this.overwriteIndex=!0,this.attributes=["position","normal","color","tangent","uv","uv2"],this._intermediateGeometry=new Map,this._geometryMergeSets=new WeakMap,this._mergeOrder=[],this._dummyMesh=null,this.setObjects(e||[])}_getDummyMesh(){if(!this._dummyMesh){let e=new Wr,t=new J;t.setAttribute("position",new X(new Float32Array(9),3)),this._dummyMesh=new Be(t,e)}return this._dummyMesh}_getMeshes(){let e=[];return Hs(this.objects,t=>{e.push(t)}),e.sort((t,r)=>t.uuid>r.uuid?1:t.uuid<r.uuid?-1:0),e.length===0&&e.push(this._getDummyMesh()),e}_updateIntermediateGeometries(){let{_intermediateGeometry:e}=this,t=this._getMeshes(),r=new Set(e.keys()),n={attributes:this.attributes,applyWorldTransforms:this.applyWorldTransforms};for(let s=0,o=t.length;s<o;s++){let c=t[s],l=c.uuid;r.delete(l);let m=e.get(l);(!m||!m.isCompatible(c,this.attributes))&&(m&&m.dispose(),m=new dr,e.set(l,m)),m.updateFrom(c,n)&&this.generateMissingAttributes&&nn(m,this.attributes)}r.forEach(s=>{e.delete(s)})}setObjects(e){Array.isArray(e)?this.objects=[...e]:this.objects=[e]}generate(e=new J){let{useGroups:t,overwriteIndex:r,_intermediateGeometry:n,_geometryMergeSets:s}=this,o=this._getMeshes(),c=[],l=[],m=s.get(e)||[];this._updateIntermediateGeometries();let p=!1;o.length!==m.length&&(p=!0);for(let a=0,h=o.length;a<h;a++){let x=o[a],b=n.get(x.uuid);l.push(b);let d=m[a];!d||d.uuid!==b.uuid?(c.push(!1),p=!0):d.version!==b.version?c.push(!1):c.push(!0)}Vs(l,e,{useGroups:t,forceUpdate:p,skipAssigningAttributes:c,overwriteIndex:r}),p&&e.dispose(),s.set(e,l.map(a=>({version:a.version,uuid:a.uuid})));let f=xr;return p?f=_o:c.includes(!1)&&(f=So),{changeType:f,materials:Ws(o),geometry:e}}};function qs(i){let e=new Set;for(let t=0,r=i.length;t<r;t++){let n=i[t];for(let s in n){let o=n[s];o&&o.isTexture&&e.add(o)}}return Array.from(e)}function js(i){let e=[],t=new Set;for(let n=0,s=i.length;n<s;n++)i[n].traverse(o=>{o.visible&&(o.isRectAreaLight||o.isSpotLight||o.isPointLight||o.isDirectionalLight)&&(e.push(o),o.iesMap&&t.add(o.iesMap))});let r=Array.from(t).sort((n,s)=>n.uuid<s.uuid?1:n.uuid>s.uuid?-1:0);return{lights:e,iesTextures:r}}var Ue=class{get initialized(){return!!this.bvh}constructor(e){this.bvhOptions={},this.attributes=["position","normal","tangent","color","uv","uv2"],this.generateBVH=!0,this.bvh=null,this.geometry=new J,this.staticGeometryGenerator=new hr(e),this._bvhWorker=null,this._pendingGenerate=null,this._buildAsync=!1}setObjects(e){this.staticGeometryGenerator.setObjects(e)}setBVHWorker(e){this._bvhWorker=e}async generateAsync(e=null){if(!this._bvhWorker)throw new Error('PathTracingSceneGenerator: "setBVHWorker" must be called before "generateAsync" can be called.');if(this.bvh instanceof Promise)return this._pendingGenerate||(this._pendingGenerate=new Promise(async()=>(await this.bvh,this._pendingGenerate=null,this.generateAsync(e)))),this._pendingGenerate;{this._buildAsync=!0;let t=this.generate(e);return this._buildAsync=!1,t.bvh=this.bvh=await t.bvh,t}}generate(e=null){let{staticGeometryGenerator:t,geometry:r,attributes:n}=this,s=t.objects;t.attributes=n,s.forEach(f=>{f.traverse(a=>{a.isSkinnedMesh&&a.skeleton&&a.skeleton.update()})});let o=t.generate(r),c=o.materials,l=qs(c),{lights:m,iesTextures:p}=js(s);if(o.changeType!==xr&&on(r,c,c),this.generateBVH){if(this.bvh instanceof Promise)throw new Error("PathTracingSceneGenerator: BVH is already building asynchronously.");if(o.changeType===_o){let f={strategy:2,maxLeafTris:1,indirect:!0,onProgress:e,...this.bvhOptions};this._buildAsync?this.bvh=this._bvhWorker.generate(r,f):this.bvh=new Le(r,f)}else o.changeType===So&&this.bvh.refit()}return{bvhChanged:o.changeType!==xr,bvh:this.bvh,lights:m,iesTextures:p,geometry:r,materials:c,textures:l,objects:s}}},Ao=class extends Ue{constructor(...e){super(...e),console.warn('DynamicPathTracingSceneGenerator has been deprecated and renamed to "PathTracingSceneGenerator".')}},Mo=class extends Ue{constructor(...e){super(...e),console.warn('PathTracingSceneWorker has been deprecated and renamed to "PathTracingSceneGenerator".')}};var Xs=new ti(-1,1,1,-1,0,1),Ro=class extends J{constructor(){super(),this.setAttribute("position",new Ur([-1,3,0,-1,-1,0,3,-1,0],3)),this.setAttribute("uv",new Ur([0,2,0,0,2,0],2))}},Ys=new Ro,oe=class{constructor(e){this._mesh=new Be(Ys,e)}dispose(){this._mesh.geometry.dispose()}render(e){e.render(this._mesh,Xs)}get material(){return this._mesh.material}set material(e){this._mesh.material=e}};var fe=class extends _e{set needsUpdate(e){super.needsUpdate=!0,this.dispatchEvent({type:"recompilation"})}constructor(e){super(e);for(let t in this.uniforms)Object.defineProperty(this,t,{get(){return this.uniforms[t].value},set(r){this.uniforms[t].value=r}})}setDefine(e,t=void 0){if(t==null){if(e in this.defines)return delete this.defines[e],this.needsUpdate=!0,!0}else if(this.defines[e]!==t)return this.defines[e]=t,this.needsUpdate=!0,!0;return!1}};var gr=class extends fe{constructor(e){super({blending:me,uniforms:{target1:{value:null},target2:{value:null},opacity:{value:1}},vertexShader:`

				varying vec2 vUv;

				void main() {

					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

				}`,fragmentShader:`

				uniform float opacity;

				uniform sampler2D target1;
				uniform sampler2D target2;

				varying vec2 vUv;

				void main() {

					vec4 color1 = texture2D( target1, vUv );
					vec4 color2 = texture2D( target2, vUv );

					float invOpacity = 1.0 - opacity;
					float totalAlpha = color1.a * invOpacity + color2.a * opacity;

					if ( color1.a != 0.0 || color2.a != 0.0 ) {

						gl_FragColor.rgb = color1.rgb * ( invOpacity * color1.a / totalAlpha ) + color2.rgb * ( opacity * color2.a / totalAlpha );
						gl_FragColor.a = totalAlpha;

					} else {

						gl_FragColor = vec4( 0.0 );

					}

				}`}),this.setValues(e)}};function vr(i=1){let e="uint";return i>1&&(e="uvec"+i),`
		${e} sobolReverseBits( ${e} x ) {

			x = ( ( ( x & 0xaaaaaaaau ) >> 1 ) | ( ( x & 0x55555555u ) << 1 ) );
			x = ( ( ( x & 0xccccccccu ) >> 2 ) | ( ( x & 0x33333333u ) << 2 ) );
			x = ( ( ( x & 0xf0f0f0f0u ) >> 4 ) | ( ( x & 0x0f0f0f0fu ) << 4 ) );
			x = ( ( ( x & 0xff00ff00u ) >> 8 ) | ( ( x & 0x00ff00ffu ) << 8 ) );
			return ( ( x >> 16 ) | ( x << 16 ) );

		}

		${e} sobolHashCombine( uint seed, ${e} v ) {

			return seed ^ ( v + ${e}( ( seed << 6 ) + ( seed >> 2 ) ) );

		}

		${e} sobolLaineKarrasPermutation( ${e} x, ${e} seed ) {

			x += seed;
			x ^= x * 0x6c50b47cu;
			x ^= x * 0xb82f1e52u;
			x ^= x * 0xc7afe638u;
			x ^= x * 0x8d22f6e6u;
			return x;

		}

		${e} nestedUniformScrambleBase2( ${e} x, ${e} seed ) {

			x = sobolLaineKarrasPermutation( x, seed );
			x = sobolReverseBits( x );
			return x;

		}
	`}function yr(i=1){let e="uint",t="float",r="",n=".r",s="1u";return i>1&&(e="uvec"+i,t="vec"+i,r=i+"",i===2?(n=".rg",s="uvec2( 1u, 2u )"):i===3?(n=".rgb",s="uvec3( 1u, 2u, 3u )"):(n="",s="uvec4( 1u, 2u, 3u, 4u )")),`

		${t} sobol${r}( int effect ) {

			uint seed = sobolGetSeed( sobolBounceIndex, uint( effect ) );
			uint index = sobolPathIndex;

			uint shuffle_seed = sobolHashCombine( seed, 0u );
			uint shuffled_index = nestedUniformScrambleBase2( sobolReverseBits( index ), shuffle_seed );
			${t} sobol_pt = sobolGetTexturePoint( shuffled_index )${n};
			${e} result = ${e}( sobol_pt * 16777216.0 );

			${e} seed2 = sobolHashCombine( seed, ${s} );
			result = nestedUniformScrambleBase2( result, seed2 );

			return SOBOL_FACTOR * ${t}( result >> 8 );

		}
	`}var br=`

	// Utils
	const float SOBOL_FACTOR = 1.0 / 16777216.0;
	const uint SOBOL_MAX_POINTS = 256u * 256u;

	${vr(1)}
	${vr(2)}
	${vr(3)}
	${vr(4)}

	uint sobolHash( uint x ) {

		// finalizer from murmurhash3
		x ^= x >> 16;
		x *= 0x85ebca6bu;
		x ^= x >> 13;
		x *= 0xc2b2ae35u;
		x ^= x >> 16;
		return x;

	}

`,dn=`

	const uint SOBOL_DIRECTIONS_1[ 32 ] = uint[ 32 ](
		0x80000000u, 0xc0000000u, 0xa0000000u, 0xf0000000u,
		0x88000000u, 0xcc000000u, 0xaa000000u, 0xff000000u,
		0x80800000u, 0xc0c00000u, 0xa0a00000u, 0xf0f00000u,
		0x88880000u, 0xcccc0000u, 0xaaaa0000u, 0xffff0000u,
		0x80008000u, 0xc000c000u, 0xa000a000u, 0xf000f000u,
		0x88008800u, 0xcc00cc00u, 0xaa00aa00u, 0xff00ff00u,
		0x80808080u, 0xc0c0c0c0u, 0xa0a0a0a0u, 0xf0f0f0f0u,
		0x88888888u, 0xccccccccu, 0xaaaaaaaau, 0xffffffffu
	);

	const uint SOBOL_DIRECTIONS_2[ 32 ] = uint[ 32 ](
		0x80000000u, 0xc0000000u, 0x60000000u, 0x90000000u,
		0xe8000000u, 0x5c000000u, 0x8e000000u, 0xc5000000u,
		0x68800000u, 0x9cc00000u, 0xee600000u, 0x55900000u,
		0x80680000u, 0xc09c0000u, 0x60ee0000u, 0x90550000u,
		0xe8808000u, 0x5cc0c000u, 0x8e606000u, 0xc5909000u,
		0x6868e800u, 0x9c9c5c00u, 0xeeee8e00u, 0x5555c500u,
		0x8000e880u, 0xc0005cc0u, 0x60008e60u, 0x9000c590u,
		0xe8006868u, 0x5c009c9cu, 0x8e00eeeeu, 0xc5005555u
	);

	const uint SOBOL_DIRECTIONS_3[ 32 ] = uint[ 32 ](
		0x80000000u, 0xc0000000u, 0x20000000u, 0x50000000u,
		0xf8000000u, 0x74000000u, 0xa2000000u, 0x93000000u,
		0xd8800000u, 0x25400000u, 0x59e00000u, 0xe6d00000u,
		0x78080000u, 0xb40c0000u, 0x82020000u, 0xc3050000u,
		0x208f8000u, 0x51474000u, 0xfbea2000u, 0x75d93000u,
		0xa0858800u, 0x914e5400u, 0xdbe79e00u, 0x25db6d00u,
		0x58800080u, 0xe54000c0u, 0x79e00020u, 0xb6d00050u,
		0x800800f8u, 0xc00c0074u, 0x200200a2u, 0x50050093u
	);

	const uint SOBOL_DIRECTIONS_4[ 32 ] = uint[ 32 ](
		0x80000000u, 0x40000000u, 0x20000000u, 0xb0000000u,
		0xf8000000u, 0xdc000000u, 0x7a000000u, 0x9d000000u,
		0x5a800000u, 0x2fc00000u, 0xa1600000u, 0xf0b00000u,
		0xda880000u, 0x6fc40000u, 0x81620000u, 0x40bb0000u,
		0x22878000u, 0xb3c9c000u, 0xfb65a000u, 0xddb2d000u,
		0x78022800u, 0x9c0b3c00u, 0x5a0fb600u, 0x2d0ddb00u,
		0xa2878080u, 0xf3c9c040u, 0xdb65a020u, 0x6db2d0b0u,
		0x800228f8u, 0x400b3cdcu, 0x200fb67au, 0xb00ddb9du
	);

	uint getMaskedSobol( uint index, uint directions[ 32 ] ) {

		uint X = 0u;
		for ( int bit = 0; bit < 32; bit ++ ) {

			uint mask = ( index >> bit ) & 1u;
			X ^= mask * directions[ bit ];

		}
		return X;

	}

	vec4 generateSobolPoint( uint index ) {

		if ( index >= SOBOL_MAX_POINTS ) {

			return vec4( 0.0 );

		}

		// NOTE: this sobol "direction" is also available but we can't write out 5 components
		// uint x = index & 0x00ffffffu;
		uint x = sobolReverseBits( getMaskedSobol( index, SOBOL_DIRECTIONS_1 ) ) & 0x00ffffffu;
		uint y = sobolReverseBits( getMaskedSobol( index, SOBOL_DIRECTIONS_2 ) ) & 0x00ffffffu;
		uint z = sobolReverseBits( getMaskedSobol( index, SOBOL_DIRECTIONS_3 ) ) & 0x00ffffffu;
		uint w = sobolReverseBits( getMaskedSobol( index, SOBOL_DIRECTIONS_4 ) ) & 0x00ffffffu;

		return vec4( x, y, z, w ) * SOBOL_FACTOR;

	}

`,hn=`

	// Seeds
	uniform sampler2D sobolTexture;
	uint sobolPixelIndex = 0u;
	uint sobolPathIndex = 0u;
	uint sobolBounceIndex = 0u;

	uint sobolGetSeed( uint bounce, uint effect ) {

		return sobolHash(
			sobolHashCombine(
				sobolHashCombine(
					sobolHash( bounce ),
					sobolPixelIndex
				),
				effect
			)
		);

	}

	vec4 sobolGetTexturePoint( uint index ) {

		if ( index >= SOBOL_MAX_POINTS ) {

			index = index % SOBOL_MAX_POINTS;

		}

		uvec2 dim = uvec2( textureSize( sobolTexture, 0 ).xy );
		uint y = index / dim.x;
		uint x = index - y * dim.x;
		vec2 uv = vec2( x, y ) / vec2( dim );
		return texture( sobolTexture, uv );

	}

	${yr(1)}
	${yr(2)}
	${yr(3)}
	${yr(4)}

`;var Co=class extends fe{constructor(){super({blending:me,uniforms:{resolution:{value:new Y}},vertexShader:`

				varying vec2 vUv;
				void main() {

					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

				}
			`,fragmentShader:`

				${br}
				${dn}

				varying vec2 vUv;
				uniform vec2 resolution;
				void main() {

					uint index = uint( gl_FragCoord.y ) * uint( resolution.x ) + uint( gl_FragCoord.x );
					gl_FragColor = generateSobolPoint( index );

				}
			`})}},Tr=class{generate(e,t=256){let r=new ve(t,t,{type:N,format:E,minFilter:k,magFilter:k,generateMipmaps:!1}),n=e.getRenderTarget();e.setRenderTarget(r);let s=new oe(new Co);return s.material.resolution.set(t,t),s.render(e),e.setRenderTarget(n),s.dispose(),r}};var At=class extends Lt{set bokehSize(e){this.fStop=this.getFocalLength()/e}get bokehSize(){return this.getFocalLength()/this.fStop}constructor(...e){super(...e),this.fStop=1.4,this.apertureBlades=0,this.apertureRotation=0,this.focusDistance=25,this.anamorphicRatio=1}copy(e,t){return super.copy(e,t),this.fStop=e.fStop,this.apertureBlades=e.apertureBlades,this.apertureRotation=e.apertureRotation,this.focusDistance=e.focusDistance,this.anamorphicRatio=e.anamorphicRatio,this}};var wr=class{constructor(){this.bokehSize=0,this.apertureBlades=0,this.apertureRotation=0,this.focusDistance=10,this.anamorphicRatio=1}updateFrom(e){e instanceof At?(this.bokehSize=e.bokehSize,this.apertureBlades=e.apertureBlades,this.apertureRotation=e.apertureRotation,this.focusDistance=e.focusDistance,this.anamorphicRatio=e.anamorphicRatio):(this.bokehSize=0,this.apertureRotation=0,this.apertureBlades=0,this.focusDistance=10,this.anamorphicRatio=1)}};function Sr(i){let e=new Uint16Array(i.length);for(let t=0,r=i.length;t<r;++t)e[t]=ie.toHalfFloat(i[t]);return e}function xn(i,e,t=0,r=i.length){let n=t,s=t+r-1;for(;n<s;){let o=n+s>>1;i[o]<e?n=o+1:s=o}return n-t}function $s(i,e,t){return .2126*i+.7152*e+.0722*t}function Qs(i,e=ee){let t=i.clone();t.source=new li({...t.image});let{width:r,height:n,data:s}=t.image,o=s;if(t.type!==e){e===ee?o=new Uint16Array(s.length):o=new Float32Array(s.length);let c;s instanceof Int8Array||s instanceof Int16Array||s instanceof Int32Array?c=2**(8*s.BYTES_PER_ELEMENT-1)-1:c=2**(8*s.BYTES_PER_ELEMENT)-1;for(let l=0,m=s.length;l<m;l++){let p=s[l];t.type===ee&&(p=ie.fromHalfFloat(s[l])),t.type!==N&&t.type!==ee&&(p/=c),e===ee&&(o[l]=ie.toHalfFloat(p))}t.image.data=o,t.type=e}if(t.flipY){let c=o;o=o.slice();for(let l=0;l<n;l++)for(let m=0;m<r;m++){let p=n-l-1,f=4*(l*r+m),a=4*(p*r+m);o[a+0]=c[f+0],o[a+1]=c[f+1],o[a+2]=c[f+2],o[a+3]=c[f+3]}t.flipY=!1,t.image.data=o}return t}var _r=class{constructor(){let e=new W(Sr(new Float32Array([0,0,0,0])),1,1);e.type=ee,e.format=E,e.minFilter=te,e.magFilter=te,e.wrapS=le,e.wrapT=le,e.generateMipmaps=!1,e.needsUpdate=!0;let t=new W(Sr(new Float32Array([0,1])),1,2);t.type=ee,t.format=Pe,t.minFilter=te,t.magFilter=te,t.generateMipmaps=!1,t.needsUpdate=!0;let r=new W(Sr(new Float32Array([0,0,1,1])),2,2);r.type=ee,r.format=Pe,r.minFilter=te,r.magFilter=te,r.generateMipmaps=!1,r.needsUpdate=!0,this.map=e,this.marginalWeights=t,this.conditionalWeights=r,this.totalSum=0}dispose(){this.marginalWeights.dispose(),this.conditionalWeights.dispose(),this.map.dispose()}updateFrom(e){let t=Qs(e);t.wrapS=le,t.wrapT=ae;let{width:r,height:n,data:s}=t.image,o=new Float32Array(r*n),c=new Float32Array(r*n),l=new Float32Array(n),m=new Float32Array(n),p=0,f=0;for(let d=0;d<n;d++){let g=0;for(let v=0;v<r;v++){let y=d*r+v,w=ie.fromHalfFloat(s[4*y+0]),T=ie.fromHalfFloat(s[4*y+1]),S=ie.fromHalfFloat(s[4*y+2]),_=$s(w,T,S);g+=_,p+=_,o[y]=_,c[y]=g}if(g!==0)for(let v=d*r,y=d*r+r;v<y;v++)o[v]/=g,c[v]/=g;f+=g,l[d]=g,m[d]=f}if(f!==0)for(let d=0,g=l.length;d<g;d++)l[d]/=f,m[d]/=f;let a=new Uint16Array(n),h=new Uint16Array(r*n);for(let d=0;d<n;d++){let g=(d+1)/n,v=xn(m,g);a[d]=ie.toHalfFloat((v+.5)/n)}for(let d=0;d<n;d++)for(let g=0;g<r;g++){let v=d*r+g,y=(g+1)/r,w=xn(c,y,d*r,r);h[v]=ie.toHalfFloat((w+.5)/r)}this.dispose();let{marginalWeights:x,conditionalWeights:b}=this;x.image={width:n,height:1,data:a},x.needsUpdate=!0,b.image={width:r,height:n,data:h},b.needsUpdate=!0,this.totalSum=p,this.map=t}};var Io=6,Ks=0,Zs=1,Js=2,ea=3,ta=4,xe=new I,se=new I,gn=new O,lt=new oi,vn=new I,ut=new I,ra=new I(0,1,0),Ar=class{constructor(){let e=new W(new Float32Array(4),1,1);e.format=E,e.type=N,e.wrapS=ae,e.wrapT=ae,e.generateMipmaps=!1,e.minFilter=k,e.magFilter=k,this.tex=e,this.count=0}updateFrom(e,t=[]){let r=this.tex,n=Math.max(e.length*Io,1),s=Math.ceil(Math.sqrt(n));r.image.width!==s&&(r.dispose(),r.image.data=new Float32Array(s*s*4),r.image.width=s,r.image.height=s);let o=r.image.data;for(let l=0,m=e.length;l<m;l++){let p=e[l],f=l*Io*4,a=0;for(let x=0;x<Io*4;x++)o[f+x]=0;p.getWorldPosition(se),o[f+a++]=se.x,o[f+a++]=se.y,o[f+a++]=se.z;let h=Ks;if(p.isRectAreaLight&&p.isCircular?h=Zs:p.isSpotLight?h=Js:p.isDirectionalLight?h=ea:p.isPointLight&&(h=ta),o[f+a++]=h,o[f+a++]=p.color.r,o[f+a++]=p.color.g,o[f+a++]=p.color.b,o[f+a++]=p.intensity,p.getWorldQuaternion(lt),p.isRectAreaLight)xe.set(p.width,0,0).applyQuaternion(lt),o[f+a++]=xe.x,o[f+a++]=xe.y,o[f+a++]=xe.z,a++,se.set(0,p.height,0).applyQuaternion(lt),o[f+a++]=se.x,o[f+a++]=se.y,o[f+a++]=se.z,o[f+a++]=xe.cross(se).length()*(p.isCircular?Math.PI/4:1);else if(p.isSpotLight){let x=p.radius||0;vn.setFromMatrixPosition(p.matrixWorld),ut.setFromMatrixPosition(p.target.matrixWorld),gn.lookAt(vn,ut,ra),lt.setFromRotationMatrix(gn),xe.set(1,0,0).applyQuaternion(lt),o[f+a++]=xe.x,o[f+a++]=xe.y,o[f+a++]=xe.z,a++,se.set(0,1,0).applyQuaternion(lt),o[f+a++]=se.x,o[f+a++]=se.y,o[f+a++]=se.z,o[f+a++]=Math.PI*x*x,o[f+a++]=x,o[f+a++]=p.decay,o[f+a++]=p.distance,o[f+a++]=Math.cos(p.angle),o[f+a++]=Math.cos(p.angle*(1-p.penumbra)),o[f+a++]=p.iesMap?t.indexOf(p.iesMap):-1}else if(p.isPointLight){let x=xe.setFromMatrixPosition(p.matrixWorld);o[f+a++]=x.x,o[f+a++]=x.y,o[f+a++]=x.z,a++,a+=4,a+=1,o[f+a++]=p.decay,o[f+a++]=p.distance}else if(p.isDirectionalLight){let x=xe.setFromMatrixPosition(p.matrixWorld),b=se.setFromMatrixPosition(p.target.matrixWorld);ut.subVectors(x,b).normalize(),o[f+a++]=ut.x,o[f+a++]=ut.y,o[f+a++]=ut.z}}this.count=e.length;let c=ct(o.buffer);return this.hash!==c?(this.hash=c,r.needsUpdate=!0,!0):!1}};function yn(i,e,t,r,n){if(e>r)throw new Error;let s=i.length/e,o=i.constructor.BYTES_PER_ELEMENT*8,c=1;switch(i.constructor){case Uint8Array:case Uint16Array:case Uint32Array:c=2**o-1;break;case Int8Array:case Int16Array:case Int32Array:c=2**(o-1)-1;break}for(let l=0;l<s;l++){let m=4*l,p=e*l;for(let f=0;f<r;f++)t[n+m+f]=e>=f+1?i[p+f]/c:0}}var Mr=class extends Ko{constructor(){super(),this._textures=[],this.type=N,this.format=E,this.internalFormat="RGBA32F"}updateAttribute(e,t){let r=this._textures[e];r.updateFrom(t);let n=r.image,s=this.image;if(n.width!==s.width||n.height!==s.height)throw new Error("FloatAttributeTextureArray: Attribute must be the same dimensions when updating single layer.");let{width:o,height:c,data:l}=s,p=o*c*4*e,f=t.itemSize;f===3&&(f=4),yn(r.image.data,f,l,4,p),this.dispose(),this.needsUpdate=!0}setAttributes(e){let t=e[0].count,r=e.length;for(let f=0,a=r;f<a;f++)if(e[f].count!==t)throw new Error("FloatAttributeTextureArray: All attributes must have the same item count.");let n=this._textures;for(;n.length<r;){let f=new at;n.push(f)}for(;n.length>r;)n.pop();for(let f=0,a=r;f<a;f++)n[f].updateFrom(e[f]);let o=n[0].image,c=this.image;(o.width!==c.width||o.height!==c.height||o.depth!==r)&&(c.width=o.width,c.height=o.height,c.depth=r,c.data=new Float32Array(c.width*c.height*c.depth*4));let{data:l,width:m,height:p}=c;for(let f=0,a=r;f<a;f++){let h=n[f],b=m*p*4*f,d=e[f].itemSize;d===3&&(d=4),yn(h.image.data,d,l,4,b)}this.dispose(),this.needsUpdate=!0}};var Rr=class extends Mr{updateNormalAttribute(e){this.updateAttribute(0,e)}updateTangentAttribute(e){this.updateAttribute(1,e)}updateUvAttribute(e){this.updateAttribute(2,e)}updateColorAttribute(e){this.updateAttribute(3,e)}updateFrom(e,t,r,n){this.setAttributes([e,t,r,n])}};function Fo(i,e){return i.uuid<e.uuid?1:i.uuid>e.uuid?-1:0}function Cr(i){return`${i.source.uuid}:${i.colorSpace}`}function oa(i){let e=new Set,t=[];for(let r=0,n=i.length;r<n;r++){let s=i[r],o=Cr(s);e.has(o)||(e.add(o),t.push(s))}return t}function bn(i){let e=i.map(r=>r.iesMap||null).filter(r=>r),t=new Set(e);return Array.from(t).sort(Fo)}function Tn(i){let e=new Set;for(let r=0,n=i.length;r<n;r++){let s=i[r];for(let o in s){let c=s[o];c&&c.isTexture&&e.add(c)}}let t=Array.from(e);return oa(t).sort(Fo)}function wn(i){let e=[];return i.traverse(t=>{t.visible&&(t.isRectAreaLight||t.isSpotLight||t.isPointLight||t.isDirectionalLight)&&e.push(t)}),e.sort(Fo)}var _n=45,Sn=_n*4,Bo=class{constructor(){this._features={}}isUsed(e){return e in this._features}setUsed(e,t=!0){t===!1?delete this._features[e]:this._features[e]=!0}reset(){this._features={}}},Ir=class extends W{constructor(){super(new Float32Array(4),1,1),this.format=E,this.type=N,this.wrapS=ae,this.wrapT=ae,this.minFilter=k,this.magFilter=k,this.generateMipmaps=!1,this.features=new Bo}updateFrom(e,t){function r(x,b,d=-1){if(b in x&&x[b]){let g=Cr(x[b]);return f[g]}else return d}function n(x,b,d){return b in x?x[b]:d}function s(x,b,d,g){let v=x[b]&&x[b].isTexture?x[b]:null;if(v){v.matrixAutoUpdate&&v.updateMatrix();let y=v.matrix.elements,w=0;d[g+w++]=y[0],d[g+w++]=y[3],d[g+w++]=y[6],w++,d[g+w++]=y[1],d[g+w++]=y[4],d[g+w++]=y[7],w++}return 8}let o=0,c=e.length*_n,l=Math.ceil(Math.sqrt(c))||1,{image:m,features:p}=this,f={};for(let x=0,b=t.length;x<b;x++)f[Cr(t[x])]=x;m.width!==l&&(this.dispose(),m.data=new Float32Array(l*l*4),m.width=l,m.height=l);let a=m.data;p.reset();for(let x=0,b=e.length;x<b;x++){let d=e[x];if(d.isFogVolumeMaterial){p.setUsed("FOG");for(let y=0;y<Sn;y++)a[o+y]=0;a[o+0*4+0]=d.color.r,a[o+0*4+1]=d.color.g,a[o+0*4+2]=d.color.b,a[o+2*4+3]=n(d,"emissiveIntensity",0),a[o+3*4+0]=d.emissive.r,a[o+3*4+1]=d.emissive.g,a[o+3*4+2]=d.emissive.b,a[o+13*4+1]=d.density,a[o+13*4+3]=0,a[o+14*4+2]=4,o+=Sn;continue}a[o++]=d.color.r,a[o++]=d.color.g,a[o++]=d.color.b,a[o++]=r(d,"map"),a[o++]=n(d,"metalness",0),a[o++]=r(d,"metalnessMap"),a[o++]=n(d,"roughness",0),a[o++]=r(d,"roughnessMap"),a[o++]=n(d,"ior",1.5),a[o++]=n(d,"transmission",0),a[o++]=r(d,"transmissionMap"),a[o++]=n(d,"emissiveIntensity",0),"emissive"in d?(a[o++]=d.emissive.r,a[o++]=d.emissive.g,a[o++]=d.emissive.b):(a[o++]=0,a[o++]=0,a[o++]=0),a[o++]=r(d,"emissiveMap"),a[o++]=r(d,"normalMap"),"normalScale"in d?(a[o++]=d.normalScale.x,a[o++]=d.normalScale.y):(a[o++]=1,a[o++]=1),a[o++]=n(d,"clearcoat",0),a[o++]=r(d,"clearcoatMap"),a[o++]=n(d,"clearcoatRoughness",0),a[o++]=r(d,"clearcoatRoughnessMap"),a[o++]=r(d,"clearcoatNormalMap"),"clearcoatNormalScale"in d?(a[o++]=d.clearcoatNormalScale.x,a[o++]=d.clearcoatNormalScale.y):(a[o++]=1,a[o++]=1),o++,a[o++]=n(d,"sheen",0),"sheenColor"in d?(a[o++]=d.sheenColor.r,a[o++]=d.sheenColor.g,a[o++]=d.sheenColor.b):(a[o++]=0,a[o++]=0,a[o++]=0),a[o++]=r(d,"sheenColorMap"),a[o++]=n(d,"sheenRoughness",0),a[o++]=r(d,"sheenRoughnessMap"),a[o++]=r(d,"iridescenceMap"),a[o++]=r(d,"iridescenceThicknessMap"),a[o++]=n(d,"iridescence",0),a[o++]=n(d,"iridescenceIOR",1.3);let g=n(d,"iridescenceThicknessRange",[100,400]);a[o++]=g[0],a[o++]=g[1],"specularColor"in d?(a[o++]=d.specularColor.r,a[o++]=d.specularColor.g,a[o++]=d.specularColor.b):(a[o++]=1,a[o++]=1,a[o++]=1),a[o++]=r(d,"specularColorMap"),a[o++]=n(d,"specularIntensity",1),a[o++]=r(d,"specularIntensityMap");let v=n(d,"thickness",0)===0&&n(d,"attenuationDistance",1/0)===1/0;if(a[o++]=Number(v),o++,"attenuationColor"in d?(a[o++]=d.attenuationColor.r,a[o++]=d.attenuationColor.g,a[o++]=d.attenuationColor.b):(a[o++]=1,a[o++]=1,a[o++]=1),a[o++]=n(d,"attenuationDistance",1/0),a[o++]=r(d,"alphaMap"),a[o++]=d.opacity,a[o++]=d.alphaTest,!v&&d.transmission>0)a[o++]=0;else switch(d.side){case dt:a[o++]=1;break;case Bt:a[o++]=-1;break;case Pt:a[o++]=0;break}a[o++]=Number(n(d,"matte",!1)),a[o++]=Number(n(d,"castShadow",!0)),a[o++]=Number(d.vertexColors)|Number(d.flatShading)<<1,a[o++]=Number(d.transparent),o+=s(d,"map",a,o),o+=s(d,"metalnessMap",a,o),o+=s(d,"roughnessMap",a,o),o+=s(d,"transmissionMap",a,o),o+=s(d,"emissiveMap",a,o),o+=s(d,"normalMap",a,o),o+=s(d,"clearcoatMap",a,o),o+=s(d,"clearcoatNormalMap",a,o),o+=s(d,"clearcoatRoughnessMap",a,o),o+=s(d,"sheenColorMap",a,o),o+=s(d,"sheenRoughnessMap",a,o),o+=s(d,"iridescenceMap",a,o),o+=s(d,"iridescenceThicknessMap",a,o),o+=s(d,"specularColorMap",a,o),o+=s(d,"specularIntensityMap",a,o)}let h=ct(a.buffer);return this.hash!==h?(this.hash=h,this.needsUpdate=!0,!0):!1}};var An=new pe;function ia(i){return i?`${i.uuid}:${i.version}`:null}function na(i,e){for(let t in e)t in i&&(i[t]=e[t])}var Mt=class extends mi{constructor(e,t,r){let n={format:E,type:ht,minFilter:te,magFilter:te,wrapS:le,wrapT:le,generateMipmaps:!1,...r};super(e,t,1,n),na(this.texture,n),this.texture.setTextures=(...o)=>{this.setTextures(...o)},this.hashes=[null];let s=new oe(new Po);this.fsQuad=s}setTextures(e,t,r=this.width,n=this.height){let s=e.getRenderTarget(),o=e.toneMapping,c=e.getClearAlpha();e.getClearColor(An);let l=t.length||1;(r!==this.width||n!==this.height||this.depth!==l)&&(this.setSize(r,n,l),this.hashes=new Array(l).fill(null)),e.setClearColor(0,0),e.toneMapping=ei;let m=this.fsQuad,p=this.hashes,f=!1;for(let a=0,h=l;a<h;a++){let x=t[a],b=ia(x);x&&(p[a]!==b||x.isWebGLRenderTarget)&&(x.matrixAutoUpdate=!1,x.matrix.identity(),m.material.map=x,e.setRenderTarget(this,a),m.render(e),x.updateMatrix(),x.matrixAutoUpdate=!0,p[a]=b,f=!0)}return m.material.map=null,e.setClearColor(An,c),e.setRenderTarget(s),e.toneMapping=o,f}dispose(){super.dispose(),this.fsQuad.dispose()}},Po=class extends _e{get map(){return this.uniforms.map.value}set map(e){this.uniforms.map.value=e}constructor(){super({uniforms:{map:{value:null}},vertexShader:`
				varying vec2 vUv;
				void main() {

					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

				}
			`,fragmentShader:`
				uniform sampler2D map;
				varying vec2 vUv;
				void main() {

					gl_FragColor = texture2D( map, vUv );

				}
			`})}};function sa(i,e=Math.random()){for(let t=i.length-1;t>0;t--){let r=Math.floor(e()*(t+1)),n=i[t];i[t]=i[r],i[r]=n}return i}var Fr=class{constructor(e,t,r=Math.random){let n=e**t,s=new Uint16Array(n),o=n;for(let c=0;c<n;c++)s[c]=c;this.samples=new Float32Array(t),this.strataCount=e,this.reset=function(){for(let c=0;c<n;c++)s[c]=c;o=0},this.reshuffle=function(){o=0},this.next=function(){let{samples:c}=this;o>=s.length&&(sa(s,r),this.reshuffle());let l=s[o++];for(let m=0;m<t;m++)c[m]=(l%e+r())/e,l=Math.floor(l/e);return c}}};var Br=class{constructor(e,t,r=Math.random){let n=0;for(let l of t)n+=l;let s=new Float32Array(n),o=[],c=0;for(let l of t){let m=new Fr(e,l,r);m.samples=new Float32Array(s.buffer,c,m.samples.length),c+=m.samples.length*4,o.push(m)}this.samples=s,this.strataCount=e,this.next=function(){for(let l of o)l.next();return s},this.reshuffle=function(){for(let l of o)l.reshuffle()},this.reset=function(){for(let l of o)l.reset()}}};var Do=class{constructor(e=0){this.m=2147483648,this.a=1103515245,this.c=12345,this.seed=e}nextInt(){return this.seed=(this.a*this.seed+this.c)%this.m,this.seed}nextFloat(){return this.nextInt()/(this.m-1)}},Pr=class extends W{constructor(e=1,t=1,r=8){super(new Float32Array(1),1,1,E,N),this.minFilter=k,this.magFilter=k,this.strata=r,this.sampler=null,this.generator=new Do,this.stableNoise=!1,this.random=()=>this.stableNoise?this.generator.nextFloat():Math.random(),this.init(e,t,r)}init(e=this.image.height,t=this.image.width,r=this.strata){let{image:n}=this;if(n.width===t&&n.height===e&&this.sampler!==null)return;let s=new Array(e*t).fill(4),o=new Br(r,s,this.random);n.width=t,n.height=e,n.data=o.samples,this.sampler=o,this.dispose(),this.next()}next(){this.sampler.next(),this.needsUpdate=!0}reset(){this.sampler.reset(),this.generator.seed=0}};function Mn(i,e=Math.random){for(let t=i.length-1;t>0;t--){let r=~~((e()-1e-6)*t),n=i[t];i[t]=i[r],i[r]=n}}function Rn(i,e){i.fill(0);for(let t=0;t<e;t++)i[t]=1}var Rt=class{constructor(e){this.count=0,this.size=-1,this.sigma=-1,this.radius=-1,this.lookupTable=null,this.score=null,this.binaryPattern=null,this.resize(e),this.setSigma(1.5)}findVoid(){let{score:e,binaryPattern:t}=this,r=1/0,n=-1;for(let s=0,o=t.length;s<o;s++){if(t[s]!==0)continue;let c=e[s];c<r&&(r=c,n=s)}return n}findCluster(){let{score:e,binaryPattern:t}=this,r=-1/0,n=-1;for(let s=0,o=t.length;s<o;s++){if(t[s]!==1)continue;let c=e[s];c>r&&(r=c,n=s)}return n}setSigma(e){if(e===this.sigma)return;let t=~~(Math.sqrt(10*2*e**2)+1),r=2*t+1,n=new Float32Array(r*r),s=e*e;for(let o=-t;o<=t;o++)for(let c=-t;c<=t;c++){let l=(t+c)*r+o+t,m=o*o+c*c;n[l]=Math.E**(-m/(2*s))}this.lookupTable=n,this.sigma=e,this.radius=t}resize(e){this.size!==e&&(this.size=e,this.score=new Float32Array(e*e),this.binaryPattern=new Uint8Array(e*e))}invert(){let{binaryPattern:e,score:t,size:r}=this;t.fill(0);for(let n=0,s=e.length;n<s;n++)if(e[n]===0){let o=~~(n/r),c=n-o*r;this.updateScore(c,o,1),e[n]=1}else e[n]=0}updateScore(e,t,r){let{size:n,score:s,lookupTable:o}=this,c=this.radius,l=2*c+1;for(let m=-c;m<=c;m++)for(let p=-c;p<=c;p++){let f=(c+p)*l+m+c,a=o[f],h=e+m;h=h<0?n+h:h%n;let x=t+p;x=x<0?n+x:x%n;let b=x*n+h;s[b]+=r*a}}addPointIndex(e){this.binaryPattern[e]=1;let t=this.size,r=~~(e/t),n=e-r*t;this.updateScore(n,r,1),this.count++}removePointIndex(e){this.binaryPattern[e]=0;let t=this.size,r=~~(e/t),n=e-r*t;this.updateScore(n,r,-1),this.count--}copy(e){this.resize(e.size),this.score.set(e.score),this.binaryPattern.set(e.binaryPattern),this.setSigma(e.sigma),this.count=e.count}};var Dr=class{constructor(){this.random=Math.random,this.sigma=1.5,this.size=64,this.majorityPointsRatio=.1,this.samples=new Rt(1),this.savedSamples=new Rt(1)}generate(){let{samples:e,savedSamples:t,sigma:r,majorityPointsRatio:n,size:s}=this;e.resize(s),e.setSigma(r);let o=Math.floor(s*s*n),c=e.binaryPattern;Rn(c,o),Mn(c,this.random);for(let f=0,a=c.length;f<a;f++)c[f]===1&&e.addPointIndex(f);for(;;){let f=e.findCluster();e.removePointIndex(f);let a=e.findVoid();if(f===a){e.addPointIndex(f);break}e.addPointIndex(a)}let l=new Uint32Array(s*s);t.copy(e);let m;for(m=e.count-1;m>=0;){let f=e.findCluster();e.removePointIndex(f),l[f]=m,m--}let p=s*s;for(m=t.count;m<p/2;){let f=t.findVoid();t.addPointIndex(f),l[f]=m,m++}for(t.invert();m<p;){let f=t.findCluster();t.removePointIndex(f),l[f]=m,m++}return{data:l,maxValue:p}}};function aa(i){return i>=3?4:i}function ca(i){switch(i){case 1:return Pe;case 2:return Gt;default:return E}}var Er=class extends W{constructor(e=64,t=1){super(new Float32Array(4),1,1,E,N),this.minFilter=k,this.magFilter=k,this.size=e,this.channels=t,this.update()}update(){let e=this.channels,t=this.size,r=new Dr;r.channels=e,r.size=t;let n=aa(e),s=ca(n);(this.image.width!==t||s!==this.format)&&(this.image.width=t,this.image.height=t,this.image.data=new Float32Array(t**2*n),this.format=s,this.dispose());let o=this.image.data;for(let c=0,l=e;c<l;c++){let m=r.generate(),p=m.data,f=m.maxValue;for(let a=0,h=p.length;a<h;a++){let x=p[a]/f;o[a*n+c]=x}}this.needsUpdate=!0}};var Cn=`

	struct PhysicalCamera {

		float focusDistance;
		float anamorphicRatio;
		float bokehSize;
		int apertureBlades;
		float apertureRotation;

	};

`;var In=`

	struct EquirectHdrInfo {

		sampler2D marginalWeights;
		sampler2D conditionalWeights;
		sampler2D map;

		float totalSum;

	};

`;var Fn=`

	#define RECT_AREA_LIGHT_TYPE 0
	#define CIRC_AREA_LIGHT_TYPE 1
	#define SPOT_LIGHT_TYPE 2
	#define DIR_LIGHT_TYPE 3
	#define POINT_LIGHT_TYPE 4

	struct LightsInfo {

		sampler2D tex;
		uint count;

	};

	struct Light {

		vec3 position;
		int type;

		vec3 color;
		float intensity;

		vec3 u;
		vec3 v;
		float area;

		// spot light fields
		float radius;
		float near;
		float decay;
		float distance;
		float coneCos;
		float penumbraCos;
		int iesProfile;

	};

	Light readLightInfo( sampler2D tex, uint index ) {

		uint i = index * 6u;

		vec4 s0 = texelFetch1D( tex, i + 0u );
		vec4 s1 = texelFetch1D( tex, i + 1u );
		vec4 s2 = texelFetch1D( tex, i + 2u );
		vec4 s3 = texelFetch1D( tex, i + 3u );

		Light l;
		l.position = s0.rgb;
		l.type = int( round( s0.a ) );

		l.color = s1.rgb;
		l.intensity = s1.a;

		l.u = s2.rgb;
		l.v = s3.rgb;
		l.area = s3.a;

		if ( l.type == SPOT_LIGHT_TYPE || l.type == POINT_LIGHT_TYPE ) {

			vec4 s4 = texelFetch1D( tex, i + 4u );
			vec4 s5 = texelFetch1D( tex, i + 5u );
			l.radius = s4.r;
			l.decay = s4.g;
			l.distance = s4.b;
			l.coneCos = s4.a;

			l.penumbraCos = s5.r;
			l.iesProfile = int( round( s5.g ) );

		} else {

			l.radius = 0.0;
			l.decay = 0.0;
			l.distance = 0.0;

			l.coneCos = 0.0;
			l.penumbraCos = 0.0;
			l.iesProfile = - 1;

		}

		return l;

	}

`;var Bn=`

	struct Material {

		vec3 color;
		int map;

		float metalness;
		int metalnessMap;

		float roughness;
		int roughnessMap;

		float ior;
		float transmission;
		int transmissionMap;

		float emissiveIntensity;
		vec3 emissive;
		int emissiveMap;

		int normalMap;
		vec2 normalScale;

		float clearcoat;
		int clearcoatMap;
		int clearcoatNormalMap;
		vec2 clearcoatNormalScale;
		float clearcoatRoughness;
		int clearcoatRoughnessMap;

		int iridescenceMap;
		int iridescenceThicknessMap;
		float iridescence;
		float iridescenceIor;
		float iridescenceThicknessMinimum;
		float iridescenceThicknessMaximum;

		vec3 specularColor;
		int specularColorMap;

		float specularIntensity;
		int specularIntensityMap;
		bool thinFilm;

		vec3 attenuationColor;
		float attenuationDistance;

		int alphaMap;

		bool castShadow;
		float opacity;
		float alphaTest;

		float side;
		bool matte;

		float sheen;
		vec3 sheenColor;
		int sheenColorMap;
		float sheenRoughness;
		int sheenRoughnessMap;

		bool vertexColors;
		bool flatShading;
		bool transparent;
		bool fogVolume;

		mat3 mapTransform;
		mat3 metalnessMapTransform;
		mat3 roughnessMapTransform;
		mat3 transmissionMapTransform;
		mat3 emissiveMapTransform;
		mat3 normalMapTransform;
		mat3 clearcoatMapTransform;
		mat3 clearcoatNormalMapTransform;
		mat3 clearcoatRoughnessMapTransform;
		mat3 sheenColorMapTransform;
		mat3 sheenRoughnessMapTransform;
		mat3 iridescenceMapTransform;
		mat3 iridescenceThicknessMapTransform;
		mat3 specularColorMapTransform;
		mat3 specularIntensityMapTransform;

	};

	mat3 readTextureTransform( sampler2D tex, uint index ) {

		mat3 textureTransform;

		vec4 row1 = texelFetch1D( tex, index );
		vec4 row2 = texelFetch1D( tex, index + 1u );

		textureTransform[0] = vec3(row1.r, row2.r, 0.0);
		textureTransform[1] = vec3(row1.g, row2.g, 0.0);
		textureTransform[2] = vec3(row1.b, row2.b, 1.0);

		return textureTransform;

	}

	Material readMaterialInfo( sampler2D tex, uint index ) {

		uint i = index * 45u;

		vec4 s0 = texelFetch1D( tex, i + 0u );
		vec4 s1 = texelFetch1D( tex, i + 1u );
		vec4 s2 = texelFetch1D( tex, i + 2u );
		vec4 s3 = texelFetch1D( tex, i + 3u );
		vec4 s4 = texelFetch1D( tex, i + 4u );
		vec4 s5 = texelFetch1D( tex, i + 5u );
		vec4 s6 = texelFetch1D( tex, i + 6u );
		vec4 s7 = texelFetch1D( tex, i + 7u );
		vec4 s8 = texelFetch1D( tex, i + 8u );
		vec4 s9 = texelFetch1D( tex, i + 9u );
		vec4 s10 = texelFetch1D( tex, i + 10u );
		vec4 s11 = texelFetch1D( tex, i + 11u );
		vec4 s12 = texelFetch1D( tex, i + 12u );
		vec4 s13 = texelFetch1D( tex, i + 13u );
		vec4 s14 = texelFetch1D( tex, i + 14u );

		Material m;
		m.color = s0.rgb;
		m.map = int( round( s0.a ) );

		m.metalness = s1.r;
		m.metalnessMap = int( round( s1.g ) );
		m.roughness = s1.b;
		m.roughnessMap = int( round( s1.a ) );

		m.ior = s2.r;
		m.transmission = s2.g;
		m.transmissionMap = int( round( s2.b ) );
		m.emissiveIntensity = s2.a;

		m.emissive = s3.rgb;
		m.emissiveMap = int( round( s3.a ) );

		m.normalMap = int( round( s4.r ) );
		m.normalScale = s4.gb;

		m.clearcoat = s4.a;
		m.clearcoatMap = int( round( s5.r ) );
		m.clearcoatRoughness = s5.g;
		m.clearcoatRoughnessMap = int( round( s5.b ) );
		m.clearcoatNormalMap = int( round( s5.a ) );
		m.clearcoatNormalScale = s6.rg;

		m.sheen = s6.a;
		m.sheenColor = s7.rgb;
		m.sheenColorMap = int( round( s7.a ) );
		m.sheenRoughness = s8.r;
		m.sheenRoughnessMap = int( round( s8.g ) );

		m.iridescenceMap = int( round( s8.b ) );
		m.iridescenceThicknessMap = int( round( s8.a ) );
		m.iridescence = s9.r;
		m.iridescenceIor = s9.g;
		m.iridescenceThicknessMinimum = s9.b;
		m.iridescenceThicknessMaximum = s9.a;

		m.specularColor = s10.rgb;
		m.specularColorMap = int( round( s10.a ) );

		m.specularIntensity = s11.r;
		m.specularIntensityMap = int( round( s11.g ) );
		m.thinFilm = bool( s11.b );

		m.attenuationColor = s12.rgb;
		m.attenuationDistance = s12.a;

		m.alphaMap = int( round( s13.r ) );

		m.opacity = s13.g;
		m.alphaTest = s13.b;
		m.side = s13.a;

		m.matte = bool( s14.r );
		m.castShadow = bool( s14.g );
		m.vertexColors = bool( int( s14.b ) & 1 );
		m.flatShading = bool( int( s14.b ) & 2 );
		m.fogVolume = bool( int( s14.b ) & 4 );
		m.transparent = bool( s14.a );

		uint firstTextureTransformIdx = i + 15u;

		// mat3( 1.0 ) is an identity matrix
		m.mapTransform = m.map == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx );
		m.metalnessMapTransform = m.metalnessMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 2u );
		m.roughnessMapTransform = m.roughnessMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 4u );
		m.transmissionMapTransform = m.transmissionMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 6u );
		m.emissiveMapTransform = m.emissiveMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 8u );
		m.normalMapTransform = m.normalMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 10u );
		m.clearcoatMapTransform = m.clearcoatMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 12u );
		m.clearcoatNormalMapTransform = m.clearcoatNormalMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 14u );
		m.clearcoatRoughnessMapTransform = m.clearcoatRoughnessMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 16u );
		m.sheenColorMapTransform = m.sheenColorMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 18u );
		m.sheenRoughnessMapTransform = m.sheenRoughnessMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 20u );
		m.iridescenceMapTransform = m.iridescenceMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 22u );
		m.iridescenceThicknessMapTransform = m.iridescenceThicknessMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 24u );
		m.specularColorMapTransform = m.specularColorMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 26u );
		m.specularIntensityMapTransform = m.specularIntensityMap == - 1 ? mat3( 1.0 ) : readTextureTransform( tex, firstTextureTransformIdx + 28u );

		return m;

	}

`;var Pn=`

	struct SurfaceRecord {

		// surface type
		bool volumeParticle;

		// geometry
		vec3 faceNormal;
		bool frontFace;
		vec3 normal;
		mat3 normalBasis;
		mat3 normalInvBasis;

		// cached properties
		float eta;
		float f0;

		// material
		float roughness;
		float filteredRoughness;
		float metalness;
		vec3 color;
		vec3 emission;

		// transmission
		float ior;
		float transmission;
		bool thinFilm;
		vec3 attenuationColor;
		float attenuationDistance;

		// clearcoat
		vec3 clearcoatNormal;
		mat3 clearcoatBasis;
		mat3 clearcoatInvBasis;
		float clearcoat;
		float clearcoatRoughness;
		float filteredClearcoatRoughness;

		// sheen
		float sheen;
		vec3 sheenColor;
		float sheenRoughness;

		// iridescence
		float iridescence;
		float iridescenceIor;
		float iridescenceThickness;

		// specular
		vec3 specularColor;
		float specularIntensity;
	};

	struct ScatterRecord {
		float specularPdf;
		float pdf;
		vec3 direction;
		vec3 color;
	};

`;var Dn=`

	// samples the the given environment map in the given direction
	vec3 sampleEquirectColor( sampler2D envMap, vec3 direction ) {

		return texture2D( envMap, equirectDirectionToUv( direction ) ).rgb;

	}

	// gets the pdf of the given direction to sample
	float equirectDirectionPdf( vec3 direction ) {

		vec2 uv = equirectDirectionToUv( direction );
		float theta = uv.y * PI;
		float sinTheta = sin( theta );
		if ( sinTheta == 0.0 ) {

			return 0.0;

		}

		return 1.0 / ( 2.0 * PI * PI * sinTheta );

	}

	// samples the color given env map with CDF and returns the pdf of the direction
	float sampleEquirect( vec3 direction, inout vec3 color ) {

		float totalSum = envMapInfo.totalSum;
		if ( totalSum == 0.0 ) {

			color = vec3( 0.0 );
			return 1.0;

		}

		vec2 uv = equirectDirectionToUv( direction );
		color = texture2D( envMapInfo.map, uv ).rgb;

		float lum = luminance( color );
		ivec2 resolution = textureSize( envMapInfo.map, 0 );
		float pdf = lum / totalSum;

		return float( resolution.x * resolution.y ) * pdf * equirectDirectionPdf( direction );

	}

	// samples a direction of the envmap with color and retrieves pdf
	float sampleEquirectProbability( vec2 r, inout vec3 color, inout vec3 direction ) {

		// sample env map cdf
		float v = texture2D( envMapInfo.marginalWeights, vec2( r.x, 0.0 ) ).x;
		float u = texture2D( envMapInfo.conditionalWeights, vec2( r.y, v ) ).x;
		vec2 uv = vec2( u, v );

		vec3 derivedDirection = equirectUvToDirection( uv );
		direction = derivedDirection;
		color = texture2D( envMapInfo.map, uv ).rgb;

		float totalSum = envMapInfo.totalSum;
		float lum = luminance( color );
		ivec2 resolution = textureSize( envMapInfo.map, 0 );
		float pdf = lum / totalSum;

		return float( resolution.x * resolution.y ) * pdf * equirectDirectionPdf( direction );

	}
`;var En=`

	float getSpotAttenuation( const in float coneCosine, const in float penumbraCosine, const in float angleCosine ) {

		return smoothstep( coneCosine, penumbraCosine, angleCosine );

	}

	float getDistanceAttenuation( const in float lightDistance, const in float cutoffDistance, const in float decayExponent ) {

		// based upon Frostbite 3 Moving to Physically-based Rendering
		// page 32, equation 26: E[window1]
		// https://seblagarde.files.wordpress.com/2015/07/course_notes_moving_frostbite_to_pbr_v32.pdf
		float distanceFalloff = 1.0 / max( pow( lightDistance, decayExponent ), EPSILON );

		if ( cutoffDistance > 0.0 ) {

			distanceFalloff *= pow2( saturate( 1.0 - pow4( lightDistance / cutoffDistance ) ) );

		}

		return distanceFalloff;

	}

	float getPhotometricAttenuation( sampler2DArray iesProfiles, int iesProfile, vec3 posToLight, vec3 lightDir, vec3 u, vec3 v ) {

		float cosTheta = dot( posToLight, lightDir );
		float angle = acos( cosTheta ) / PI;

		return texture2D( iesProfiles, vec3( angle, 0.0, iesProfile ) ).r;

	}

	struct LightRecord {

		float dist;
		vec3 direction;
		float pdf;
		vec3 emission;
		int type;

	};

	bool intersectLightAtIndex( sampler2D lights, vec3 rayOrigin, vec3 rayDirection, uint l, inout LightRecord lightRec ) {

		bool didHit = false;
		Light light = readLightInfo( lights, l );

		vec3 u = light.u;
		vec3 v = light.v;

		// check for backface
		vec3 normal = normalize( cross( u, v ) );
		if ( dot( normal, rayDirection ) > 0.0 ) {

			u *= 1.0 / dot( u, u );
			v *= 1.0 / dot( v, v );

			float dist;

			// MIS / light intersection is not supported for punctual lights.
			if(
				( light.type == RECT_AREA_LIGHT_TYPE && intersectsRectangle( light.position, normal, u, v, rayOrigin, rayDirection, dist ) ) ||
				( light.type == CIRC_AREA_LIGHT_TYPE && intersectsCircle( light.position, normal, u, v, rayOrigin, rayDirection, dist ) )
			) {

				float cosTheta = dot( rayDirection, normal );
				didHit = true;
				lightRec.dist = dist;
				lightRec.pdf = ( dist * dist ) / ( light.area * cosTheta );
				lightRec.emission = light.color * light.intensity;
				lightRec.direction = rayDirection;
				lightRec.type = light.type;

			}

		}

		return didHit;

	}

	LightRecord randomAreaLightSample( Light light, vec3 rayOrigin, vec2 ruv ) {

		vec3 randomPos;
		if( light.type == RECT_AREA_LIGHT_TYPE ) {

			// rectangular area light
			randomPos = light.position + light.u * ( ruv.x - 0.5 ) + light.v * ( ruv.y - 0.5 );

		} else if( light.type == CIRC_AREA_LIGHT_TYPE ) {

			// circular area light
			float r = 0.5 * sqrt( ruv.x );
			float theta = ruv.y * 2.0 * PI;
			float x = r * cos( theta );
			float y = r * sin( theta );

			randomPos = light.position + light.u * x + light.v * y;

		}

		vec3 toLight = randomPos - rayOrigin;
		float lightDistSq = dot( toLight, toLight );
		float dist = sqrt( lightDistSq );
		vec3 direction = toLight / dist;
		vec3 lightNormal = normalize( cross( light.u, light.v ) );

		LightRecord lightRec;
		lightRec.type = light.type;
		lightRec.emission = light.color * light.intensity;
		lightRec.dist = dist;
		lightRec.direction = direction;

		// TODO: the denominator is potentially zero
		lightRec.pdf = lightDistSq / ( light.area * dot( direction, lightNormal ) );

		return lightRec;

	}

	LightRecord randomSpotLightSample( Light light, sampler2DArray iesProfiles, vec3 rayOrigin, vec2 ruv ) {

		float radius = light.radius * sqrt( ruv.x );
		float theta = ruv.y * 2.0 * PI;
		float x = radius * cos( theta );
		float y = radius * sin( theta );

		vec3 u = light.u;
		vec3 v = light.v;
		vec3 normal = normalize( cross( u, v ) );

		float angle = acos( light.coneCos );
		float angleTan = tan( angle );
		float startDistance = light.radius / max( angleTan, EPSILON );

		vec3 randomPos = light.position - normal * startDistance + u * x + v * y;
		vec3 toLight = randomPos - rayOrigin;
		float lightDistSq = dot( toLight, toLight );
		float dist = sqrt( lightDistSq );

		vec3 direction = toLight / max( dist, EPSILON );
		float cosTheta = dot( direction, normal );

		float spotAttenuation = light.iesProfile != - 1 ?
			getPhotometricAttenuation( iesProfiles, light.iesProfile, direction, normal, u, v ) :
			getSpotAttenuation( light.coneCos, light.penumbraCos, cosTheta );

		float distanceAttenuation = getDistanceAttenuation( dist, light.distance, light.decay );
		LightRecord lightRec;
		lightRec.type = light.type;
		lightRec.dist = dist;
		lightRec.direction = direction;
		lightRec.emission = light.color * light.intensity * distanceAttenuation * spotAttenuation;
		lightRec.pdf = 1.0;

		return lightRec;

	}

	LightRecord randomLightSample( sampler2D lights, sampler2DArray iesProfiles, uint lightCount, vec3 rayOrigin, vec3 ruv ) {

		LightRecord result;

		// pick a random light
		uint l = uint( ruv.x * float( lightCount ) );
		Light light = readLightInfo( lights, l );

		if ( light.type == SPOT_LIGHT_TYPE ) {

			result = randomSpotLightSample( light, iesProfiles, rayOrigin, ruv.yz );

		} else if ( light.type == POINT_LIGHT_TYPE ) {

			vec3 lightRay = light.u - rayOrigin;
			float lightDist = length( lightRay );
			float cutoffDistance = light.distance;
			float distanceFalloff = 1.0 / max( pow( lightDist, light.decay ), 0.01 );
			if ( cutoffDistance > 0.0 ) {

				distanceFalloff *= pow2( saturate( 1.0 - pow4( lightDist / cutoffDistance ) ) );

			}

			LightRecord rec;
			rec.direction = normalize( lightRay );
			rec.dist = length( lightRay );
			rec.pdf = 1.0;
			rec.emission = light.color * light.intensity * distanceFalloff;
			rec.type = light.type;
			result = rec;

		} else if ( light.type == DIR_LIGHT_TYPE ) {

			LightRecord rec;
			rec.dist = 1e10;
			rec.direction = light.u;
			rec.pdf = 1.0;
			rec.emission = light.color * light.intensity;
			rec.type = light.type;

			result = rec;

		} else {

			// sample the light
			result = randomAreaLightSample( light, rayOrigin, ruv.yz );

		}

		return result;

	}

`;var Ln=`

	vec3 sampleHemisphere( vec3 n, vec2 uv ) {

		// https://www.rorydriscoll.com/2009/01/07/better-sampling/
		// https://graphics.pixar.com/library/OrthonormalB/paper.pdf
		float sign = n.z == 0.0 ? 1.0 : sign( n.z );
		float a = - 1.0 / ( sign + n.z );
		float b = n.x * n.y * a;
		vec3 b1 = vec3( 1.0 + sign * n.x * n.x * a, sign * b, - sign * n.x );
		vec3 b2 = vec3( b, sign + n.y * n.y * a, - n.y );

		float r = sqrt( uv.x );
		float theta = 2.0 * PI * uv.y;
		float x = r * cos( theta );
		float y = r * sin( theta );
		return x * b1 + y * b2 + sqrt( 1.0 - uv.x ) * n;

	}

	vec2 sampleTriangle( vec2 a, vec2 b, vec2 c, vec2 r ) {

		// get the edges of the triangle and the diagonal across the
		// center of the parallelogram
		vec2 e1 = a - b;
		vec2 e2 = c - b;
		vec2 diag = normalize( e1 + e2 );

		// pick the point in the parallelogram
		if ( r.x + r.y > 1.0 ) {

			r = vec2( 1.0 ) - r;

		}

		return e1 * r.x + e2 * r.y;

	}

	vec2 sampleCircle( vec2 uv ) {

		float angle = 2.0 * PI * uv.x;
		float radius = sqrt( uv.y );
		return vec2( cos( angle ), sin( angle ) ) * radius;

	}

	vec3 sampleSphere( vec2 uv ) {

		float u = ( uv.x - 0.5 ) * 2.0;
		float t = uv.y * PI * 2.0;
		float f = sqrt( 1.0 - u * u );

		return vec3( f * cos( t ), f * sin( t ), u );

	}

	vec2 sampleRegularPolygon( int sides, vec3 uvw ) {

		sides = max( sides, 3 );

		vec3 r = uvw;
		float anglePerSegment = 2.0 * PI / float( sides );
		float segment = floor( float( sides ) * r.x );

		float angle1 = anglePerSegment * segment;
		float angle2 = angle1 + anglePerSegment;
		vec2 a = vec2( sin( angle1 ), cos( angle1 ) );
		vec2 b = vec2( 0.0, 0.0 );
		vec2 c = vec2( sin( angle2 ), cos( angle2 ) );

		return sampleTriangle( a, b, c, r.yz );

	}

	// samples an aperture shape with the given number of sides. 0 means circle
	vec2 sampleAperture( int blades, vec3 uvw ) {

		return blades == 0 ?
			sampleCircle( uvw.xy ) :
			sampleRegularPolygon( blades, uvw );

	}


`;var Nn=`

	bool totalInternalReflection( float cosTheta, float eta ) {

		float sinTheta = sqrt( 1.0 - cosTheta * cosTheta );
		return eta * sinTheta > 1.0;

	}

	// https://google.github.io/filament/Filament.md.html#materialsystem/diffusebrdf
	float schlickFresnel( float cosine, float f0 ) {

		return f0 + ( 1.0 - f0 ) * pow( 1.0 - cosine, 5.0 );

	}

	vec3 schlickFresnel( float cosine, vec3 f0 ) {

		return f0 + ( 1.0 - f0 ) * pow( 1.0 - cosine, 5.0 );

	}

	vec3 schlickFresnel( float cosine, vec3 f0, vec3 f90 ) {

		return f0 + ( f90 - f0 ) * pow( 1.0 - cosine, 5.0 );

	}

	float dielectricFresnel( float cosThetaI, float eta ) {

		// https://schuttejoe.github.io/post/disneybsdf/
		float ni = eta;
		float nt = 1.0;

		// Check for total internal reflection
		float sinThetaISq = 1.0f - cosThetaI * cosThetaI;
		float sinThetaTSq = eta * eta * sinThetaISq;
		if( sinThetaTSq >= 1.0 ) {

			return 1.0;

		}

		float sinThetaT = sqrt( sinThetaTSq );

		float cosThetaT = sqrt( max( 0.0, 1.0f - sinThetaT * sinThetaT ) );
		float rParallel = ( ( nt * cosThetaI ) - ( ni * cosThetaT ) ) / ( ( nt * cosThetaI ) + ( ni * cosThetaT ) );
		float rPerpendicular = ( ( ni * cosThetaI ) - ( nt * cosThetaT ) ) / ( ( ni * cosThetaI ) + ( nt * cosThetaT ) );
		return ( rParallel * rParallel + rPerpendicular * rPerpendicular ) / 2.0;

	}

	// https://raytracing.github.io/books/RayTracingInOneWeekend.html#dielectrics/schlickapproximation
	float iorRatioToF0( float eta ) {

		return pow( ( 1.0 - eta ) / ( 1.0 + eta ), 2.0 );

	}

	vec3 evaluateFresnel( float cosTheta, float eta, vec3 f0, vec3 f90 ) {

		if ( totalInternalReflection( cosTheta, eta ) ) {

			return f90;

		}

		return schlickFresnel( cosTheta, f0, f90 );

	}

	// TODO: disney fresnel was removed and replaced with this fresnel function to better align with
	// the glTF but is causing blown out pixels. Should be revisited
	// float evaluateFresnelWeight( float cosTheta, float eta, float f0 ) {

	// 	if ( totalInternalReflection( cosTheta, eta ) ) {

	// 		return 1.0;

	// 	}

	// 	return schlickFresnel( cosTheta, f0 );

	// }

	// https://schuttejoe.github.io/post/disneybsdf/
	float disneyFresnel( vec3 wo, vec3 wi, vec3 wh, float f0, float eta, float metalness ) {

		float dotHV = dot( wo, wh );
		if ( totalInternalReflection( dotHV, eta ) ) {

			return 1.0;

		}

		float dotHL = dot( wi, wh );
		float dielectricFresnel = dielectricFresnel( abs( dotHV ), eta );
		float metallicFresnel = schlickFresnel( dotHL, f0 );

		return mix( dielectricFresnel, metallicFresnel, metalness );

	}

`;var kn=`

	// Fast arccos approximation used to remove banding artifacts caused by numerical errors in acos.
	// This is a cubic Lagrange interpolating polynomial for x = [-1, -1/2, 0, 1/2, 1].
	// For more information see: https://github.com/gkjohnson/three-gpu-pathtracer/pull/171#issuecomment-1152275248
	float acosApprox( float x ) {

		x = clamp( x, -1.0, 1.0 );
		return ( - 0.69813170079773212 * x * x - 0.87266462599716477 ) * x + 1.5707963267948966;

	}

	// An acos with input values bound to the range [-1, 1].
	float acosSafe( float x ) {

		return acos( clamp( x, -1.0, 1.0 ) );

	}

	float saturateCos( float val ) {

		return clamp( val, 0.001, 1.0 );

	}

	float square( float t ) {

		return t * t;

	}

	vec2 square( vec2 t ) {

		return t * t;

	}

	vec3 square( vec3 t ) {

		return t * t;

	}

	vec4 square( vec4 t ) {

		return t * t;

	}

	vec2 rotateVector( vec2 v, float t ) {

		float ac = cos( t );
		float as = sin( t );
		return vec2(
			v.x * ac - v.y * as,
			v.x * as + v.y * ac
		);

	}

	// forms a basis with the normal vector as Z
	mat3 getBasisFromNormal( vec3 normal ) {

		vec3 other;
		if ( abs( normal.x ) > 0.5 ) {

			other = vec3( 0.0, 1.0, 0.0 );

		} else {

			other = vec3( 1.0, 0.0, 0.0 );

		}

		vec3 ortho = normalize( cross( normal, other ) );
		vec3 ortho2 = normalize( cross( normal, ortho ) );
		return mat3( ortho2, ortho, normal );

	}

`;var Gn=`

	// Finds the point where the ray intersects the plane defined by u and v and checks if this point
	// falls in the bounds of the rectangle on that same plane.
	// Plane intersection: https://lousodrome.net/blog/light/2020/07/03/intersection-of-a-ray-and-a-plane/
	bool intersectsRectangle( vec3 center, vec3 normal, vec3 u, vec3 v, vec3 rayOrigin, vec3 rayDirection, inout float dist ) {

		float t = dot( center - rayOrigin, normal ) / dot( rayDirection, normal );

		if ( t > EPSILON ) {

			vec3 p = rayOrigin + rayDirection * t;
			vec3 vi = p - center;

			// check if p falls inside the rectangle
			float a1 = dot( u, vi );
			if ( abs( a1 ) <= 0.5 ) {

				float a2 = dot( v, vi );
				if ( abs( a2 ) <= 0.5 ) {

					dist = t;
					return true;

				}

			}

		}

		return false;

	}

	// Finds the point where the ray intersects the plane defined by u and v and checks if this point
	// falls in the bounds of the circle on that same plane. See above URL for a description of the plane intersection algorithm.
	bool intersectsCircle( vec3 position, vec3 normal, vec3 u, vec3 v, vec3 rayOrigin, vec3 rayDirection, inout float dist ) {

		float t = dot( position - rayOrigin, normal ) / dot( rayDirection, normal );

		if ( t > EPSILON ) {

			vec3 hit = rayOrigin + rayDirection * t;
			vec3 vi = hit - position;

			float a1 = dot( u, vi );
			float a2 = dot( v, vi );

			if( length( vec2( a1, a2 ) ) <= 0.5 ) {

				dist = t;
				return true;

			}

		}

		return false;

	}

`;var On=`

	// add texel fetch functions for texture arrays
	vec4 texelFetch1D( sampler2DArray tex, int layer, uint index ) {

		uint width = uint( textureSize( tex, 0 ).x );
		uvec2 uv;
		uv.x = index % width;
		uv.y = index / width;

		return texelFetch( tex, ivec3( uv, layer ), 0 );

	}

	vec4 textureSampleBarycoord( sampler2DArray tex, int layer, vec3 barycoord, uvec3 faceIndices ) {

		return
			barycoord.x * texelFetch1D( tex, layer, faceIndices.x ) +
			barycoord.y * texelFetch1D( tex, layer, faceIndices.y ) +
			barycoord.z * texelFetch1D( tex, layer, faceIndices.z );

	}

`;var ft=`

	// TODO: possibly this should be renamed something related to material or path tracing logic

	#ifndef RAY_OFFSET
	#define RAY_OFFSET 1e-4
	#endif

	// adjust the hit point by the surface normal by a factor of some offset and the
	// maximum component-wise value of the current point to accommodate floating point
	// error as values increase.
	vec3 stepRayOrigin( vec3 rayOrigin, vec3 rayDirection, vec3 offset, float dist ) {

		vec3 point = rayOrigin + rayDirection * dist;
		vec3 absPoint = abs( point );
		float maxPoint = max( absPoint.x, max( absPoint.y, absPoint.z ) );
		return point + offset * ( maxPoint + 1.0 ) * RAY_OFFSET;

	}

	// https://github.com/KhronosGroup/glTF/blob/main/extensions/2.0/Khronos/KHR_materials_volume/README.md#attenuation
	vec3 transmissionAttenuation( float dist, vec3 attColor, float attDist ) {

		vec3 ot = - log( attColor ) / attDist;
		return exp( - ot * dist );

	}

	vec3 getHalfVector( vec3 wi, vec3 wo, float eta ) {

		// get the half vector - assuming if the light incident vector is on the other side
		// of the that it's transmissive.
		vec3 h;
		if ( wi.z > 0.0 ) {

			h = normalize( wi + wo );

		} else {

			// Scale by the ior ratio to retrieve the appropriate half vector
			// From Section 2.2 on computing the transmission half vector:
			// https://blog.selfshadow.com/publications/s2015-shading-course/burley/s2015_pbs_disney_bsdf_notes.pdf
			h = normalize( wi + wo * eta );

		}

		h *= sign( h.z );
		return h;

	}

	vec3 getHalfVector( vec3 a, vec3 b ) {

		return normalize( a + b );

	}

	// The discrepancy between interpolated surface normal and geometry normal can cause issues when a ray
	// is cast that is on the top side of the geometry normal plane but below the surface normal plane. If
	// we find a ray like that we ignore it to avoid artifacts.
	// This function returns if the direction is on the same side of both planes.
	bool isDirectionValid( vec3 direction, vec3 surfaceNormal, vec3 geometryNormal ) {

		bool aboveSurfaceNormal = dot( direction, surfaceNormal ) > 0.0;
		bool aboveGeometryNormal = dot( direction, geometryNormal ) > 0.0;
		return aboveSurfaceNormal == aboveGeometryNormal;

	}

	// ray sampling x and z are swapped to align with expected background view
	vec2 equirectDirectionToUv( vec3 direction ) {

		// from Spherical.setFromCartesianCoords
		vec2 uv = vec2( atan( direction.z, direction.x ), acos( direction.y ) );
		uv /= vec2( 2.0 * PI, PI );

		// apply adjustments to get values in range [0, 1] and y right side up
		uv.x += 0.5;
		uv.y = 1.0 - uv.y;
		return uv;

	}

	vec3 equirectUvToDirection( vec2 uv ) {

		// undo above adjustments
		uv.x -= 0.5;
		uv.y = 1.0 - uv.y;

		// from Vector3.setFromSphericalCoords
		float theta = uv.x * 2.0 * PI;
		float phi = uv.y * PI;

		float sinPhi = sin( phi );

		return vec3( sinPhi * cos( theta ), cos( phi ), sinPhi * sin( theta ) );

	}

	// power heuristic for multiple importance sampling
	float misHeuristic( float a, float b ) {

		float aa = a * a;
		float bb = b * b;
		return aa / ( aa + bb );

	}

	// tentFilter from Peter Shirley's 'Realistic Ray Tracing (2nd Edition)' book, pg. 60
	// erichlof/THREE.js-PathTracing-Renderer/
	float tentFilter( float x ) {

		return x < 0.5 ? sqrt( 2.0 * x ) - 1.0 : 1.0 - sqrt( 2.0 - ( 2.0 * x ) );

	}
`;var Lo=`

	// https://www.shadertoy.com/view/wltcRS
	uvec4 WHITE_NOISE_SEED;

	void rng_initialize( vec2 p, int frame ) {

		// white noise seed
		WHITE_NOISE_SEED = uvec4( p, uint( frame ), uint( p.x ) + uint( p.y ) );

	}

	// https://www.pcg-random.org/
	void pcg4d( inout uvec4 v ) {

		v = v * 1664525u + 1013904223u;
		v.x += v.y * v.w;
		v.y += v.z * v.x;
		v.z += v.x * v.y;
		v.w += v.y * v.z;
		v = v ^ ( v >> 16u );
		v.x += v.y*v.w;
		v.y += v.z*v.x;
		v.z += v.x*v.y;
		v.w += v.y*v.z;

	}

	// returns [ 0, 1 ]
	float pcgRand() {

		pcg4d( WHITE_NOISE_SEED );
		return float( WHITE_NOISE_SEED.x ) / float( 0xffffffffu );

	}

	vec2 pcgRand2() {

		pcg4d( WHITE_NOISE_SEED );
		return vec2( WHITE_NOISE_SEED.xy ) / float(0xffffffffu);

	}

	vec3 pcgRand3() {

		pcg4d( WHITE_NOISE_SEED );
		return vec3( WHITE_NOISE_SEED.xyz ) / float( 0xffffffffu );

	}

	vec4 pcgRand4() {

		pcg4d( WHITE_NOISE_SEED );
		return vec4( WHITE_NOISE_SEED ) / float( 0xffffffffu );

	}
`;var zn=`

	uniform sampler2D stratifiedTexture;
	uniform sampler2D stratifiedOffsetTexture;

	uint sobolPixelIndex = 0u;
	uint sobolPathIndex = 0u;
	uint sobolBounceIndex = 0u;
	vec4 pixelSeed = vec4( 0 );

	vec4 rand4( int v ) {

		ivec2 uv = ivec2( v, sobolBounceIndex );
		vec4 stratifiedSample = texelFetch( stratifiedTexture, uv, 0 );
		return fract( stratifiedSample + pixelSeed.r ); // blue noise + stratified samples

	}

	vec3 rand3( int v ) {

		return rand4( v ).xyz;

	}

	vec2 rand2( int v ) {

		return rand4( v ).xy;

	}

	float rand( int v ) {

		return rand4( v ).x;

	}

	void rng_initialize( vec2 screenCoord, int frame ) {

		// tile the small noise texture across the entire screen
		ivec2 noiseSize = ivec2( textureSize( stratifiedOffsetTexture, 0 ) );
		ivec2 pixel = ivec2( screenCoord.xy ) % noiseSize;
		vec2 pixelWidth = 1.0 / vec2( noiseSize );
		vec2 uv = vec2( pixel ) * pixelWidth + pixelWidth * 0.5;

		// note that using "texelFetch" here seems to break Android for some reason
		pixelSeed = texture( stratifiedOffsetTexture, uv );

	}

`;var Un=`

	// diffuse
	float diffuseEval( vec3 wo, vec3 wi, vec3 wh, SurfaceRecord surf, inout vec3 color ) {

		// https://schuttejoe.github.io/post/disneybsdf/
		float fl = schlickFresnel( wi.z, 0.0 );
		float fv = schlickFresnel( wo.z, 0.0 );

		float metalFactor = ( 1.0 - surf.metalness );
		float transFactor = ( 1.0 - surf.transmission );
		float rr = 0.5 + 2.0 * surf.roughness * fl * fl;
		float retro = rr * ( fl + fv + fl * fv * ( rr - 1.0f ) );
		float lambert = ( 1.0f - 0.5f * fl ) * ( 1.0f - 0.5f * fv );

		// TODO: subsurface approx?

		// float F = evaluateFresnelWeight( dot( wo, wh ), surf.eta, surf.f0 );
		float F = disneyFresnel( wo, wi, wh, surf.f0, surf.eta, surf.metalness );
		color = ( 1.0 - F ) * transFactor * metalFactor * wi.z * surf.color * ( retro + lambert ) / PI;

		return wi.z / PI;

	}

	vec3 diffuseDirection( vec3 wo, SurfaceRecord surf ) {

		vec3 lightDirection = sampleSphere( rand2( 11 ) );
		lightDirection.z += 1.0;
		lightDirection = normalize( lightDirection );

		return lightDirection;

	}

	// specular
	float specularEval( vec3 wo, vec3 wi, vec3 wh, SurfaceRecord surf, inout vec3 color ) {

		// if roughness is set to 0 then D === NaN which results in black pixels
		float metalness = surf.metalness;
		float roughness = surf.filteredRoughness;

		float eta = surf.eta;
		float f0 = surf.f0;

		vec3 f0Color = mix( f0 * surf.specularColor * surf.specularIntensity, surf.color, surf.metalness );
		vec3 f90Color = vec3( mix( surf.specularIntensity, 1.0, surf.metalness ) );
		vec3 F = evaluateFresnel( dot( wo, wh ), eta, f0Color, f90Color );

		vec3 iridescenceF = evalIridescence( 1.0, surf.iridescenceIor, dot( wi, wh ), surf.iridescenceThickness, f0Color );
		F = mix( F, iridescenceF,  surf.iridescence );

		// PDF
		// See 14.1.1 Microfacet BxDFs in https://www.pbr-book.org/
		float incidentTheta = acos( wo.z );
		float G = ggxShadowMaskG2( wi, wo, roughness );
		float D = ggxDistribution( wh, roughness );
		float G1 = ggxShadowMaskG1( incidentTheta, roughness );
		float ggxPdf = D * G1 * max( 0.0, abs( dot( wo, wh ) ) ) / abs ( wo.z );

		color = wi.z * F * G * D / ( 4.0 * abs( wi.z * wo.z ) );
		return ggxPdf / ( 4.0 * dot( wo, wh ) );

	}

	vec3 specularDirection( vec3 wo, SurfaceRecord surf ) {

		// sample ggx vndf distribution which gives a new normal
		float roughness = surf.filteredRoughness;
		vec3 halfVector = ggxDirection(
			wo,
			vec2( roughness ),
			rand2( 12 )
		);

		// apply to new ray by reflecting off the new normal
		return - reflect( wo, halfVector );

	}


	// transmission
	/*
	float transmissionEval( vec3 wo, vec3 wi, vec3 wh, SurfaceRecord surf, inout vec3 color ) {

		// See section 4.2 in https://www.cs.cornell.edu/~srm/publications/EGSR07-btdf.pdf

		float filteredRoughness = surf.filteredRoughness;
		float eta = surf.eta;
		bool frontFace = surf.frontFace;
		bool thinFilm = surf.thinFilm;

		color = surf.transmission * surf.color;

		float denom = pow( eta * dot( wi, wh ) + dot( wo, wh ), 2.0 );
		return ggxPDF( wo, wh, filteredRoughness ) / denom;

	}

	vec3 transmissionDirection( vec3 wo, SurfaceRecord surf ) {

		float filteredRoughness = surf.filteredRoughness;
		float eta = surf.eta;
		bool frontFace = surf.frontFace;

		// sample ggx vndf distribution which gives a new normal
		vec3 halfVector = ggxDirection(
			wo,
			vec2( filteredRoughness ),
			rand2( 13 )
		);

		vec3 lightDirection = refract( normalize( - wo ), halfVector, eta );
		if ( surf.thinFilm ) {

			lightDirection = - refract( normalize( - lightDirection ), - vec3( 0.0, 0.0, 1.0 ), 1.0 / eta );

		}

		return normalize( lightDirection );

	}
	*/

	// TODO: This is just using a basic cosine-weighted specular distribution with an
	// incorrect PDF value at the moment. Update it to correctly use a GGX distribution
	float transmissionEval( vec3 wo, vec3 wi, vec3 wh, SurfaceRecord surf, inout vec3 color ) {

		color = surf.transmission * surf.color;

		// PDF
		// float F = evaluateFresnelWeight( dot( wo, wh ), surf.eta, surf.f0 );
		// float F = disneyFresnel( wo, wi, wh, surf.f0, surf.eta, surf.metalness );
		// if ( F >= 1.0 ) {

		// 	return 0.0;

		// }

		// return 1.0 / ( 1.0 - F );

		// reverted to previous to transmission. The above was causing black pixels
		float eta = surf.eta;
		float f0 = surf.f0;
		float cosTheta = min( wo.z, 1.0 );
		float sinTheta = sqrt( 1.0 - cosTheta * cosTheta );
		float reflectance = schlickFresnel( cosTheta, f0 );
		bool cannotRefract = eta * sinTheta > 1.0;
		if ( cannotRefract ) {

			return 0.0;

		}

		return 1.0 / ( 1.0 - reflectance );

	}

	vec3 transmissionDirection( vec3 wo, SurfaceRecord surf ) {

		float roughness = surf.filteredRoughness;
		float eta = surf.eta;
		vec3 halfVector = normalize( vec3( 0.0, 0.0, 1.0 ) + sampleSphere( rand2( 13 ) ) * roughness );
		vec3 lightDirection = refract( normalize( - wo ), halfVector, eta );

		if ( surf.thinFilm ) {

			lightDirection = - refract( normalize( - lightDirection ), - vec3( 0.0, 0.0, 1.0 ), 1.0 / eta );

		}
		return normalize( lightDirection );

	}

	// clearcoat
	float clearcoatEval( vec3 wo, vec3 wi, vec3 wh, SurfaceRecord surf, inout vec3 color ) {

		float ior = 1.5;
		float f0 = iorRatioToF0( ior );
		bool frontFace = surf.frontFace;
		float roughness = surf.filteredClearcoatRoughness;

		float eta = frontFace ? 1.0 / ior : ior;
		float G = ggxShadowMaskG2( wi, wo, roughness );
		float D = ggxDistribution( wh, roughness );
		float F = schlickFresnel( dot( wi, wh ), f0 );

		float fClearcoat = F * D * G / ( 4.0 * abs( wi.z * wo.z ) );
		color = color * ( 1.0 - surf.clearcoat * F ) + fClearcoat * surf.clearcoat * wi.z;

		// PDF
		// See equation (27) in http://jcgt.org/published/0003/02/03/
		return ggxPDF( wo, wh, roughness ) / ( 4.0 * dot( wi, wh ) );

	}

	vec3 clearcoatDirection( vec3 wo, SurfaceRecord surf ) {

		// sample ggx vndf distribution which gives a new normal
		float roughness = surf.filteredClearcoatRoughness;
		vec3 halfVector = ggxDirection(
			wo,
			vec2( roughness ),
			rand2( 14 )
		);

		// apply to new ray by reflecting off the new normal
		return - reflect( wo, halfVector );

	}

	// sheen
	vec3 sheenColor( vec3 wo, vec3 wi, vec3 wh, SurfaceRecord surf ) {

		float cosThetaO = saturateCos( wo.z );
		float cosThetaI = saturateCos( wi.z );
		float cosThetaH = wh.z;

		float D = velvetD( cosThetaH, surf.sheenRoughness );
		float G = velvetG( cosThetaO, cosThetaI, surf.sheenRoughness );

		// See equation (1) in http://www.aconty.com/pdf/s2017_pbs_imageworks_sheen.pdf
		vec3 color = surf.sheenColor;
		color *= D * G / ( 4.0 * abs( cosThetaO * cosThetaI ) );
		color *= wi.z;

		return color;

	}

	// bsdf
	void getLobeWeights(
		vec3 wo, vec3 wi, vec3 wh, vec3 clearcoatWo, SurfaceRecord surf,
		inout float diffuseWeight, inout float specularWeight, inout float transmissionWeight, inout float clearcoatWeight
	) {

		float metalness = surf.metalness;
		float transmission = surf.transmission;
		// float fEstimate = evaluateFresnelWeight( dot( wo, wh ), surf.eta, surf.f0 );
		float fEstimate = disneyFresnel( wo, wi, wh, surf.f0, surf.eta, surf.metalness );

		float transSpecularProb = mix( max( 0.25, fEstimate ), 1.0, metalness );
		float diffSpecularProb = 0.5 + 0.5 * metalness;

		diffuseWeight = ( 1.0 - transmission ) * ( 1.0 - diffSpecularProb );
		specularWeight = transmission * transSpecularProb + ( 1.0 - transmission ) * diffSpecularProb;
		transmissionWeight = transmission * ( 1.0 - transSpecularProb );
		clearcoatWeight = surf.clearcoat * schlickFresnel( clearcoatWo.z, 0.04 );

		float totalWeight = diffuseWeight + specularWeight + transmissionWeight + clearcoatWeight;
		diffuseWeight /= totalWeight;
		specularWeight /= totalWeight;
		transmissionWeight /= totalWeight;
		clearcoatWeight /= totalWeight;
	}

	float bsdfEval(
		vec3 wo, vec3 clearcoatWo, vec3 wi, vec3 clearcoatWi, SurfaceRecord surf,
		float diffuseWeight, float specularWeight, float transmissionWeight, float clearcoatWeight, inout float specularPdf, inout vec3 color
	) {

		float metalness = surf.metalness;
		float transmission = surf.transmission;

		float spdf = 0.0;
		float dpdf = 0.0;
		float tpdf = 0.0;
		float cpdf = 0.0;
		color = vec3( 0.0 );

		vec3 halfVector = getHalfVector( wi, wo, surf.eta );

		// diffuse
		if ( diffuseWeight > 0.0 && wi.z > 0.0 ) {

			dpdf = diffuseEval( wo, wi, halfVector, surf, color );
			color *= 1.0 - surf.transmission;

		}

		// ggx specular
		if ( specularWeight > 0.0 && wi.z > 0.0 ) {

			vec3 outColor;
			spdf = specularEval( wo, wi, getHalfVector( wi, wo ), surf, outColor );
			color += outColor;

		}

		// transmission
		if ( transmissionWeight > 0.0 && wi.z < 0.0 ) {

			tpdf = transmissionEval( wo, wi, halfVector, surf, color );

		}

		// sheen
		color *= mix( 1.0, sheenAlbedoScaling( wo, wi, surf ), surf.sheen );
		color += sheenColor( wo, wi, halfVector, surf ) * surf.sheen;

		// clearcoat
		if ( clearcoatWi.z >= 0.0 && clearcoatWeight > 0.0 ) {

			vec3 clearcoatHalfVector = getHalfVector( clearcoatWo, clearcoatWi );
			cpdf = clearcoatEval( clearcoatWo, clearcoatWi, clearcoatHalfVector, surf, color );

		}

		float pdf =
			dpdf * diffuseWeight
			+ spdf * specularWeight
			+ tpdf * transmissionWeight
			+ cpdf * clearcoatWeight;

		// retrieve specular rays for the shadows flag
		specularPdf = spdf * specularWeight + cpdf * clearcoatWeight;

		return pdf;

	}

	float bsdfResult( vec3 worldWo, vec3 worldWi, SurfaceRecord surf, inout vec3 color ) {

		if ( surf.volumeParticle ) {

			color = surf.color / ( 4.0 * PI );
			return 1.0 / ( 4.0 * PI );

		}

		vec3 wo = normalize( surf.normalInvBasis * worldWo );
		vec3 wi = normalize( surf.normalInvBasis * worldWi );

		vec3 clearcoatWo = normalize( surf.clearcoatInvBasis * worldWo );
		vec3 clearcoatWi = normalize( surf.clearcoatInvBasis * worldWi );

		vec3 wh = getHalfVector( wo, wi, surf.eta );
		float diffuseWeight;
		float specularWeight;
		float transmissionWeight;
		float clearcoatWeight;
		getLobeWeights( wo, wi, wh, clearcoatWo, surf, diffuseWeight, specularWeight, transmissionWeight, clearcoatWeight );

		float specularPdf;
		return bsdfEval( wo, clearcoatWo, wi, clearcoatWi, surf, diffuseWeight, specularWeight, transmissionWeight, clearcoatWeight, specularPdf, color );

	}

	ScatterRecord bsdfSample( vec3 worldWo, SurfaceRecord surf ) {

		if ( surf.volumeParticle ) {

			ScatterRecord sampleRec;
			sampleRec.specularPdf = 0.0;
			sampleRec.pdf = 1.0 / ( 4.0 * PI );
			sampleRec.direction = sampleSphere( rand2( 16 ) );
			sampleRec.color = surf.color / ( 4.0 * PI );
			return sampleRec;

		}

		vec3 wo = normalize( surf.normalInvBasis * worldWo );
		vec3 clearcoatWo = normalize( surf.clearcoatInvBasis * worldWo );
		mat3 normalBasis = surf.normalBasis;
		mat3 invBasis = surf.normalInvBasis;
		mat3 clearcoatNormalBasis = surf.clearcoatBasis;
		mat3 clearcoatInvBasis = surf.clearcoatInvBasis;

		float diffuseWeight;
		float specularWeight;
		float transmissionWeight;
		float clearcoatWeight;
		// using normal and basically-reflected ray since we don't have proper half vector here
		getLobeWeights( wo, wo, vec3( 0, 0, 1 ), clearcoatWo, surf, diffuseWeight, specularWeight, transmissionWeight, clearcoatWeight );

		float pdf[4];
		pdf[0] = diffuseWeight;
		pdf[1] = specularWeight;
		pdf[2] = transmissionWeight;
		pdf[3] = clearcoatWeight;

		float cdf[4];
		cdf[0] = pdf[0];
		cdf[1] = pdf[1] + cdf[0];
		cdf[2] = pdf[2] + cdf[1];
		cdf[3] = pdf[3] + cdf[2];

		if( cdf[3] != 0.0 ) {

			float invMaxCdf = 1.0 / cdf[3];
			cdf[0] *= invMaxCdf;
			cdf[1] *= invMaxCdf;
			cdf[2] *= invMaxCdf;
			cdf[3] *= invMaxCdf;

		} else {

			cdf[0] = 1.0;
			cdf[1] = 0.0;
			cdf[2] = 0.0;
			cdf[3] = 0.0;

		}

		vec3 wi;
		vec3 clearcoatWi;

		float r = rand( 15 );
		if ( r <= cdf[0] ) { // diffuse

			wi = diffuseDirection( wo, surf );
			clearcoatWi = normalize( clearcoatInvBasis * normalize( normalBasis * wi ) );

		} else if ( r <= cdf[1] ) { // specular

			wi = specularDirection( wo, surf );
			clearcoatWi = normalize( clearcoatInvBasis * normalize( normalBasis * wi ) );

		} else if ( r <= cdf[2] ) { // transmission / refraction

			wi = transmissionDirection( wo, surf );
			clearcoatWi = normalize( clearcoatInvBasis * normalize( normalBasis * wi ) );

		} else if ( r <= cdf[3] ) { // clearcoat

			clearcoatWi = clearcoatDirection( clearcoatWo, surf );
			wi = normalize( invBasis * normalize( clearcoatNormalBasis * clearcoatWi ) );

		}

		ScatterRecord result;
		result.pdf = bsdfEval( wo, clearcoatWo, wi, clearcoatWi, surf, diffuseWeight, specularWeight, transmissionWeight, clearcoatWeight, result.specularPdf, result.color );
		result.direction = normalize( surf.normalBasis * wi );

		return result;

	}

`;var Hn=`

	// returns the hit distance given the material density
	float intersectFogVolume( Material material, float u ) {

		// https://raytracing.github.io/books/RayTracingTheNextWeek.html#volumes/constantdensitymediums
		return material.opacity == 0.0 ? INFINITY : ( - 1.0 / material.opacity ) * log( u );

	}

	ScatterRecord sampleFogVolume( SurfaceRecord surf, vec2 uv ) {

		ScatterRecord sampleRec;
		sampleRec.specularPdf = 0.0;
		sampleRec.pdf = 1.0 / ( 2.0 * PI );
		sampleRec.direction = sampleSphere( uv );
		sampleRec.color = surf.color;
		return sampleRec;

	}

`;var Wn=`

	// The GGX functions provide sampling and distribution information for normals as output so
	// in order to get probability of scatter direction the half vector must be computed and provided.
	// [0] https://www.cs.cornell.edu/~srm/publications/EGSR07-btdf.pdf
	// [1] https://hal.archives-ouvertes.fr/hal-01509746/document
	// [2] http://jcgt.org/published/0007/04/01/
	// [4] http://jcgt.org/published/0003/02/03/

	// trowbridge-reitz === GGX === GTR

	vec3 ggxDirection( vec3 incidentDir, vec2 roughness, vec2 uv ) {

		// TODO: try GGXVNDF implementation from reference [2], here. Needs to update ggxDistribution
		// function below, as well

		// Implementation from reference [1]
		// stretch view
		vec3 V = normalize( vec3( roughness * incidentDir.xy, incidentDir.z ) );

		// orthonormal basis
		vec3 T1 = ( V.z < 0.9999 ) ? normalize( cross( V, vec3( 0.0, 0.0, 1.0 ) ) ) : vec3( 1.0, 0.0, 0.0 );
		vec3 T2 = cross( T1, V );

		// sample point with polar coordinates (r, phi)
		float a = 1.0 / ( 1.0 + V.z );
		float r = sqrt( uv.x );
		float phi = ( uv.y < a ) ? uv.y / a * PI : PI + ( uv.y - a ) / ( 1.0 - a ) * PI;
		float P1 = r * cos( phi );
		float P2 = r * sin( phi ) * ( ( uv.y < a ) ? 1.0 : V.z );

		// compute normal
		vec3 N = P1 * T1 + P2 * T2 + V * sqrt( max( 0.0, 1.0 - P1 * P1 - P2 * P2 ) );

		// unstretch
		N = normalize( vec3( roughness * N.xy, max( 0.0, N.z ) ) );

		return N;

	}

	// Below are PDF and related functions for use in a Monte Carlo path tracer
	// as specified in Appendix B of the following paper
	// See equation (34) from reference [0]
	float ggxLamda( float theta, float roughness ) {

		float tanTheta = tan( theta );
		float tanTheta2 = tanTheta * tanTheta;
		float alpha2 = roughness * roughness;

		float numerator = - 1.0 + sqrt( 1.0 + alpha2 * tanTheta2 );
		return numerator / 2.0;

	}

	// See equation (34) from reference [0]
	float ggxShadowMaskG1( float theta, float roughness ) {

		return 1.0 / ( 1.0 + ggxLamda( theta, roughness ) );

	}

	// See equation (125) from reference [4]
	float ggxShadowMaskG2( vec3 wi, vec3 wo, float roughness ) {

		float incidentTheta = acos( wi.z );
		float scatterTheta = acos( wo.z );
		return 1.0 / ( 1.0 + ggxLamda( incidentTheta, roughness ) + ggxLamda( scatterTheta, roughness ) );

	}

	// See equation (33) from reference [0]
	float ggxDistribution( vec3 halfVector, float roughness ) {

		float a2 = roughness * roughness;
		a2 = max( EPSILON, a2 );
		float cosTheta = halfVector.z;
		float cosTheta4 = pow( cosTheta, 4.0 );

		if ( cosTheta == 0.0 ) return 0.0;

		float theta = acosSafe( halfVector.z );
		float tanTheta = tan( theta );
		float tanTheta2 = pow( tanTheta, 2.0 );

		float denom = PI * cosTheta4 * pow( a2 + tanTheta2, 2.0 );
		return ( a2 / denom );

	}

	// See equation (3) from reference [2]
	float ggxPDF( vec3 wi, vec3 halfVector, float roughness ) {

		float incidentTheta = acos( wi.z );
		float D = ggxDistribution( halfVector, roughness );
		float G1 = ggxShadowMaskG1( incidentTheta, roughness );

		return D * G1 * max( 0.0, dot( wi, halfVector ) ) / wi.z;

	}

`;var Vn=`

	// XYZ to sRGB color space
	const mat3 XYZ_TO_REC709 = mat3(
		3.2404542, -0.9692660,  0.0556434,
		-1.5371385,  1.8760108, -0.2040259,
		-0.4985314,  0.0415560,  1.0572252
	);

	vec3 fresnel0ToIor( vec3 fresnel0 ) {

		vec3 sqrtF0 = sqrt( fresnel0 );
		return ( vec3( 1.0 ) + sqrtF0 ) / ( vec3( 1.0 ) - sqrtF0 );

	}

	// Conversion FO/IOR
	vec3 iorToFresnel0( vec3 transmittedIor, float incidentIor ) {

		return square( ( transmittedIor - vec3( incidentIor ) ) / ( transmittedIor + vec3( incidentIor ) ) );

	}

	// ior is a value between 1.0 and 3.0. 1.0 is air interface
	float iorToFresnel0( float transmittedIor, float incidentIor ) {

		return square( ( transmittedIor - incidentIor ) / ( transmittedIor + incidentIor ) );

	}

	// Fresnel equations for dielectric/dielectric interfaces. See https://belcour.github.io/blog/research/2017/05/01/brdf-thin-film.html
	vec3 evalSensitivity( float OPD, vec3 shift ) {

		float phase = 2.0 * PI * OPD * 1.0e-9;

		vec3 val = vec3( 5.4856e-13, 4.4201e-13, 5.2481e-13 );
		vec3 pos = vec3( 1.6810e+06, 1.7953e+06, 2.2084e+06 );
		vec3 var = vec3( 4.3278e+09, 9.3046e+09, 6.6121e+09 );

		vec3 xyz = val * sqrt( 2.0 * PI * var ) * cos( pos * phase + shift ) * exp( - square( phase ) * var );
		xyz.x += 9.7470e-14 * sqrt( 2.0 * PI * 4.5282e+09 ) * cos( 2.2399e+06 * phase + shift[ 0 ] ) * exp( - 4.5282e+09 * square( phase ) );
		xyz /= 1.0685e-7;

		vec3 srgb = XYZ_TO_REC709 * xyz;
		return srgb;

	}

	// See Section 4. Analytic Spectral Integration, A Practical Extension to Microfacet Theory for the Modeling of Varying Iridescence, https://hal.archives-ouvertes.fr/hal-01518344/document
	vec3 evalIridescence( float outsideIOR, float eta2, float cosTheta1, float thinFilmThickness, vec3 baseF0 ) {

		vec3 I;

		// Force iridescenceIor -> outsideIOR when thinFilmThickness -> 0.0
		float iridescenceIor = mix( outsideIOR, eta2, smoothstep( 0.0, 0.03, thinFilmThickness ) );

		// Evaluate the cosTheta on the base layer (Snell law)
		float sinTheta2Sq = square( outsideIOR / iridescenceIor ) * ( 1.0 - square( cosTheta1 ) );

		// Handle TIR:
		float cosTheta2Sq = 1.0 - sinTheta2Sq;
		if ( cosTheta2Sq < 0.0 ) {

			return vec3( 1.0 );

		}

		float cosTheta2 = sqrt( cosTheta2Sq );

		// First interface
		float R0 = iorToFresnel0( iridescenceIor, outsideIOR );
		float R12 = schlickFresnel( cosTheta1, R0 );
		float R21 = R12;
		float T121 = 1.0 - R12;
		float phi12 = 0.0;
		if ( iridescenceIor < outsideIOR ) {

			phi12 = PI;

		}

		float phi21 = PI - phi12;

		// Second interface
		vec3 baseIOR = fresnel0ToIor( clamp( baseF0, 0.0, 0.9999 ) ); // guard against 1.0
		vec3 R1 = iorToFresnel0( baseIOR, iridescenceIor );
		vec3 R23 = schlickFresnel( cosTheta2, R1 );
		vec3 phi23 = vec3( 0.0 );
		if ( baseIOR[0] < iridescenceIor ) {

			phi23[ 0 ] = PI;

		}

		if ( baseIOR[1] < iridescenceIor ) {

			phi23[ 1 ] = PI;

		}

		if ( baseIOR[2] < iridescenceIor ) {

			phi23[ 2 ] = PI;

		}

		// Phase shift
		float OPD = 2.0 * iridescenceIor * thinFilmThickness * cosTheta2;
		vec3 phi = vec3( phi21 ) + phi23;

		// Compound terms
		vec3 R123 = clamp( R12 * R23, 1e-5, 0.9999 );
		vec3 r123 = sqrt( R123 );
		vec3 Rs = square( T121 ) * R23 / ( vec3( 1.0 ) - R123 );

		// Reflectance term for m = 0 (DC term amplitude)
		vec3 C0 = R12 + Rs;
		I = C0;

		// Reflectance term for m > 0 (pairs of diracs)
		vec3 Cm = Rs - T121;
		for ( int m = 1; m <= 2; ++ m ) {

			Cm *= r123;
			vec3 Sm = 2.0 * evalSensitivity( float( m ) * OPD, float( m ) * phi );
			I += Cm * Sm;

		}

		// Since out of gamut colors might be produced, negative color values are clamped to 0.
		return max( I, vec3( 0.0 ) );

	}

`;var qn=`

	// See equation (2) in http://www.aconty.com/pdf/s2017_pbs_imageworks_sheen.pdf
	float velvetD( float cosThetaH, float roughness ) {

		float alpha = max( roughness, 0.07 );
		alpha = alpha * alpha;

		float invAlpha = 1.0 / alpha;

		float sqrCosThetaH = cosThetaH * cosThetaH;
		float sinThetaH = max( 1.0 - sqrCosThetaH, 0.001 );

		return ( 2.0 + invAlpha ) * pow( sinThetaH, 0.5 * invAlpha ) / ( 2.0 * PI );

	}

	float velvetParamsInterpolate( int i, float oneMinusAlphaSquared ) {

		const float p0[5] = float[5]( 25.3245, 3.32435, 0.16801, -1.27393, -4.85967 );
		const float p1[5] = float[5]( 21.5473, 3.82987, 0.19823, -1.97760, -4.32054 );

		return mix( p1[i], p0[i], oneMinusAlphaSquared );

	}

	float velvetL( float x, float alpha ) {

		float oneMinusAlpha = 1.0 - alpha;
		float oneMinusAlphaSquared = oneMinusAlpha * oneMinusAlpha;

		float a = velvetParamsInterpolate( 0, oneMinusAlphaSquared );
		float b = velvetParamsInterpolate( 1, oneMinusAlphaSquared );
		float c = velvetParamsInterpolate( 2, oneMinusAlphaSquared );
		float d = velvetParamsInterpolate( 3, oneMinusAlphaSquared );
		float e = velvetParamsInterpolate( 4, oneMinusAlphaSquared );

		return a / ( 1.0 + b * pow( abs( x ), c ) ) + d * x + e;

	}

	// See equation (3) in http://www.aconty.com/pdf/s2017_pbs_imageworks_sheen.pdf
	float velvetLambda( float cosTheta, float alpha ) {

		return abs( cosTheta ) < 0.5 ? exp( velvetL( cosTheta, alpha ) ) : exp( 2.0 * velvetL( 0.5, alpha ) - velvetL( 1.0 - cosTheta, alpha ) );

	}

	// See Section 3, Shadowing Term, in http://www.aconty.com/pdf/s2017_pbs_imageworks_sheen.pdf
	float velvetG( float cosThetaO, float cosThetaI, float roughness ) {

		float alpha = max( roughness, 0.07 );
		alpha = alpha * alpha;

		return 1.0 / ( 1.0 + velvetLambda( cosThetaO, alpha ) + velvetLambda( cosThetaI, alpha ) );

	}

	float directionalAlbedoSheen( float cosTheta, float alpha ) {

		cosTheta = saturate( cosTheta );

		float c = 1.0 - cosTheta;
		float c3 = c * c * c;

		return 0.65584461 * c3 + 1.0 / ( 4.16526551 + exp( -7.97291361 * sqrt( alpha ) + 6.33516894 ) );

	}

	float sheenAlbedoScaling( vec3 wo, vec3 wi, SurfaceRecord surf ) {

		float alpha = max( surf.sheenRoughness, 0.07 );
		alpha = alpha * alpha;

		float maxSheenColor = max( max( surf.sheenColor.r, surf.sheenColor.g ), surf.sheenColor.b );

		float eWo = directionalAlbedoSheen( saturateCos( wo.z ), alpha );
		float eWi = directionalAlbedoSheen( saturateCos( wi.z ), alpha );

		return min( 1.0 - maxSheenColor * eWo, 1.0 - maxSheenColor * eWi );

	}

	// See Section 5, Layering, in http://www.aconty.com/pdf/s2017_pbs_imageworks_sheen.pdf
	float sheenAlbedoScaling( vec3 wo, SurfaceRecord surf ) {

		float alpha = max( surf.sheenRoughness, 0.07 );
		alpha = alpha * alpha;

		float maxSheenColor = max( max( surf.sheenColor.r, surf.sheenColor.g ), surf.sheenColor.b );

		float eWo = directionalAlbedoSheen( saturateCos( wo.z ), alpha );

		return 1.0 - maxSheenColor * eWo;

	}

`;var jn=`

#ifndef FOG_CHECK_ITERATIONS
#define FOG_CHECK_ITERATIONS 30
#endif

// returns whether the given material is a fog material or not
bool isMaterialFogVolume( sampler2D materials, uint materialIndex ) {

	uint i = materialIndex * 45u;
	vec4 s14 = texelFetch1D( materials, i + 14u );
	return bool( int( s14.b ) & 4 );

}

// returns true if we're within the first fog volume we hit
bool bvhIntersectFogVolumeHit(
	vec3 rayOrigin, vec3 rayDirection,
	usampler2D materialIndexAttribute, sampler2D materials,
	inout Material material
) {

	material.fogVolume = false;

	for ( int i = 0; i < FOG_CHECK_ITERATIONS; i ++ ) {

		// find nearest hit
		uvec4 faceIndices = uvec4( 0u );
		vec3 faceNormal = vec3( 0.0, 0.0, 1.0 );
		vec3 barycoord = vec3( 0.0 );
		float side = 1.0;
		float dist = 0.0;
		bool hit = bvhIntersectFirstHit( bvh, rayOrigin, rayDirection, faceIndices, faceNormal, barycoord, side, dist );
		if ( hit ) {

			// if it's a fog volume return whether we hit the front or back face
			uint materialIndex = uTexelFetch1D( materialIndexAttribute, faceIndices.x ).r;
			if ( isMaterialFogVolume( materials, materialIndex ) ) {

				material = readMaterialInfo( materials, materialIndex );
				return side == - 1.0;

			} else {

				// move the ray forward
				rayOrigin = stepRayOrigin( rayOrigin, rayDirection, - faceNormal, dist );

			}

		} else {

			return false;

		}

	}

	return false;

}

`;var Xn=`

	// step through multiple surface hits and accumulate color attenuation based on transmissive surfaces
	// returns true if a solid surface was hit
	bool attenuateHit(
		RenderState state,
		Ray ray, float rayDist,
		out vec3 color
	) {

		// store the original bounce index so we can reset it after
		uint originalBounceIndex = sobolBounceIndex;

		int traversals = state.traversals;
		int transmissiveTraversals = state.transmissiveTraversals;
		bool isShadowRay = state.isShadowRay;
		Material fogMaterial = state.fogMaterial;

		vec3 startPoint = ray.origin;

		// hit results
		SurfaceHit surfaceHit;

		color = vec3( 1.0 );

		bool result = true;
		for ( int i = 0; i < traversals; i ++ ) {

			sobolBounceIndex ++;

			int hitType = traceScene( ray, fogMaterial, surfaceHit );

			if ( hitType == FOG_HIT ) {

				result = true;
				break;

			} else if ( hitType == SURFACE_HIT ) {

				float totalDist = distance( startPoint, ray.origin + ray.direction * surfaceHit.dist );
				if ( totalDist > rayDist ) {

					result = false;
					break;

				}

				// TODO: attenuate the contribution based on the PDF of the resulting ray including refraction values
				// Should be able to work using the material BSDF functions which will take into account specularity, etc.
				// TODO: should we account for emissive surfaces here?

				uint materialIndex = uTexelFetch1D( materialIndexAttribute, surfaceHit.faceIndices.x ).r;
				Material material = readMaterialInfo( materials, materialIndex );

				// adjust the ray to the new surface
				bool isEntering = surfaceHit.side == 1.0;
				ray.origin = stepRayOrigin( ray.origin, ray.direction, - surfaceHit.faceNormal, surfaceHit.dist );

				#if FEATURE_FOG

				if ( material.fogVolume ) {

					fogMaterial = material;
					fogMaterial.fogVolume = surfaceHit.side == 1.0;
					i -= sign( transmissiveTraversals );
					transmissiveTraversals --;
					continue;

				}

				#endif

				if ( ! material.castShadow && isShadowRay ) {

					continue;

				}

				vec2 uv = textureSampleBarycoord( attributesArray, ATTR_UV, surfaceHit.barycoord, surfaceHit.faceIndices.xyz ).xy;
				vec4 vertexColor = textureSampleBarycoord( attributesArray, ATTR_COLOR, surfaceHit.barycoord, surfaceHit.faceIndices.xyz );

				// albedo
				vec4 albedo = vec4( material.color, material.opacity );
				if ( material.map != - 1 ) {

					vec3 uvPrime = material.mapTransform * vec3( uv, 1 );
					albedo *= texture2D( textures, vec3( uvPrime.xy, material.map ) );

				}

				if ( material.vertexColors ) {

					albedo *= vertexColor;

				}

				// alphaMap
				if ( material.alphaMap != - 1 ) {

					albedo.a *= texture2D( textures, vec3( uv, material.alphaMap ) ).x;

				}

				// transmission
				float transmission = material.transmission;
				if ( material.transmissionMap != - 1 ) {

					vec3 uvPrime = material.transmissionMapTransform * vec3( uv, 1 );
					transmission *= texture2D( textures, vec3( uvPrime.xy, material.transmissionMap ) ).r;

				}

				// metalness
				float metalness = material.metalness;
				if ( material.metalnessMap != - 1 ) {

					vec3 uvPrime = material.metalnessMapTransform * vec3( uv, 1 );
					metalness *= texture2D( textures, vec3( uvPrime.xy, material.metalnessMap ) ).b;

				}

				float alphaTest = material.alphaTest;
				bool useAlphaTest = alphaTest != 0.0;
				float transmissionFactor = ( 1.0 - metalness ) * transmission;
				if (
					transmissionFactor < rand( 9 ) && ! (
						// material sidedness
						material.side != 0.0 && surfaceHit.side == material.side

						// alpha test
						|| useAlphaTest && albedo.a < alphaTest

						// opacity
						|| material.transparent && ! useAlphaTest && albedo.a < rand( 10 )
					)
				) {

					result = true;
					break;

				}

				if ( surfaceHit.side == 1.0 && isEntering ) {

					// only attenuate by surface color on the way in
					color *= mix( vec3( 1.0 ), albedo.rgb, transmissionFactor );

				} else if ( surfaceHit.side == - 1.0 ) {

					// attenuate by medium once we hit the opposite side of the model
					color *= transmissionAttenuation( surfaceHit.dist, material.attenuationColor, material.attenuationDistance );

				}

				bool isTransmissiveRay = dot( ray.direction, surfaceHit.faceNormal * surfaceHit.side ) < 0.0;
				if ( ( isTransmissiveRay || isEntering ) && transmissiveTraversals > 0 ) {

					i -= sign( transmissiveTraversals );
					transmissiveTraversals --;

				}

			} else {

				result = false;
				break;

			}

		}

		// reset the bounce index
		sobolBounceIndex = originalBounceIndex;
		return result;

	}

`;var Yn=`

	vec3 ndcToRayOrigin( vec2 coord ) {

		vec4 rayOrigin4 = cameraWorldMatrix * invProjectionMatrix * vec4( coord, - 1.0, 1.0 );
		return rayOrigin4.xyz / rayOrigin4.w;
	}

	Ray getCameraRay() {

		vec2 ssd = vec2( 1.0 ) / resolution;

		// Jitter the camera ray by finding a uv coordinate at a random sample
		// around this pixel's UV coordinate for AA
		vec2 ruv = rand2( 0 );
		vec2 jitteredUv = vUv + vec2( tentFilter( ruv.x ) * ssd.x, tentFilter( ruv.y ) * ssd.y );
		Ray ray;

		#if CAMERA_TYPE == 2

			// Equirectangular projection
			vec4 rayDirection4 = vec4( equirectUvToDirection( jitteredUv ), 0.0 );
			vec4 rayOrigin4 = vec4( 0.0, 0.0, 0.0, 1.0 );

			rayDirection4 = cameraWorldMatrix * rayDirection4;
			rayOrigin4 = cameraWorldMatrix * rayOrigin4;

			ray.direction = normalize( rayDirection4.xyz );
			ray.origin = rayOrigin4.xyz / rayOrigin4.w;

		#else

			// get [- 1, 1] normalized device coordinates
			vec2 ndc = 2.0 * jitteredUv - vec2( 1.0 );
			ray.origin = ndcToRayOrigin( ndc );

			#if CAMERA_TYPE == 1

				// Orthographic projection
				ray.direction = ( cameraWorldMatrix * vec4( 0.0, 0.0, - 1.0, 0.0 ) ).xyz;
				ray.direction = normalize( ray.direction );

			#else

				// Perspective projection
				ray.direction = normalize( mat3( cameraWorldMatrix ) * ( invProjectionMatrix * vec4( ndc, 0.0, 1.0 ) ).xyz );

			#endif

		#endif

		#if FEATURE_DOF
		{

			// depth of field
			vec3 focalPoint = ray.origin + normalize( ray.direction ) * physicalCamera.focusDistance;

			// get the aperture sample
			// if blades === 0 then we assume a circle
			vec3 shapeUVW= rand3( 1 );
			int blades = physicalCamera.apertureBlades;
			float anamorphicRatio = physicalCamera.anamorphicRatio;
			vec2 apertureSample = blades == 0 ? sampleCircle( shapeUVW.xy ) : sampleRegularPolygon( blades, shapeUVW );
			apertureSample *= physicalCamera.bokehSize * 0.5 * 1e-3;

			// rotate the aperture shape
			apertureSample =
				rotateVector( apertureSample, physicalCamera.apertureRotation ) *
				saturate( vec2( anamorphicRatio, 1.0 / anamorphicRatio ) );

			// create the new ray
			ray.origin += ( cameraWorldMatrix * vec4( apertureSample, 0.0, 0.0 ) ).xyz;
			ray.direction = focalPoint - ray.origin;

		}
		#endif

		ray.direction = normalize( ray.direction );

		return ray;

	}

`;var $n=`

	vec3 directLightContribution( vec3 worldWo, SurfaceRecord surf, RenderState state, vec3 rayOrigin ) {

		vec3 result = vec3( 0.0 );

		// uniformly pick a light or environment map
		if( lightsDenom != 0.0 && rand( 5 ) < float( lights.count ) / lightsDenom ) {

			// sample a light or environment
			LightRecord lightRec = randomLightSample( lights.tex, iesProfiles, lights.count, rayOrigin, rand3( 6 ) );

			bool isSampleBelowSurface = ! surf.volumeParticle && dot( surf.faceNormal, lightRec.direction ) < 0.0;
			if ( isSampleBelowSurface ) {

				lightRec.pdf = 0.0;

			}

			// check if a ray could even reach the light area
			Ray lightRay;
			lightRay.origin = rayOrigin;
			lightRay.direction = lightRec.direction;
			vec3 attenuatedColor;
			if (
				lightRec.pdf > 0.0 &&
				isDirectionValid( lightRec.direction, surf.normal, surf.faceNormal ) &&
				! attenuateHit( state, lightRay, lightRec.dist, attenuatedColor )
			) {

				// get the material pdf
				vec3 sampleColor;
				float lightMaterialPdf = bsdfResult( worldWo, lightRec.direction, surf, sampleColor );
				bool isValidSampleColor = all( greaterThanEqual( sampleColor, vec3( 0.0 ) ) );
				if ( lightMaterialPdf > 0.0 && isValidSampleColor ) {

					// weight the direct light contribution
					float lightPdf = lightRec.pdf / lightsDenom;
					float misWeight = lightRec.type == SPOT_LIGHT_TYPE || lightRec.type == DIR_LIGHT_TYPE || lightRec.type == POINT_LIGHT_TYPE ? 1.0 : misHeuristic( lightPdf, lightMaterialPdf );
					result = attenuatedColor * lightRec.emission * state.throughputColor * sampleColor * misWeight / lightPdf;

				}

			}

		} else if ( envMapInfo.totalSum != 0.0 && environmentIntensity != 0.0 ) {

			// find a sample in the environment map to include in the contribution
			vec3 envColor, envDirection;
			float envPdf = sampleEquirectProbability( rand2( 7 ), envColor, envDirection );
			envDirection = invEnvRotation3x3 * envDirection;

			// this env sampling is not set up for transmissive sampling and yields overly bright
			// results so we ignore the sample in this case.
			// TODO: this should be improved but how? The env samples could traverse a few layers?
			bool isSampleBelowSurface = ! surf.volumeParticle && dot( surf.faceNormal, envDirection ) < 0.0;
			if ( isSampleBelowSurface ) {

				envPdf = 0.0;

			}

			// check if a ray could even reach the surface
			Ray envRay;
			envRay.origin = rayOrigin;
			envRay.direction = envDirection;
			vec3 attenuatedColor;
			if (
				envPdf > 0.0 &&
				isDirectionValid( envDirection, surf.normal, surf.faceNormal ) &&
				! attenuateHit( state, envRay, INFINITY, attenuatedColor )
			) {

				// get the material pdf
				vec3 sampleColor;
				float envMaterialPdf = bsdfResult( worldWo, envDirection, surf, sampleColor );
				bool isValidSampleColor = all( greaterThanEqual( sampleColor, vec3( 0.0 ) ) );
				if ( envMaterialPdf > 0.0 && isValidSampleColor ) {

					// weight the direct light contribution
					envPdf /= lightsDenom;
					float misWeight = misHeuristic( envPdf, envMaterialPdf );
					result = attenuatedColor * environmentIntensity * envColor * state.throughputColor * sampleColor * misWeight / envPdf;

				}

			}

		}

		// Function changed to have a single return statement to potentially help with crashes on Mac OS.
		// See issue #470
		return result;

	}

`;var Qn=`

	#define SKIP_SURFACE 0
	#define HIT_SURFACE 1
	int getSurfaceRecord(
		Material material, SurfaceHit surfaceHit, sampler2DArray attributesArray,
		float accumulatedRoughness,
		inout SurfaceRecord surf
	) {

		if ( material.fogVolume ) {

			vec3 normal = vec3( 0, 0, 1 );

			SurfaceRecord fogSurface;
			fogSurface.volumeParticle = true;
			fogSurface.color = material.color;
			fogSurface.emission = material.emissiveIntensity * material.emissive;
			fogSurface.normal = normal;
			fogSurface.faceNormal = normal;
			fogSurface.clearcoatNormal = normal;

			surf = fogSurface;
			return HIT_SURFACE;

		}

		// uv coord for textures
		vec2 uv = textureSampleBarycoord( attributesArray, ATTR_UV, surfaceHit.barycoord, surfaceHit.faceIndices.xyz ).xy;
		vec4 vertexColor = textureSampleBarycoord( attributesArray, ATTR_COLOR, surfaceHit.barycoord, surfaceHit.faceIndices.xyz );

		// albedo
		vec4 albedo = vec4( material.color, material.opacity );
		if ( material.map != - 1 ) {

			vec3 uvPrime = material.mapTransform * vec3( uv, 1 );
			albedo *= texture2D( textures, vec3( uvPrime.xy, material.map ) );

		}

		if ( material.vertexColors ) {

			albedo *= vertexColor;

		}

		// alphaMap
		if ( material.alphaMap != - 1 ) {

			albedo.a *= texture2D( textures, vec3( uv, material.alphaMap ) ).x;

		}

		// possibly skip this sample if it's transparent, alpha test is enabled, or we hit the wrong material side
		// and it's single sided.
		// - alpha test is disabled when it === 0
		// - the material sidedness test is complicated because we want light to pass through the back side but still
		// be able to see the front side. This boolean checks if the side we hit is the front side on the first ray
		// and we're rendering the other then we skip it. Do the opposite on subsequent bounces to get incoming light.
		float alphaTest = material.alphaTest;
		bool useAlphaTest = alphaTest != 0.0;
		if (
			// material sidedness
			material.side != 0.0 && surfaceHit.side != material.side

			// alpha test
			|| useAlphaTest && albedo.a < alphaTest

			// opacity
			|| material.transparent && ! useAlphaTest && albedo.a < rand( 3 )
		) {

			return SKIP_SURFACE;

		}

		// fetch the interpolated smooth normal
		vec3 normal = normalize( textureSampleBarycoord(
			attributesArray,
			ATTR_NORMAL,
			surfaceHit.barycoord,
			surfaceHit.faceIndices.xyz
		).xyz );

		// roughness
		float roughness = material.roughness;
		if ( material.roughnessMap != - 1 ) {

			vec3 uvPrime = material.roughnessMapTransform * vec3( uv, 1 );
			roughness *= texture2D( textures, vec3( uvPrime.xy, material.roughnessMap ) ).g;

		}

		// metalness
		float metalness = material.metalness;
		if ( material.metalnessMap != - 1 ) {

			vec3 uvPrime = material.metalnessMapTransform * vec3( uv, 1 );
			metalness *= texture2D( textures, vec3( uvPrime.xy, material.metalnessMap ) ).b;

		}

		// emission
		vec3 emission = material.emissiveIntensity * material.emissive;
		if ( material.emissiveMap != - 1 ) {

			vec3 uvPrime = material.emissiveMapTransform * vec3( uv, 1 );
			emission *= texture2D( textures, vec3( uvPrime.xy, material.emissiveMap ) ).xyz;

		}

		// transmission
		float transmission = material.transmission;
		if ( material.transmissionMap != - 1 ) {

			vec3 uvPrime = material.transmissionMapTransform * vec3( uv, 1 );
			transmission *= texture2D( textures, vec3( uvPrime.xy, material.transmissionMap ) ).r;

		}

		// normal
		if ( material.flatShading ) {

			// if we're rendering a flat shaded object then use the face normals - the face normal
			// is provided based on the side the ray hits the mesh so flip it to align with the
			// interpolated vertex normals.
			normal = surfaceHit.faceNormal * surfaceHit.side;

		}

		vec3 baseNormal = normal;
		if ( material.normalMap != - 1 ) {

			vec4 tangentSample = textureSampleBarycoord(
				attributesArray,
				ATTR_TANGENT,
				surfaceHit.barycoord,
				surfaceHit.faceIndices.xyz
			);

			// some provided tangents can be malformed (0, 0, 0) causing the normal to be degenerate
			// resulting in NaNs and slow path tracing.
			if ( length( tangentSample.xyz ) > 0.0 ) {

				vec3 tangent = normalize( tangentSample.xyz );
				vec3 bitangent = normalize( cross( normal, tangent ) * tangentSample.w );
				mat3 vTBN = mat3( tangent, bitangent, normal );

				vec3 uvPrime = material.normalMapTransform * vec3( uv, 1 );
				vec3 texNormal = texture2D( textures, vec3( uvPrime.xy, material.normalMap ) ).xyz * 2.0 - 1.0;
				texNormal.xy *= material.normalScale;
				normal = vTBN * texNormal;

			}

		}

		normal *= surfaceHit.side;

		// clearcoat
		float clearcoat = material.clearcoat;
		if ( material.clearcoatMap != - 1 ) {

			vec3 uvPrime = material.clearcoatMapTransform * vec3( uv, 1 );
			clearcoat *= texture2D( textures, vec3( uvPrime.xy, material.clearcoatMap ) ).r;

		}

		// clearcoatRoughness
		float clearcoatRoughness = material.clearcoatRoughness;
		if ( material.clearcoatRoughnessMap != - 1 ) {

			vec3 uvPrime = material.clearcoatRoughnessMapTransform * vec3( uv, 1 );
			clearcoatRoughness *= texture2D( textures, vec3( uvPrime.xy, material.clearcoatRoughnessMap ) ).g;

		}

		// clearcoatNormal
		vec3 clearcoatNormal = baseNormal;
		if ( material.clearcoatNormalMap != - 1 ) {

			vec4 tangentSample = textureSampleBarycoord(
				attributesArray,
				ATTR_TANGENT,
				surfaceHit.barycoord,
				surfaceHit.faceIndices.xyz
			);

			// some provided tangents can be malformed (0, 0, 0) causing the normal to be degenerate
			// resulting in NaNs and slow path tracing.
			if ( length( tangentSample.xyz ) > 0.0 ) {

				vec3 tangent = normalize( tangentSample.xyz );
				vec3 bitangent = normalize( cross( clearcoatNormal, tangent ) * tangentSample.w );
				mat3 vTBN = mat3( tangent, bitangent, clearcoatNormal );

				vec3 uvPrime = material.clearcoatNormalMapTransform * vec3( uv, 1 );
				vec3 texNormal = texture2D( textures, vec3( uvPrime.xy, material.clearcoatNormalMap ) ).xyz * 2.0 - 1.0;
				texNormal.xy *= material.clearcoatNormalScale;
				clearcoatNormal = vTBN * texNormal;

			}

		}

		clearcoatNormal *= surfaceHit.side;

		// sheenColor
		vec3 sheenColor = material.sheenColor;
		if ( material.sheenColorMap != - 1 ) {

			vec3 uvPrime = material.sheenColorMapTransform * vec3( uv, 1 );
			sheenColor *= texture2D( textures, vec3( uvPrime.xy, material.sheenColorMap ) ).rgb;

		}

		// sheenRoughness
		float sheenRoughness = material.sheenRoughness;
		if ( material.sheenRoughnessMap != - 1 ) {

			vec3 uvPrime = material.sheenRoughnessMapTransform * vec3( uv, 1 );
			sheenRoughness *= texture2D( textures, vec3( uvPrime.xy, material.sheenRoughnessMap ) ).a;

		}

		// iridescence
		float iridescence = material.iridescence;
		if ( material.iridescenceMap != - 1 ) {

			vec3 uvPrime = material.iridescenceMapTransform * vec3( uv, 1 );
			iridescence *= texture2D( textures, vec3( uvPrime.xy, material.iridescenceMap ) ).r;

		}

		// iridescence thickness
		float iridescenceThickness = material.iridescenceThicknessMaximum;
		if ( material.iridescenceThicknessMap != - 1 ) {

			vec3 uvPrime = material.iridescenceThicknessMapTransform * vec3( uv, 1 );
			float iridescenceThicknessSampled = texture2D( textures, vec3( uvPrime.xy, material.iridescenceThicknessMap ) ).g;
			iridescenceThickness = mix( material.iridescenceThicknessMinimum, material.iridescenceThicknessMaximum, iridescenceThicknessSampled );

		}

		iridescence = iridescenceThickness == 0.0 ? 0.0 : iridescence;

		// specular color
		vec3 specularColor = material.specularColor;
		if ( material.specularColorMap != - 1 ) {

			vec3 uvPrime = material.specularColorMapTransform * vec3( uv, 1 );
			specularColor *= texture2D( textures, vec3( uvPrime.xy, material.specularColorMap ) ).rgb;

		}

		// specular intensity
		float specularIntensity = material.specularIntensity;
		if ( material.specularIntensityMap != - 1 ) {

			vec3 uvPrime = material.specularIntensityMapTransform * vec3( uv, 1 );
			specularIntensity *= texture2D( textures, vec3( uvPrime.xy, material.specularIntensityMap ) ).a;

		}

		surf.volumeParticle = false;

		surf.faceNormal = surfaceHit.faceNormal;
		surf.normal = normal;

		surf.metalness = metalness;
		surf.color = albedo.rgb;
		surf.emission = emission;

		surf.ior = material.ior;
		surf.transmission = transmission;
		surf.thinFilm = material.thinFilm;
		surf.attenuationColor = material.attenuationColor;
		surf.attenuationDistance = material.attenuationDistance;

		surf.clearcoatNormal = clearcoatNormal;
		surf.clearcoat = clearcoat;

		surf.sheen = material.sheen;
		surf.sheenColor = sheenColor;

		surf.iridescence = iridescence;
		surf.iridescenceIor = material.iridescenceIor;
		surf.iridescenceThickness = iridescenceThickness;

		surf.specularColor = specularColor;
		surf.specularIntensity = specularIntensity;

		// apply perceptual roughness factor from gltf. sheen perceptual roughness is
		// applied by its brdf function
		// https://registry.khronos.org/glTF/specs/2.0/glTF-2.0.html#microfacet-surfaces
		surf.roughness = roughness * roughness;
		surf.clearcoatRoughness = clearcoatRoughness * clearcoatRoughness;
		surf.sheenRoughness = sheenRoughness;

		// frontFace is used to determine transmissive properties and PDF. If no transmission is used
		// then we can just always assume this is a front face.
		surf.frontFace = surfaceHit.side == 1.0 || transmission == 0.0;
		surf.eta = material.thinFilm || surf.frontFace ? 1.0 / material.ior : material.ior;
		surf.f0 = iorRatioToF0( surf.eta );

		// Compute the filtered roughness value to use during specular reflection computations.
		// The accumulated roughness value is scaled by a user setting and a "magic value" of 5.0.
		// If we're exiting something transmissive then scale the factor down significantly so we can retain
		// sharp internal reflections
		surf.filteredRoughness = applyFilteredGlossy( surf.roughness, accumulatedRoughness );
		surf.filteredClearcoatRoughness = applyFilteredGlossy( surf.clearcoatRoughness, accumulatedRoughness );

		// get the normal frames
		surf.normalBasis = getBasisFromNormal( surf.normal );
		surf.normalInvBasis = inverse( surf.normalBasis );

		surf.clearcoatBasis = getBasisFromNormal( surf.clearcoatNormal );
		surf.clearcoatInvBasis = inverse( surf.clearcoatBasis );

		return HIT_SURFACE;

	}
`;var Kn=`

	struct Ray {

		vec3 origin;
		vec3 direction;

	};

	struct SurfaceHit {

		uvec4 faceIndices;
		vec3 barycoord;
		vec3 faceNormal;
		float side;
		float dist;

	};

	struct RenderState {

		bool firstRay;
		bool transmissiveRay;
		bool isShadowRay;
		float accumulatedRoughness;
		int transmissiveTraversals;
		int traversals;
		uint depth;
		vec3 throughputColor;
		Material fogMaterial;

	};

	RenderState initRenderState() {

		RenderState result;
		result.firstRay = true;
		result.transmissiveRay = true;
		result.isShadowRay = false;
		result.accumulatedRoughness = 0.0;
		result.transmissiveTraversals = 0;
		result.traversals = 0;
		result.throughputColor = vec3( 1.0 );
		result.depth = 0u;
		result.fogMaterial.fogVolume = false;
		return result;

	}

`;var Zn=`

	#define NO_HIT 0
	#define SURFACE_HIT 1
	#define LIGHT_HIT 2
	#define FOG_HIT 3

	// Passing the global variable 'lights' into this function caused shader program errors.
	// So global variables like 'lights' and 'bvh' were moved out of the function parameters.
	// For more information, refer to: https://github.com/gkjohnson/three-gpu-pathtracer/pull/457
	int traceScene(
		Ray ray, Material fogMaterial, inout SurfaceHit surfaceHit
	) {

		int result = NO_HIT;
		bool hit = bvhIntersectFirstHit( bvh, ray.origin, ray.direction, surfaceHit.faceIndices, surfaceHit.faceNormal, surfaceHit.barycoord, surfaceHit.side, surfaceHit.dist );

		#if FEATURE_FOG

		if ( fogMaterial.fogVolume ) {

			// offset the distance so we don't run into issues with particles on the same surface
			// as other objects
			float particleDist = intersectFogVolume( fogMaterial, rand( 1 ) );
			if ( particleDist + RAY_OFFSET < surfaceHit.dist ) {

				surfaceHit.side = 1.0;
				surfaceHit.faceNormal = normalize( - ray.direction );
				surfaceHit.dist = particleDist;
				return FOG_HIT;

			}

		}

		#endif

		if ( hit ) {

			result = SURFACE_HIT;

		}

		return result;

	}

`;var Ct=class extends fe{onBeforeRender(){this.setDefine("FEATURE_DOF",this.physicalCamera.bokehSize===0?0:1),this.setDefine("FEATURE_BACKGROUND_MAP",this.backgroundMap?1:0),this.setDefine("FEATURE_FOG",this.materials.features.isUsed("FOG")?1:0)}constructor(e){super({transparent:!0,depthWrite:!1,defines:{FEATURE_MIS:1,FEATURE_RUSSIAN_ROULETTE:1,FEATURE_DOF:1,FEATURE_BACKGROUND_MAP:0,FEATURE_FOG:1,RANDOM_TYPE:2,CAMERA_TYPE:0,DEBUG_MODE:0,ATTR_NORMAL:0,ATTR_TANGENT:1,ATTR_UV:2,ATTR_COLOR:3},uniforms:{resolution:{value:new Y},opacity:{value:1},bounces:{value:10},transmissiveBounces:{value:10},filterGlossyFactor:{value:0},physicalCamera:{value:new wr},cameraWorldMatrix:{value:new O},invProjectionMatrix:{value:new O},bvh:{value:new lr},attributesArray:{value:new Rr},materialIndexAttribute:{value:new st},materials:{value:new Ir},textures:{value:new Mt().texture},lights:{value:new Ar},iesProfiles:{value:new Mt(360,180,{type:ee,wrapS:ae,wrapT:ae}).texture},environmentIntensity:{value:1},environmentRotation:{value:new O},envMapInfo:{value:new _r},backgroundBlur:{value:0},backgroundMap:{value:null},backgroundAlpha:{value:1},backgroundIntensity:{value:1},backgroundRotation:{value:new O},seed:{value:0},sobolTexture:{value:null},stratifiedTexture:{value:new Pr},stratifiedOffsetTexture:{value:new Er(64,1)}},vertexShader:`

				varying vec2 vUv;
				void main() {

					vec4 mvPosition = vec4( position, 1.0 );
					mvPosition = modelViewMatrix * mvPosition;
					gl_Position = projectionMatrix * mvPosition;

					vUv = uv;

				}

			`,fragmentShader:`
				#define RAY_OFFSET 1e-4
				#define INFINITY 1e20

				precision highp isampler2D;
				precision highp usampler2D;
				precision highp sampler2DArray;
				vec4 envMapTexelToLinear( vec4 a ) { return a; }
				#include <common>

				// bvh intersection
				${Ne.common_functions}
				${Ne.bvh_struct_definitions}
				${Ne.bvh_ray_functions}

				// uniform structs
				${Cn}
				${Fn}
				${In}
				${Bn}
				${Pn}

				// random
				#if RANDOM_TYPE == 2 	// Stratified List

					${zn}

				#elif RANDOM_TYPE == 1 	// Sobol

					${Lo}
					${br}
					${hn}

					#define rand(v) sobol(v)
					#define rand2(v) sobol2(v)
					#define rand3(v) sobol3(v)
					#define rand4(v) sobol4(v)

				#else 					// PCG

				${Lo}

					// Using the sobol functions seems to break the the compiler on MacOS
					// - specifically the "sobolReverseBits" function.
					uint sobolPixelIndex = 0u;
					uint sobolPathIndex = 0u;
					uint sobolBounceIndex = 0u;

					#define rand(v) pcgRand()
					#define rand2(v) pcgRand2()
					#define rand3(v) pcgRand3()
					#define rand4(v) pcgRand4()

				#endif

				// common
				${On}
				${Nn}
				${ft}
				${kn}
				${Gn}

				// environment
				uniform EquirectHdrInfo envMapInfo;
				uniform mat4 environmentRotation;
				uniform float environmentIntensity;

				// lighting
				uniform sampler2DArray iesProfiles;
				uniform LightsInfo lights;

				// background
				uniform float backgroundBlur;
				uniform float backgroundAlpha;
				#if FEATURE_BACKGROUND_MAP

				uniform sampler2D backgroundMap;
				uniform mat4 backgroundRotation;
				uniform float backgroundIntensity;

				#endif

				// camera
				uniform mat4 cameraWorldMatrix;
				uniform mat4 invProjectionMatrix;
				#if FEATURE_DOF

				uniform PhysicalCamera physicalCamera;

				#endif

				// geometry
				uniform sampler2DArray attributesArray;
				uniform usampler2D materialIndexAttribute;
				uniform sampler2D materials;
				uniform sampler2DArray textures;
				uniform BVH bvh;

				// path tracer
				uniform int bounces;
				uniform int transmissiveBounces;
				uniform float filterGlossyFactor;
				uniform int seed;

				// image
				uniform vec2 resolution;
				uniform float opacity;

				varying vec2 vUv;

				// globals
				mat3 envRotation3x3;
				mat3 invEnvRotation3x3;
				float lightsDenom;

				// sampling
				${Ln}
				${Dn}
				${En}

				${jn}
				${Wn}
				${qn}
				${Vn}
				${Hn}
				${Un}

				float applyFilteredGlossy( float roughness, float accumulatedRoughness ) {

					return clamp(
						max(
							roughness,
							accumulatedRoughness * filterGlossyFactor * 5.0 ),
						0.0,
						1.0
					);

				}

				vec3 sampleBackground( vec3 direction, vec2 uv ) {

					vec3 sampleDir = sampleHemisphere( direction, uv ) * 0.5 * backgroundBlur;

					#if FEATURE_BACKGROUND_MAP

					sampleDir = normalize( mat3( backgroundRotation ) * direction + sampleDir );
					return backgroundIntensity * sampleEquirectColor( backgroundMap, sampleDir );

					#else

					sampleDir = normalize( envRotation3x3 * direction + sampleDir );
					return environmentIntensity * sampleEquirectColor( envMapInfo.map, sampleDir );

					#endif

				}

				${Kn}
				${Yn}
				${Zn}
				${Xn}
				${$n}
				${Qn}

				void main() {

					// init
					rng_initialize( gl_FragCoord.xy, seed );
					sobolPixelIndex = ( uint( gl_FragCoord.x ) << 16 ) | uint( gl_FragCoord.y );
					sobolPathIndex = uint( seed );

					// get camera ray
					Ray ray = getCameraRay();

					// inverse environment rotation
					envRotation3x3 = mat3( environmentRotation );
					invEnvRotation3x3 = inverse( envRotation3x3 );
					lightsDenom =
						( environmentIntensity == 0.0 || envMapInfo.totalSum == 0.0 ) && lights.count != 0u ?
							float( lights.count ) :
							float( lights.count + 1u );

					// final color
					gl_FragColor = vec4( 0, 0, 0, 1 );

					// surface results
					SurfaceHit surfaceHit;
					ScatterRecord scatterRec;

					// path tracing state
					RenderState state = initRenderState();
					state.transmissiveTraversals = transmissiveBounces;
					#if FEATURE_FOG

					state.fogMaterial.fogVolume = bvhIntersectFogVolumeHit(
						ray.origin, - ray.direction,
						materialIndexAttribute, materials,
						state.fogMaterial
					);

					#endif

					for ( int i = 0; i < bounces; i ++ ) {

						sobolBounceIndex ++;

						state.depth ++;
						state.traversals = bounces - i;
						state.firstRay = i == 0 && state.transmissiveTraversals == transmissiveBounces;

						int hitType = traceScene( ray, state.fogMaterial, surfaceHit );

						// check if we intersect any lights and accumulate the light contribution
						// TODO: we can add support for light surface rendering in the else condition if we
						// add the ability to toggle visibility of the the light
						if ( ! state.firstRay && ! state.transmissiveRay ) {

							LightRecord lightRec;
							float lightDist = hitType == NO_HIT ? INFINITY : surfaceHit.dist;
							for ( uint i = 0u; i < lights.count; i ++ ) {

								if (
									intersectLightAtIndex( lights.tex, ray.origin, ray.direction, i, lightRec ) &&
									lightRec.dist < lightDist
								) {

									#if FEATURE_MIS

									// weight the contribution
									// NOTE: Only area lights are supported for forward sampling and can be hit
									float misWeight = misHeuristic( scatterRec.pdf, lightRec.pdf / lightsDenom );
									gl_FragColor.rgb += lightRec.emission * state.throughputColor * misWeight;

									#else

									gl_FragColor.rgb += lightRec.emission * state.throughputColor;

									#endif

								}

							}

						}

						if ( hitType == NO_HIT ) {

							if ( state.firstRay || state.transmissiveRay ) {

								gl_FragColor.rgb += sampleBackground( ray.direction, rand2( 2 ) ) * state.throughputColor;
								gl_FragColor.a = backgroundAlpha;

							} else {

								#if FEATURE_MIS

								// get the PDF of the hit envmap point
								vec3 envColor;
								float envPdf = sampleEquirect( envRotation3x3 * ray.direction, envColor );
								envPdf /= lightsDenom;

								// and weight the contribution
								float misWeight = misHeuristic( scatterRec.pdf, envPdf );
								gl_FragColor.rgb += environmentIntensity * envColor * state.throughputColor * misWeight;

								#else

								gl_FragColor.rgb +=
									environmentIntensity *
									sampleEquirectColor( envMapInfo.map, envRotation3x3 * ray.direction ) *
									state.throughputColor;

								#endif

							}
							break;

						}

						uint materialIndex = uTexelFetch1D( materialIndexAttribute, surfaceHit.faceIndices.x ).r;
						Material material = readMaterialInfo( materials, materialIndex );

						#if FEATURE_FOG

						if ( hitType == FOG_HIT ) {

							material = state.fogMaterial;
							state.accumulatedRoughness += 0.2;

						} else if ( material.fogVolume ) {

							state.fogMaterial = material;
							state.fogMaterial.fogVolume = surfaceHit.side == 1.0;

							ray.origin = stepRayOrigin( ray.origin, ray.direction, - surfaceHit.faceNormal, surfaceHit.dist );

							i -= sign( state.transmissiveTraversals );
							state.transmissiveTraversals -= sign( state.transmissiveTraversals );
							continue;

						}

						#endif

						// early out if this is a matte material
						if ( material.matte && state.firstRay ) {

							gl_FragColor = vec4( 0.0 );
							break;

						}

						// if we've determined that this is a shadow ray and we've hit an item with no shadow casting
						// then skip it
						if ( ! material.castShadow && state.isShadowRay ) {

							ray.origin = stepRayOrigin( ray.origin, ray.direction, - surfaceHit.faceNormal, surfaceHit.dist );
							continue;

						}

						SurfaceRecord surf;
						if (
							getSurfaceRecord(
								material, surfaceHit, attributesArray, state.accumulatedRoughness,
								surf
							) == SKIP_SURFACE
						) {

							// only allow a limited number of transparency discards otherwise we could
							// crash the context with too long a loop.
							i -= sign( state.transmissiveTraversals );
							state.transmissiveTraversals -= sign( state.transmissiveTraversals );

							ray.origin = stepRayOrigin( ray.origin, ray.direction, - surfaceHit.faceNormal, surfaceHit.dist );
							continue;

						}

						scatterRec = bsdfSample( - ray.direction, surf );
						state.isShadowRay = scatterRec.specularPdf < rand( 4 );

						bool isBelowSurface = ! surf.volumeParticle && dot( scatterRec.direction, surf.faceNormal ) < 0.0;
						vec3 hitPoint = stepRayOrigin( ray.origin, ray.direction, isBelowSurface ? - surf.faceNormal : surf.faceNormal, surfaceHit.dist );

						// next event estimation
						#if FEATURE_MIS

						gl_FragColor.rgb += directLightContribution( - ray.direction, surf, state, hitPoint );

						#endif

						// accumulate a roughness value to offset diffuse, specular, diffuse rays that have high contribution
						// to a single pixel resulting in fireflies
						// TODO: handle transmissive surfaces
						if ( ! surf.volumeParticle && ! isBelowSurface ) {

							// determine if this is a rough normal or not by checking how far off straight up it is
							vec3 halfVector = normalize( - ray.direction + scatterRec.direction );
							state.accumulatedRoughness += max(
								sin( acosApprox( dot( halfVector, surf.normal ) ) ),
								sin( acosApprox( dot( halfVector, surf.clearcoatNormal ) ) )
							);

							state.transmissiveRay = false;

						}

						// accumulate emissive color
						gl_FragColor.rgb += ( surf.emission * state.throughputColor );

						// skip the sample if our PDF or ray is impossible
						if ( scatterRec.pdf <= 0.0 || ! isDirectionValid( scatterRec.direction, surf.normal, surf.faceNormal ) ) {

							break;

						}

						// if we're bouncing around the inside a transmissive material then decrement
						// perform this separate from a bounce
						bool isTransmissiveRay = ! surf.volumeParticle && dot( scatterRec.direction, surf.faceNormal * surfaceHit.side ) < 0.0;
						if ( ( isTransmissiveRay || isBelowSurface ) && state.transmissiveTraversals > 0 ) {

							state.transmissiveTraversals --;
							i --;

						}

						//

						// handle throughput color transformation
						// attenuate the throughput color by the medium color
						if ( ! surf.frontFace ) {

							state.throughputColor *= transmissionAttenuation( surfaceHit.dist, surf.attenuationColor, surf.attenuationDistance );

						}

						#if FEATURE_RUSSIAN_ROULETTE

						// russian roulette path termination
						// https://www.arnoldrenderer.com/research/physically_based_shader_design_in_arnold.pdf
						uint minBounces = 3u;
						float depthProb = float( state.depth < minBounces );

						float rrProb = luminance( state.throughputColor * scatterRec.color / scatterRec.pdf );
						rrProb /= luminance( state.throughputColor );
						rrProb = sqrt( rrProb );
						rrProb = max( rrProb, depthProb );
						rrProb = min( rrProb, 1.0 );
						if ( rand( 8 ) > rrProb ) {

							break;

						}

						// perform sample clamping here to avoid bright pixels
						state.throughputColor *= min( 1.0 / rrProb, 20.0 );

						#endif

						// adjust the throughput and discard and exit if we find discard the sample if there are any NaNs
						state.throughputColor *= scatterRec.color / scatterRec.pdf;
						if ( any( isnan( state.throughputColor ) ) || any( isinf( state.throughputColor ) ) ) {

							break;

						}

						//

						// prepare for next ray
						ray.direction = scatterRec.direction;
						ray.origin = hitPoint;

					}

					gl_FragColor.a *= opacity;

					#if DEBUG_MODE == 1

					// output the number of rays checked in the path and number of
					// transmissive rays encountered.
					gl_FragColor.rgb = vec3(
						float( state.depth ),
						transmissiveBounces - state.transmissiveTraversals,
						0.0
					);
					gl_FragColor.a = 1.0;

					#endif

				}

			`}),this.setValues(e)}};function*ha(){let{_renderer:i,_fsQuad:e,_blendQuad:t,_primaryTarget:r,_blendTargets:n,_sobolTarget:s,_subframe:o,alpha:c,material:l}=this,m=new Te,p=new Te,f=t.material,[a,h]=n;for(;;){c?(f.opacity=this._opacityFactor/(this.samples+1),l.blending=me,l.opacity=1):(l.opacity=this._opacityFactor/(this.samples+1),l.blending=Et);let[x,b,d,g]=o,v=r.width,y=r.height;l.resolution.set(v*d,y*g),l.sobolTexture=s.texture,l.stratifiedTexture.init(20,l.bounces+l.transmissiveBounces+5),l.stratifiedTexture.next(),l.seed++;let w=this.tiles.x||1,T=this.tiles.y||1,S=w*T,_=Math.ceil(v*d),A=Math.ceil(y*g),M=Math.floor(x*v),C=Math.floor(b*y),F=Math.ceil(_/w),R=Math.ceil(A/T);for(let B=0;B<T;B++)for(let P=0;P<w;P++){let D=i.getRenderTarget(),H=i.autoClear,ge=i.getScissorTest();i.getScissor(m),i.getViewport(p);let be=P,Se=B;if(!this.stableTiles){let Gr=this._currentTile%(w*T);be=Gr%w,Se=~~(Gr/w),this._currentTile=Gr+1}let qo=T-Se-1;r.scissor.set(M+be*F,C+qo*R,Math.min(F,_-be*F),Math.min(R,A-qo*R)),r.viewport.set(M,C,_,A),i.setRenderTarget(r),i.setScissorTest(!0),i.autoClear=!1,e.render(i),i.setViewport(p),i.setScissor(m),i.setScissorTest(ge),i.setRenderTarget(D),i.autoClear=H,c&&(f.target1=a.texture,f.target2=r.texture,i.setRenderTarget(h),t.render(i),i.setRenderTarget(D)),this.samples+=1/S,P===w-1&&B===T-1&&(this.samples=Math.round(this.samples)),yield}[a,h]=[h,a]}}var Jn=new pe,pt=class{get material(){return this._fsQuad.material}set material(e){this._fsQuad.material.removeEventListener("recompilation",this._compileFunction),e.addEventListener("recompilation",this._compileFunction),this._fsQuad.material=e}get target(){return this._alpha?this._blendTargets[1]:this._primaryTarget}set alpha(e){this._alpha!==e&&(e||(this._blendTargets[0].dispose(),this._blendTargets[1].dispose()),this._alpha=e,this.reset())}get alpha(){return this._alpha}get isCompiling(){return!!this._compilePromise}constructor(e){this.camera=null,this.tiles=new Y(3,3),this.stableNoise=!1,this.stableTiles=!0,this.samples=0,this._subframe=new Te(0,0,1,1),this._opacityFactor=1,this._renderer=e,this._alpha=!1,this._fsQuad=new oe(new Ct),this._blendQuad=new oe(new gr),this._task=null,this._currentTile=0,this._compilePromise=null,this._sobolTarget=new Tr().generate(e),this._primaryTarget=new ve(1,1,{format:E,type:N,magFilter:k,minFilter:k}),this._blendTargets=[new ve(1,1,{format:E,type:N,magFilter:k,minFilter:k}),new ve(1,1,{format:E,type:N,magFilter:k,minFilter:k})],this._compileFunction=()=>{let t=this.compileMaterial(this._fsQuad._mesh);t.then(()=>{this._compilePromise===t&&(this._compilePromise=null)}),this._compilePromise=t},this.material.addEventListener("recompilation",this._compileFunction)}compileMaterial(){return this._renderer.compileAsync(this._fsQuad._mesh)}setCamera(e){let{material:t}=this;t.cameraWorldMatrix.copy(e.matrixWorld),t.invProjectionMatrix.copy(e.projectionMatrixInverse),t.physicalCamera.updateFrom(e);let r=0;e.projectionMatrix.elements[15]>0&&(r=1),e.isEquirectCamera&&(r=2),t.setDefine("CAMERA_TYPE",r),this.camera=e}setSize(e,t){e=Math.ceil(e),t=Math.ceil(t),!(this._primaryTarget.width===e&&this._primaryTarget.height===t)&&(this._primaryTarget.setSize(e,t),this._blendTargets[0].setSize(e,t),this._blendTargets[1].setSize(e,t),this.reset())}getSize(e){e.x=this._primaryTarget.width,e.y=this._primaryTarget.height}dispose(){this._primaryTarget.dispose(),this._blendTargets[0].dispose(),this._blendTargets[1].dispose(),this._sobolTarget.dispose(),this._fsQuad.dispose(),this._blendQuad.dispose(),this._task=null}reset(){let{_renderer:e,_primaryTarget:t,_blendTargets:r}=this,n=e.getRenderTarget(),s=e.getClearAlpha();e.getClearColor(Jn),e.setRenderTarget(t),e.setClearColor(0,0),e.clearColor(),e.setRenderTarget(r[0]),e.setClearColor(0,0),e.clearColor(),e.setRenderTarget(r[1]),e.setClearColor(0,0),e.clearColor(),e.setClearColor(Jn,s),e.setRenderTarget(n),this.samples=0,this._task=null,this.material.stratifiedTexture.stableNoise=this.stableNoise,this.stableNoise&&(this.material.seed=0,this.material.stratifiedTexture.reset())}update(){this.material.onBeforeRender(),!this.isCompiling&&(this._task||(this._task=ha.call(this)),this._task.next())}};var He=new Y,es=new Y,Lr=new ui,Nr=new pe,It=class extends W{constructor(e=512,t=512){super(new Float32Array(e*t*4),e,t,E,N,Ve,le,ae,te,te),this.generationCallback=null}update(){this.dispose(),this.needsUpdate=!0;let{data:e,width:t,height:r}=this.image;for(let n=0;n<t;n++)for(let s=0;s<r;s++){es.set(t,r),He.set(n/t,s/r),He.x-=.5,He.y=1-He.y,Lr.theta=He.x*2*Math.PI,Lr.phi=He.y*Math.PI,Lr.radius=1,this.generationCallback(Lr,He,es,Nr);let c=4*(s*t+n);e[c+0]=Nr.r,e[c+1]=Nr.g,e[c+2]=Nr.b,e[c+3]=1}}copy(e){return super.copy(e),this.generationCallback=e.generationCallback,this}};var ts=new I,Ft=class extends It{constructor(e=512){super(e,e),this.topColor=new pe().set(16777215),this.bottomColor=new pe().set(0),this.exponent=2,this.generationCallback=(t,r,n,s)=>{ts.setFromSpherical(t);let o=ts.y*.5+.5;s.lerpColors(this.bottomColor,this.topColor,o**this.exponent)}}copy(e){return super.copy(e),this.topColor.copy(e.topColor),this.bottomColor.copy(e.bottomColor),this}};var kr=class extends _e{get map(){return this.uniforms.map.value}set map(e){this.uniforms.map.value=e}get opacity(){return this.uniforms.opacity.value}set opacity(e){this.uniforms&&(this.uniforms.opacity.value=e)}constructor(e){super({uniforms:{map:{value:null},opacity:{value:1}},vertexShader:`
				varying vec2 vUv;
				void main() {

					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

				}
			`,fragmentShader:`
				uniform sampler2D map;
				uniform float opacity;
				varying vec2 vUv;

				vec4 clampedTexelFatch( sampler2D map, ivec2 px, int lod ) {

					vec4 res = texelFetch( map, ivec2( px.x, px.y ), 0 );

					#if defined( TONE_MAPPING )

					res.xyz = toneMapping( res.xyz );

					#endif

			  		return linearToOutputTexel( res );

				}

				void main() {

					vec2 size = vec2( textureSize( map, 0 ) );
					vec2 pxUv = vUv * size;
					vec2 pxCurr = floor( pxUv );
					vec2 pxFrac = fract( pxUv ) - 0.5;
					vec2 pxOffset;
					pxOffset.x = pxFrac.x > 0.0 ? 1.0 : - 1.0;
					pxOffset.y = pxFrac.y > 0.0 ? 1.0 : - 1.0;

					vec2 pxNext = clamp( pxOffset + pxCurr, vec2( 0.0 ), size - 1.0 );
					vec2 alpha = abs( pxFrac );

					vec4 p1 = mix(
						clampedTexelFatch( map, ivec2( pxCurr.x, pxCurr.y ), 0 ),
						clampedTexelFatch( map, ivec2( pxNext.x, pxCurr.y ), 0 ),
						alpha.x
					);

					vec4 p2 = mix(
						clampedTexelFatch( map, ivec2( pxCurr.x, pxNext.y ), 0 ),
						clampedTexelFatch( map, ivec2( pxNext.x, pxNext.y ), 0 ),
						alpha.x
					);

					gl_FragColor = mix( p1, p2, alpha.y );
					gl_FragColor.a *= opacity;
					#include <premultiplied_alpha_fragment>

				}
			`}),this.setValues(e)}};var No=class extends _e{constructor(){super({uniforms:{envMap:{value:null},flipEnvMap:{value:-1}},vertexShader:`
				varying vec2 vUv;
				void main() {

					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

				}`,fragmentShader:`
				#define ENVMAP_TYPE_CUBE_UV

				uniform samplerCube envMap;
				uniform float flipEnvMap;
				varying vec2 vUv;

				#include <common>
				#include <cube_uv_reflection_fragment>

				${ft}

				void main() {

					vec3 rayDirection = equirectUvToDirection( vUv );
					rayDirection.x *= flipEnvMap;
					gl_FragColor = textureCube( envMap, rayDirection );

				}`}),this.depthWrite=!1,this.depthTest=!1}},We=class{constructor(e){this._renderer=e,this._quad=new oe(new No)}generate(e,t=null,r=null){if(!e.isCubeTexture)throw new Error("CubeToEquirectMaterial: Source can only be cube textures.");let n=e.images[0],s=this._renderer,o=this._quad;t===null&&(t=4*n.height),r===null&&(r=2*n.height);let c=new ve(t,r,{type:N,colorSpace:n.colorSpace}),l=n.height,m=Math.log2(l)-2,p=1/l,f=1/(3*Math.max(Math.pow(2,m),7*16));o.material.defines.CUBEUV_MAX_MIP=`${m}.0`,o.material.defines.CUBEUV_TEXEL_WIDTH=f,o.material.defines.CUBEUV_TEXEL_HEIGHT=p,o.material.uniforms.envMap.value=e,o.material.uniforms.flipEnvMap.value=e.isRenderTargetTexture?1:-1,o.material.needsUpdate=!0;let a=s.getRenderTarget(),h=s.autoClear;s.autoClear=!0,s.setRenderTarget(c),o.render(s),s.setRenderTarget(a),s.autoClear=h;let x=new Uint16Array(t*r*4),b=new Float32Array(t*r*4);s.readRenderTargetPixels(c,0,0,t,r,b),c.dispose();for(let g=0,v=b.length;g<v;g++)x[g]=ie.toHalfFloat(b[g]);let d=new W(x,t,r,E,ee);return d.minFilter=Zo,d.magFilter=te,d.wrapS=le,d.wrapT=le,d.mapping=Ve,d.needsUpdate=!0,d}dispose(){this._quad.dispose()}};function xa(i){return i.extensions.get("EXT_float_blend")}var mt=new Y,ko=class{get multipleImportanceSampling(){return!!this._pathTracer.material.defines.FEATURE_MIS}set multipleImportanceSampling(e){this._pathTracer.material.setDefine("FEATURE_MIS",e?1:0)}get transmissiveBounces(){return this._pathTracer.material.transmissiveBounces}set transmissiveBounces(e){this._pathTracer.material.transmissiveBounces=e}get bounces(){return this._pathTracer.material.bounces}set bounces(e){this._pathTracer.material.bounces=e}get filterGlossyFactor(){return this._pathTracer.material.filterGlossyFactor}set filterGlossyFactor(e){this._pathTracer.material.filterGlossyFactor=e}get samples(){return this._pathTracer.samples}get target(){return this._pathTracer.target}get tiles(){return this._pathTracer.tiles}get stableNoise(){return this._pathTracer.stableNoise}set stableNoise(e){this._pathTracer.stableNoise=e}get isCompiling(){return!!this._pathTracer.isCompiling}constructor(e){this._renderer=e,this._generator=new Ue,this._pathTracer=new pt(e),this._queueReset=!1,this._clock=new Qo,this._compilePromise=null,this._lowResPathTracer=new pt(e),this._lowResPathTracer.tiles.set(1,1),this._quad=new oe(new kr({map:null,transparent:!0,blending:me,premultipliedAlpha:e.getContextAttributes().premultipliedAlpha})),this._materials=null,this._previousEnvironment=null,this._previousBackground=null,this._internalBackground=null,this.renderDelay=100,this.minSamples=5,this.fadeDuration=500,this.enablePathTracing=!0,this.pausePathTracing=!1,this.dynamicLowRes=!1,this.lowResScale=.25,this.renderScale=1,this.synchronizeRenderSize=!0,this.rasterizeScene=!0,this.renderToCanvas=!0,this.textureSize=new Y(1024,1024),this.rasterizeSceneCallback=(t,r)=>{this._renderer.render(t,r)},this.renderToCanvasCallback=(t,r,n)=>{let s=r.autoClear;r.autoClear=!1,n.render(r),r.autoClear=s},this.setScene(new ai,new Lt)}setBVHWorker(e){this._generator.setBVHWorker(e)}setScene(e,t,r={}){e.updateMatrixWorld(!0),t.updateMatrixWorld();let n=this._generator;if(n.setObjects(e),this._buildAsync)return n.generateAsync(r.onProgress).then(s=>this._updateFromResults(e,t,s));{let s=n.generate();return this._updateFromResults(e,t,s)}}setSceneAsync(...e){this._buildAsync=!0;let t=this.setScene(...e);return this._buildAsync=!1,t}setCamera(e){this.camera=e,this.updateCamera()}updateCamera(){let e=this.camera;e.updateMatrixWorld(),this._pathTracer.setCamera(e),this._lowResPathTracer.setCamera(e),this.reset()}updateMaterials(){let e=this._pathTracer.material,t=this._renderer,r=this._materials,n=this.textureSize,s=Tn(r);e.textures.setTextures(t,s,n.x,n.y),e.materials.updateFrom(r,s),this.reset()}updateLights(){let e=this.scene,t=this._renderer,r=this._pathTracer.material,n=wn(e),s=bn(n);r.lights.updateFrom(n,s),r.iesProfiles.setTextures(t,s),this.reset()}updateEnvironment(){let e=this.scene,t=this._pathTracer.material;if(this._internalBackground&&(this._internalBackground.dispose(),this._internalBackground=null),t.backgroundBlur=e.backgroundBlurriness,t.backgroundIntensity=e.backgroundIntensity??1,t.backgroundRotation.makeRotationFromEuler(e.backgroundRotation).invert(),e.background===null)t.backgroundMap=null,t.backgroundAlpha=0;else if(e.background.isColor){this._colorBackground=this._colorBackground||new Ft(16);let r=this._colorBackground;r.topColor.equals(e.background)||(r.topColor.set(e.background),r.bottomColor.set(e.background),r.update()),t.backgroundMap=r,t.backgroundAlpha=1}else if(e.background.isCubeTexture){if(e.background!==this._previousBackground){let r=new We(this._renderer).generate(e.background);this._internalBackground=r,t.backgroundMap=r,t.backgroundAlpha=1}}else t.backgroundMap=e.background,t.backgroundAlpha=1;if(t.environmentIntensity=e.environmentIntensity??1,t.environmentRotation.makeRotationFromEuler(e.environmentRotation).invert(),this._previousEnvironment!==e.environment)if(e.environment!==null)if(e.environment.isCubeTexture){let r=new We(this._renderer).generate(e.environment);t.envMapInfo.updateFrom(r)}else t.envMapInfo.updateFrom(e.environment);else t.environmentIntensity=0;this._previousEnvironment=e.environment,this._previousBackground=e.background,this.reset()}_updateFromResults(e,t,r){let{materials:n,geometry:s,bvh:o,bvhChanged:c}=r;this._materials=n;let m=this._pathTracer.material;return c&&(m.bvh.updateFrom(o),m.attributesArray.updateFrom(s.attributes.normal,s.attributes.tangent,s.attributes.uv,s.attributes.color),m.materialIndexAttribute.updateFrom(s.attributes.materialIndex)),this._previousScene=e,this.scene=e,this.camera=t,this.updateCamera(),this.updateMaterials(),this.updateEnvironment(),this.updateLights(),r}renderSample(){let e=this._lowResPathTracer,t=this._pathTracer,r=this._renderer,n=this._clock,s=this._quad;this._updateScale(),this._queueReset&&(t.reset(),e.reset(),this._queueReset=!1,s.material.opacity=0,n.start());let o=n.getDelta()*1e3,c=n.getElapsedTime()*1e3;if(!this.pausePathTracing&&this.enablePathTracing&&this.renderDelay<=c&&!this.isCompiling&&t.update(),t.alpha=t.material.backgroundAlpha!==1||!xa(r),e.alpha=t.alpha,this.renderToCanvas){let l=this._renderer,m=this.minSamples;if(c>=this.renderDelay&&this.samples>=this.minSamples&&(this.fadeDuration!==0?s.material.opacity=Math.min(s.material.opacity+o/this.fadeDuration,1):s.material.opacity=1),!this.enablePathTracing||this.samples<m||s.material.opacity<1){if(this.dynamicLowRes&&!this.isCompiling){e.samples<1&&(e.material=t.material,e.update());let p=s.material.opacity;s.material.opacity=1-s.material.opacity,s.material.map=e.target.texture,s.render(l),s.material.opacity=p}(!this.dynamicLowRes&&this.rasterizeScene||this.dynamicLowRes&&this.isCompiling)&&this.rasterizeSceneCallback(this.scene,this.camera)}this.enablePathTracing&&s.material.opacity>0&&(s.material.opacity<1&&(s.material.blending=this.dynamicLowRes?Xo:Et),s.material.map=t.target.texture,this.renderToCanvasCallback(t.target,l,s),s.material.blending=me)}}reset(){this._queueReset=!0,this._pathTracer.samples=0}dispose(){this._renderQuad.dispose(),this._renderQuad.material.dispose(),this._pathTracer.dispose()}_updateScale(){if(this.synchronizeRenderSize){this._renderer.getDrawingBufferSize(mt);let e=Math.floor(this.renderScale*mt.x),t=Math.floor(this.renderScale*mt.y);if(this._pathTracer.getSize(mt),mt.x!==e||mt.y!==t){let r=this.lowResScale;this._pathTracer.setSize(e,t),this._lowResPathTracer.setSize(Math.floor(e*r),Math.floor(t*r))}}}};var Go=class extends $o{constructor(){super(),this.isEquirectCamera=!0}};var Oo=class extends fi{constructor(...e){super(...e),this.iesMap=null,this.radius=0}copy(e,t){return super.copy(e,t),this.iesMap=e.iesMap,this.radius=e.radius,this}};var zo=class extends ni{constructor(...e){super(...e),this.isCircular=!1}copy(e,t){return super.copy(e,t),this.isCircular=e.isCircular,this}};var Uo=class extends fe{constructor(){super({uniforms:{envMap:{value:null},blur:{value:0}},vertexShader:`

				varying vec2 vUv;
				void main() {
					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );
				}

			`,fragmentShader:`

				#include <common>
				#include <cube_uv_reflection_fragment>

				${ft}

				uniform sampler2D envMap;
				uniform float blur;
				varying vec2 vUv;
				void main() {

					vec3 rayDirection = equirectUvToDirection( vUv );
					gl_FragColor = textureCubeUV( envMap, rayDirection, blur );

				}

			`})}},Ho=class{constructor(e){this.renderer=e,this.pmremGenerator=new ri(e),this.copyQuad=new oe(new Uo),this.renderTarget=new ve(1,1,{type:N,format:E})}dispose(){this.pmremGenerator.dispose(),this.copyQuad.dispose(),this.renderTarget.dispose()}generate(e,t){let{pmremGenerator:r,renderTarget:n,copyQuad:s,renderer:o}=this,c=r.fromEquirectangular(e),{width:l,height:m}=e.image;n.setSize(l,m),s.material.envMap=c.texture,s.material.blur=t;let p=o.getRenderTarget(),f=o.autoClear;o.setRenderTarget(n),o.autoClear=!0,s.render(o),o.setRenderTarget(p),o.autoClear=f;let a=new Uint16Array(l*m*4),h=new Float32Array(l*m*4);o.readRenderTargetPixels(n,0,0,l,m,h);for(let b=0,d=h.length;b<d;b++)a[b]=ie.toHalfFloat(h[b]);let x=new W(a,l,m,E,ee);return x.minFilter=e.minFilter,x.magFilter=e.magFilter,x.wrapS=e.wrapS,x.wrapT=e.wrapT,x.mapping=Ve,x.needsUpdate=!0,c.dispose(),x}};var Wo=class extends fe{constructor(e){super({blending:me,transparent:!1,depthWrite:!1,depthTest:!1,defines:{USE_SLIDER:0},uniforms:{sigma:{value:5},threshold:{value:.03},kSigma:{value:1},map:{value:null},opacity:{value:1}},vertexShader:`

				varying vec2 vUv;

				void main() {

					vUv = uv;
					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

				}

			`,fragmentShader:`

				//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
				//  Copyright (c) 2018-2019 Michele Morrone
				//  All rights reserved.
				//
				//  https://michelemorrone.eu - https://BrutPitt.com
				//
				//  me@michelemorrone.eu - brutpitt@gmail.com
				//  twitter: @BrutPitt - github: BrutPitt
				//
				//  https://github.com/BrutPitt/glslSmartDeNoise/
				//
				//  This software is distributed under the terms of the BSD 2-Clause license
				//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

				uniform sampler2D map;

				uniform float sigma;
				uniform float threshold;
				uniform float kSigma;
				uniform float opacity;

				varying vec2 vUv;

				#define INV_SQRT_OF_2PI 0.39894228040143267793994605993439
				#define INV_PI 0.31830988618379067153776752674503

				// Parameters:
				//	 sampler2D tex	 - sampler image / texture
				//	 vec2 uv		   - actual fragment coord
				//	 float sigma  >  0 - sigma Standard Deviation
				//	 float kSigma >= 0 - sigma coefficient
				//		 kSigma * sigma  -->  radius of the circular kernel
				//	 float threshold   - edge sharpening threshold
				vec4 smartDeNoise( sampler2D tex, vec2 uv, float sigma, float kSigma, float threshold ) {

					float radius = round( kSigma * sigma );
					float radQ = radius * radius;

					float invSigmaQx2 = 0.5 / ( sigma * sigma );
					float invSigmaQx2PI = INV_PI * invSigmaQx2;

					float invThresholdSqx2 = 0.5 / ( threshold * threshold );
					float invThresholdSqrt2PI = INV_SQRT_OF_2PI / threshold;

					vec4 centrPx = texture2D( tex, uv );
					centrPx.rgb *= centrPx.a;

					float zBuff = 0.0;
					vec4 aBuff = vec4( 0.0 );
					vec2 size = vec2( textureSize( tex, 0 ) );

					vec2 d;
					for ( d.x = - radius; d.x <= radius; d.x ++ ) {

						float pt = sqrt( radQ - d.x * d.x );

						for ( d.y = - pt; d.y <= pt; d.y ++ ) {

							float blurFactor = exp( - dot( d, d ) * invSigmaQx2 ) * invSigmaQx2PI;

							vec4 walkPx = texture2D( tex, uv + d / size );
							walkPx.rgb *= walkPx.a;

							vec4 dC = walkPx - centrPx;
							float deltaFactor = exp( - dot( dC.rgba, dC.rgba ) * invThresholdSqx2 ) * invThresholdSqrt2PI * blurFactor;

							zBuff += deltaFactor;
							aBuff += deltaFactor * walkPx;

						}

					}

					return aBuff / zBuff;

				}

				void main() {

					gl_FragColor = smartDeNoise( map, vec2( vUv.x, vUv.y ), sigma, kSigma, threshold );
					#include <tonemapping_fragment>
					#include <colorspace_fragment>
					#include <premultiplied_alpha_fragment>

					gl_FragColor.a *= opacity;

				}

			`}),this.setValues(e)}};var Vo=class extends Jo{constructor(e){super(e),this.isFogVolumeMaterial=!0,this.density=.015,this.emissive=new pe,this.emissiveIntensity=0,this.opacity=.15,this.transparent=!0,this.roughness=1,this.metalness=0,this.setValues(e)}};return ss(ga);})();
